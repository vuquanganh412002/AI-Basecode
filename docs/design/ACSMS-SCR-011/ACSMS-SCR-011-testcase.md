---
customer_name: 日本農業新聞様
system_name: クラウド版購読者管理システム
document_name: テスト仕様書
screen_id: ACSMS-SCR-011
screen_name: 購読者情報登録画面
format_code: 16-BM/PM/VTI
format_version: "1.0"
issue_date: 2026-06-03
test_level: 結合テスト
test_environment: Windows 10/11, Chrome, Edge
author: Kieu Thi Diem
reviewer: Nguyen Huy Dat
---


## 変更履歴

| No. | 発行日 | 版数 | 担当者 | 変更内容 | 確認者 | 承認者 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-06-03 | 1.0 | Kieu Thi Diem | 新規作成 | Nguyen Huy Dat |  |


## システム概要

本システムは、JA向けのクラウド型読者管理システムであり、
購読者情報の管理、購読履歴の管理、口座振替データの管理などの機能を提供する。
主な機能として、購読者情報の登録・更新・検索、
購読内容の変更履歴管理、口座振替データの作成および管理、
ファイルのアップロード・ダウンロード機能、システムのお知らせ管理などを提供する。
また、ユーザーのログイン管理、ログイン履歴の記録、
ユーザー操作ログの記録などのセキュリティ・監査機能をサポートする。

## 資料目的

「購読者情報登録画面（ACSMS-SCR-011）」において、システム上で新規作成されるテスト仕様書の詳細を記述した資料です。
本資料はISTQBおよびIEEE 829を参考とし、以下の品質基準を満たすものとする。

- 各テストケースは単一のシナリオに基づき作成する（単一責任）。
- 手順は再現可能な粒度で記述し、テストデータを明示する。
- 期待結果は測定可能とする（メッセージ内容、DBクエリ結果、HTTPステータスコード等）。

## 関連資料

| No | 資料コード | 資料名 |
| --- | --- | --- |
| 1 | ACSMS-SCR-011 | 購読者情報登録画面設計書 |
| 2 | ACSMS-SCR-011-api | 購読者情報登録画面 API設計書 |
| 3 | testcase-viewpoints | 全システム共通 テスト観点一覧 |


## テストカテゴリ一覧

| # | カテゴリ | テストケース数 |
| --- | --- | --- |
| 1 | アクセス権限制御（Access Control） | 6 |
| 2 | 画面表示（Layout & Rendering） | 7 |
| 3 | 入力チェック（Input Validation） | 10 |
| 4 | 業務ロジック登録（Function — Create） | 5 |
| 5 | 業務ロジック更新（編集・承認・否認） | 8 |
| 6 | 共通エラーハンドリング（Common Error Handling） | 14 |
|  | 合計 | 50 |

---

# カテゴリ 1: アクセス権限制御（Access Control）

## ACSMS-TC-011-001 — 未認証ユーザーによる購読者情報登録画面アクセス禁止

- 観点ID: VP-A-05
- 種類: Abnormal (異常)
- 前提条件:
  - ・未ログイン（セッションCookieなし、または失効済）

### 手順

ステップ1：
ブラウザのアドレスバーに `/dokusya/create` を入力し、直接アクセス

ステップ2：
DevTools の Network タブで GET `/api/v1/dokusya/1` を直接呼び出し

### 期待結果

ステップ1：
購読者情報登録画面が表示されないこと、`/login` へ遷移すること（クエリ `?redirect=/dokusya/create` が付与されること）

ステップ2：
HTTPステータスコード401が返却されること（`error_code: UNAUTHORIZED`、メッセージ `セッションが切れました。再度ログインしてください。`）

補足：
・購読者情報登録画面は認証必須画面であり、未認証時はフロントエンド側ルーターガードでログイン画面へ誘導されること
・バックエンド側 SessionAuthGuard でも401が返却されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-002 — NICHINO_ADMIN による購読者情報登録画面アクセス禁止

- 観点ID: VP-A-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・権限「dokusya.create」「dokusya.view」「dokusya.update」をいずれも持たない

### 手順

ステップ1：
メニュー画面を開き、購読者管理メニューが表示されないことを確認

ステップ2：
ブラウザのアドレスバーに `/dokusya/create` を入力し、直接アクセス

ステップ3：
DevTools の Network タブで POST `/api/v1/dokusya` を有効なリクエストボディで送信

ステップ4：
DBで以下クエリを実行し、ログを確認
```sql
SELECT * FROM t_log
WHERE result_status = 2
  AND target_table = 't_dokusya'
ORDER BY log_datetime DESC
LIMIT 1;
```

### 期待結果

ステップ1：
購読者管理メニューが表示されないこと

ステップ2：
トースト `アクセス権がありません。` が表示されること、かつ `/dashboard` へ遷移すること

ステップ3：
HTTPステータスコード403が返却されること（`error_code: FORBIDDEN`、メッセージ `この画面へのアクセス権限がありません。`）

ステップ4：
・t_dokusya に新規データが登録されないこと
・権限のないユーザーはアクセスできないこと

補足：
・フロントエンド側メニュー、フロントエンド側ルーターガード、バックエンド側 API ガードの3層すべてでアクセスが制御されること
・`account_concept.md` 権限マトリクスにより NICHINO_ADMIN は購読者管理 ×

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-003 — NICHINO_STAFF による購読者情報登録画面アクセス禁止

- 観点ID: VP-A-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_STAFF
  - ・ログイン済 + MFA認証済
  - ・権限「dokusya.create」「dokusya.view」「dokusya.update」をいずれも持たない

### 手順

ステップ1：
ブラウザのアドレスバーに `/dokusya/create` を入力し、直接アクセス

ステップ2：
DevTools の Network タブで GET `/api/v1/dokusya/1` を直接呼び出し

ステップ3：
DevTools の Network タブで POST `/api/v1/dokusya` を有効なリクエストボディで送信

### 期待結果

ステップ1：
トースト `アクセス権がありません。` が表示されること、かつ `/dashboard` へ遷移すること

ステップ2：
HTTPステータスコード403が返却されること（`error_code: FORBIDDEN`、メッセージ `この画面へのアクセス権限がありません。`）

ステップ3：
HTTPステータスコード403が返却されること（`error_code: FORBIDDEN`、メッセージ `この画面へのアクセス権限がありません。`）

補足：
・NICHINO_STAFF 自身は `dokusya.create` / `dokusya.view` / `dokusya.update` を持たないため本画面を直接利用できないこと
・NICHINO_STAFF が販売店代行入力を行う場合はJAのアカウントを借りて操作する運用であり、本画面への直接アクセスは権限の対象外であること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-004 — CHUOKAI による購読者登録・参照（自中央会スコープ）

- 観点ID: VP-A-02
- 種類: Normal (正常)
- 前提条件:
  - ・role: CHUOKAI（chuokai-001 所属, ja_id=100）
  - ・権限「dokusya.create」「dokusya.view」「dokusya.update」を持つ
  - ・自中央会管轄JA（ja_id=100）の購読者が存在
  - ・他中央会管轄JA（ja_id=200）の購読者が存在

### 手順

ステップ1：
ログイン後、購読者情報登録画面（新規作成モード）を開く

ステップ2：
管理支店・販売店プルダウンの選択肢を確認

ステップ3：
自中央会管轄JA（ja_id=100）の購読者を GET `/api/v1/dokusya/{dokusya_id}` で取得

ステップ4：
他中央会管轄JA（ja_id=200）の購読者IDで GET `/api/v1/dokusya/{他中央会dokusya_id}` を呼び出し

### 期待結果

ステップ1：
購読者情報登録画面が表示されること

ステップ2：
自中央会管轄の管理支店・販売店のみが選択肢に表示されること

ステップ3：
HTTPステータスコード200が返却されること（自中央会管轄JAの購読者データが取得できること）

ステップ4：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`、メッセージ `指定された購読者が見つかりません。`）（他中央会のレコードは存在隠蔽されること）

補足：
・CHUOKAI は自中央会のみ参照・登録可能であること（管轄JAの読者個人情報は直接参照しない運用だが、システム上のスコープは自中央会管轄JAに限定）
・DataScope 違反（他中央会のレコードへの参照アクセス）は404で存在隠蔽されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-005 — JA_HONTEN による購読者登録・参照（自JAスコープ）

- 観点ID: VP-A-02
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・権限「dokusya.create」「dokusya.view」「dokusya.update」を持つ
  - ・自JA（ja_id=100）の購読者が存在
  - ・他JA（ja_id=200）の購読者が存在

### 手順

ステップ1：
ログイン後、購読者情報登録画面（新規作成モード）を開く

ステップ2：
自JA（ja_id=100）の購読者を GET `/api/v1/dokusya/{dokusya_id}` で取得

ステップ3：
他JA（ja_id=200）の購読者IDで GET `/api/v1/dokusya/{他JAdokusya_id}` を呼び出し

ステップ4：
DBで以下クエリを実行し、取得対象のスコープを確認
```sql
SELECT dokusya_id, ja_id FROM t_dokusya
WHERE dokusya_id = :dokusya_id
  AND deleted_at IS NULL;
```

### 期待結果

ステップ1：
購読者情報登録画面が表示されること

ステップ2：
HTTPステータスコード200が返却されること（自JA（ja_id=100）の購読者データが取得できること）

ステップ3：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`、メッセージ `指定された購読者が見つかりません。`）（他JAのレコードは存在隠蔽されること）

ステップ4：
取得対象レコードの `ja_id` が 100（自JA）であること

補足：
・JA_HONTEN は自JA（`ja_id = user.ja_id`）のみ参照・登録可能であること
・他JA（ja_id=200）のレコードへの参照アクセスは404で存在隠蔽されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-006 — JA_KANRI_SHITEN による購読者登録・参照（自管理支店スコープ）

- 観点ID: VP-A-02
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_KANRI_SHITEN（ja_id=100 / kanri_shiten_id=10 所属）
  - ・権限「dokusya.create」「dokusya.view」「dokusya.update」を持つ
  - ・自管理支店（kanri_shiten_id=10）の購読者が存在
  - ・他管理支店（kanri_shiten_id=11、同JA配下）の購読者が存在

### 手順

ステップ1：
ログイン後、購読者情報登録画面（新規作成モード）を開く

ステップ2：
自管理支店（kanri_shiten_id=10）の購読者を GET `/api/v1/dokusya/{dokusya_id}` で取得

ステップ3：
他管理支店（kanri_shiten_id=11）の購読者IDで GET `/api/v1/dokusya/{他管理支店dokusya_id}` を呼び出し

ステップ4：
DBで以下クエリを実行し、取得対象のスコープを確認
```sql
SELECT dokusya_id, ja_id, kanri_shiten_id FROM t_dokusya
WHERE dokusya_id = :dokusya_id
  AND deleted_at IS NULL;
```

### 期待結果

ステップ1：
購読者情報登録画面が表示されること

ステップ2：
HTTPステータスコード200が返却されること（自管理支店（kanri_shiten_id=10）の購読者データが取得できること）

ステップ3：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`、メッセージ `指定された購読者が見つかりません。`）（他管理支店のレコードは存在隠蔽されること）

ステップ4：
取得対象レコードの `ja_id` が 100 かつ `kanri_shiten_id` が 10 であること

補足：
・JA_KANRI_SHITEN は自JA かつ自管理支店（`ja_id = user.ja_id AND kanri_shiten_id = user.kanri_shiten_id`）のみ参照・登録可能であること
・他管理支店（kanri_shiten_id=11）のレコードへの参照アクセスは404で存在隠蔽されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 2: 画面表示（Layout & Rendering）

## ACSMS-TC-011-007 — 新規作成モード 全体レイアウトが設計仕様に一致

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・ブラウザ: Chrome (latest), 解像度 1920×1080

### 手順

ステップ1：
ログイン後、購読者情報登録画面（新規作成モード）を開く

ステップ2：
設計仕様書（screen-design.md / index.html）と画面を並べて比較

ステップ3：
各セクション（基本情報・管理情報・購読者氏名・購読者情報・配達先情報・販売店郵送区分・支払方法・購読者層分類・購読期間・その他）の配置と各項目の表示を確認

### 期待結果

ステップ1：
購読者情報登録画面が表示されること

ステップ2：
背景色、フォント、フォントサイズ、padding/margin、ボタン色、カードスタイルがすべて設計仕様に一致すること

ステップ3：
設計仕様通りに各セクション・各項目が配置されていること

補足：
・新規作成モードでは電子版読者種別（denshi_dokusya_shubetsu）・履歴No（rireki_no）・ID（dokusya_rireki_id）が非表示であること
・design tokens（`design-tokens.ts`）が適用されていること（ハードコードされた色がないこと）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-008 — ヘッダー・パンくず表示

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
画面上部のヘッダータイトルとパンくずリストを確認

ステップ2：
パンくずの中間ノード（購読者明細検索）を押下

### 期待結果

ステップ1：
ヘッダーに「購読者情報登録画面」が表示されること、パンくずに「ホーム > 購読者明細検索 > 購読者情報登録画面」が表示されること

ステップ2：
購読者明細検索画面へ遷移すること

補足：
・パンくずの中間ノード（購読者明細検索）はリンクであり押下可能であること
・現在ページ（購読者情報登録画面）はリンクではないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-009 — 購読種別・支払方法による配達先／口座入力エリアの表示切替

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者情報登録画面（新規作成モード）を開いた状態

### 手順

ステップ1：
購読種別（dokusya_shubetsu）で「電子版」を選択

ステップ2：
購読種別で「紙版」を選択し直し、支払方法（shiharai_hoho）で「口座引落」を選択

ステップ3：
支払方法を「現金集金」に変更

### 期待結果

ステップ1：
配達先情報エリアが非活性状態で表示されること、メールアドレス（email）が必須項目として表示されること

ステップ2：
配達先情報エリアが活性状態で表示されること、引落口座支店（bank_shiten_id）・引落口座貯金種目（hikiotoshi_yokin_shubetsu）・引落口座番号（hikiotoshi_koza_no）・引落口座名義（hikiotoshi_koza_meigi）が必須項目として表示されること

ステップ3：
引落口座支店・引落口座貯金種目・引落口座番号・引落口座名義が任意項目として表示されること

補足：
・購読種別が「電子版」または「併読」の場合、配達先情報エリアを非活性化すること（機能定義 §7.5）
・支払方法=口座引落（shiharai_hoho=1）の場合のみ引落口座クラスタが必須となること（機能定義 §10.1）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-010 — 手続種類「解約」選択時の購読部数固定表示

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者情報登録画面（新規作成モード）を開いた状態

### 手順

ステップ1：
手続種類（tetsuzuki_shurui）で「解約」を選択

ステップ2：
購読部数（dokusya_busu）と購読中止日（dokusya_chushi_date）の表示・入力可否を確認

ステップ3：
手続種類で「新規」を選択し直し、購読部数の入力可否を確認

### 期待結果

ステップ1：
手続種類が「解約」として選択されること

ステップ2：
購読部数（dokusya_busu）が0かつ読み取り専用で表示されること、購読中止日（dokusya_chushi_date）が入力可能で表示されること

ステップ3：
購読部数（dokusya_busu）が入力可能で表示されること

補足：
・手続種類=解約（tetsuzuki_shurui=0）の場合、購読部数を0に固定すること（機能定義 §8.1）
・登録時に解約の場合は kaiyaku_flg=TRUE が設定されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-011 — 「購読者情報と同じ」チェックによる配達先項目の非活性化とクリア

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読種別=紙版
  - ・配達先項目（haitatsu_yubin_no 等）に値が入力済み

### 手順

ステップ1：
「購読者情報と同じ」（haitatsu_same_flg）チェックボックスをチェック

ステップ2：
配達先郵便番号（haitatsu_yubin_no）・配達先都道府県（haitatsu_todofuken_code）・配達先苗字漢字（haitatsu_shimei_sei）等の表示・値を確認

ステップ3：
チェックを解除し、各配達先項目の活性状態を確認

### 期待結果

ステップ1：
「購読者情報と同じ」がチェック状態となること

ステップ2：
配達先項目（haitatsu_yubin_no 等）が非活性状態で表示されること、入力済みの値がクリアされること

ステップ3：
配達先項目（haitatsu_yubin_no 等）が活性状態で表示されること、配達先郵便番号・都道府県・市町村郡・丁目番地・配達先氏名（漢字・かな）が必須項目となること

補足：
・チェック時は配達先項目を非活性化し、入力済みデータをクリアすること（機能定義 §9.1）
・未チェック時は配達先郵便番号〜丁目番地および配達先氏名（漢字・かな）が必須となること（機能定義 §9.2）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-012 — 購読者層分類「農業者」選択時の農業者分類表示

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者情報登録画面（新規作成モード）を開いた状態

### 手順

ステップ1：
購読者層分類（dokusyaso_bunrui）で「農業者」をチェック

ステップ2：
農業者分類（nogyosya_bunrui）の表示と選択肢を確認

ステップ3：
購読者層分類「農業者」のチェックを解除し、農業者分類の表示と値を確認

### 期待結果

ステップ1：
購読者層分類「農業者」がチェック状態となること

ステップ2：
農業者分類（nogyosya_bunrui）が表示されること（水稲／野菜／果樹／畜産／その他）

ステップ3：
農業者分類（nogyosya_bunrui）が非表示となること、選択値がクリアされること

補足：
・購読者層分類で「農業者」を選択した場合に農業者分類を表示すること（機能定義 §11.1）
・「農業者」を未チェックまたはチェック解除した場合は農業者分類を非表示とし、選択値をクリアすること（機能定義 §11.2）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-013 — 編集モード 読取専用項目の表示（電子版読者種別・履歴No・請求開始月）

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読種別=電子版 かつ denshi_shonin_status=1（承認済み）の購読者が存在
  - ・対象購読者の dokusya_id を保持

### 手順

ステップ1：
購読者明細検索画面の一覧から対象購読者の行を押下し、編集モードで購読者情報登録画面を開く

ステップ2：
電子版読者種別（denshi_dokusya_shubetsu）・履歴No（rireki_no）・ID（dokusya_rireki_id）の表示と入力可否を確認

ステップ3：
請求開始月（seikyu_kaishi_month）の表示と入力可否を確認

### 期待結果

ステップ1：
編集モードで購読者情報登録画面が表示されること、フォームに既存データが反映されること

ステップ2：
電子版読者種別（denshi_dokusya_shubetsu）・履歴No（rireki_no）・ID（dokusya_rireki_id）が読み取り専用で表示されること

ステップ3：
購読種別=電子版のため請求開始月（seikyu_kaishi_month）が読み取り専用ラベルで表示されること

補足：
・電子版読者種別・履歴No・IDは入力不可とし、編集モードのみに読取専用項目として表示されること
・請求開始月は購読種別=電子版 または 併読 の場合に読み取り専用で表示されること（電子版読者管理システムで決定）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 3: 入力チェック（Input Validation）

## ACSMS-TC-011-014 — 購読者氏名クラスタ 必須チェック（空欄 + 空白文字）

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者情報登録画面（新規作成モード）を開いた状態

### 手順

ステップ1：
購読者氏名_氏（shimei_sei）・購読者氏名_名（shimei_mei）・購読者かな_氏（shimei_kana_sei）・購読者かな_名（shimei_kana_mei）を空欄のまま「承認・登録」ボタンを押下

ステップ2：
購読者氏名_氏（shimei_sei）に空白文字のみ（`"   "`）を入力し、再度「承認・登録」ボタンを押下

ステップ3：
各氏名項目に任意の1文字を入力し、メッセージの表示を確認

### 期待結果

ステップ1：
各氏名項目の下に必須入力エラーが表示されること（メッセージ `必須項目です。`）

ステップ2：
購読者氏名_氏（shimei_sei）の下に必須入力エラーが表示されること（空白文字のみは空欄として扱われること）

ステップ3：
各氏名項目の必須入力エラーが消えること

補足：
・必須項目を入力しない場合 ACSMS-MSG-011-013 メッセージ `必須項目です。` を表示すること
・空白文字のみの入力は前後空白除去後に空欄として扱われること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-015 — 購読者かな 全角ひらがなチェック

- 観点ID: VP-B-04
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者情報登録画面（新規作成モード）を開いた状態

### 手順

ステップ1：
購読者かな_氏（shimei_kana_sei）に半角英字「yamada」を入力し「承認・登録」ボタンを押下

ステップ2：
購読者かな_氏（shimei_kana_sei）に漢字「山田」を入力し「承認・登録」ボタンを押下

ステップ3：
購読者かな_氏（shimei_kana_sei）に全角ひらがな「やまだ」を入力し「承認・登録」ボタンを押下

### 期待結果

ステップ1：
形式エラーが表示されること（メッセージ `ひらがなで入力してください。`）

ステップ2：
形式エラーが表示されること（メッセージ `ひらがなで入力してください。`）

ステップ3：
形式エラーが表示されないこと（全角ひらがなが入力可能であること）

補足：
・購読者かな項目は全角ひらがなのみ入力可能とすること（電子版仕様）
・形式不正の場合 ACSMS-MSG-011-002 メッセージ `ひらがなで入力してください。` を表示すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-015-2 — 購読者氏名 漢字・ひらがな・カタカナチェック（顧客要件 2026-07）

- 観点ID: VP-B-04
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者情報登録画面（新規作成モード）を開いた状態

### 手順

ステップ1：
購読者氏名_氏（shimei_sei）に半角英数字「Yamada12」を入力し「承認・登録」ボタンを押下

ステップ2：
購読者氏名_氏（shimei_sei）に全角ひらがな「やまだ」を入力し「承認・登録」ボタンを押下

ステップ3：
購読者氏名_名（shimei_mei）に全角カタカナ「タロウ」を入力し「承認・登録」ボタンを押下

ステップ4：
購読者氏名_氏（shimei_sei）に漢字「山田」を入力し「承認・登録」ボタンを押下

### 期待結果

ステップ1：
形式エラーが表示されること（メッセージ `漢字・ひらがな・カタカナ・アルファベットで入力してください。`）

ステップ2：
形式エラーが表示されないこと（全角ひらがなが入力可能であること）

ステップ3：
形式エラーが表示されないこと（全角カタカナが入力可能であること）

ステップ4：
形式エラーが表示されないこと（漢字が入力可能であること）

補足：
・顧客要件 2026-07：ひらがな/カタカナ表記の氏名の方に対応するため、氏名（漢字）は漢字・ひらがな・カタカナに加えアルファベットも許容
・対象は 購読者氏名_氏/名（shimei_sei/shimei_mei）および配達先氏名_姓/名（haitatsu_shimei_sei/haitatsu_shimei_mei）
・半角カナ・数字は引き続き不可（アルファベットは許容）。形式不正時は `漢字・ひらがな・カタカナ・アルファベットで入力してください。` を表示すること
・かな項目（shimei_kana_*）の全角ひらがな形式は変更なし（ACSMS-TC-011-015）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-016 — 郵便番号 7桁チェック

- 観点ID: VP-B-03
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者情報登録画面（新規作成モード）を開いた状態

### 手順

ステップ1：
郵便番号（yubin_no）に「123」（6桁未満）を入力し「承認・登録」ボタンを押下

ステップ2：
郵便番号（yubin_no）に「123-4567」（ハイフン入り）を入力し「承認・登録」ボタンを押下

ステップ3：
郵便番号（yubin_no）に「1234567」（半角数字7桁）を入力し「承認・登録」ボタンを押下

### 期待結果

ステップ1：
形式エラーが表示されること（メッセージ `郵便番号は7桁で入力してください。`）

ステップ2：
形式エラーが表示されること（ハイフン入力不可、メッセージ `郵便番号は7桁で入力してください。`）

ステップ3：
郵便番号の形式エラーが表示されないこと（半角数字7桁が入力可能であること）

補足：
・郵便番号は半角数字7桁固定、ハイフン入力不可とすること
・桁数不正の場合 ACSMS-MSG-011-004 メッセージ `郵便番号は7桁で入力してください。` を表示すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-017 — メールアドレス 形式チェック

- 観点ID: VP-B-03
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者情報登録画面（新規作成モード）を開いた状態

### 手順

ステップ1：
メールアドレス（email）に「yamada」（@なし）を入力し「承認・登録」ボタンを押下

ステップ2：
メールアドレス（email）に「yamada@」（ドメインなし）を入力し「承認・登録」ボタンを押下

ステップ3：
メールアドレス（email）に「yamada@example.com」（正しい形式）を入力し「承認・登録」ボタンを押下

### 期待結果

ステップ1：
形式エラーが表示されること（メッセージ `正しいメールアドレスを入力してください。`）

ステップ2：
形式エラーが表示されること（メッセージ `正しいメールアドレスを入力してください。`）

ステップ3：
メールアドレスの形式エラーが表示されないこと

補足：
・メールアドレスは RFC 5322 形式チェックを行うこと
・形式不正の場合 ACSMS-MSG-011-005 メッセージ `正しいメールアドレスを入力してください。` を表示すること
・メールアドレス項目は `type="text"` を使用し、ブラウザ標準の英語ポップアップを使用しないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-018 — メールアドレス 条件付き必須（電子版・併読）

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者情報登録画面（新規作成モード）を開いた状態

### 手順

ステップ1：
購読種別（dokusya_shubetsu）で「電子版」を選択し、メールアドレス（email）を空欄のまま「承認・登録」ボタンを押下

ステップ2：
購読種別で「併読」を選択し、メールアドレスを空欄のまま「承認・登録」ボタンを押下

ステップ3：
購読種別で「紙版」を選択し、メールアドレスを空欄のまま他必須項目を入力して「承認・登録」ボタンを押下

### 期待結果

ステップ1：
メールアドレス（email）の下に必須入力エラーが表示されること（メッセージ `必須項目です。`）

ステップ2：
メールアドレス（email）の下に必須入力エラーが表示されること（メッセージ `必須項目です。`）

ステップ3：
メールアドレス（email）の必須入力エラーが表示されないこと（紙版の場合は任意であること）

補足：
・購読種別=電子版/併読時はメールアドレスを必須とすること（機能定義 §7.1）
・紙版の場合はメールアドレスは任意であること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-019 — 口座引落時の銀行クラスタ 条件付き必須

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読種別=紙版
  - ・支払方法（shiharai_hoho）で「口座引落」を選択した状態

### 手順

ステップ1：
引落口座支店（bank_shiten_id）・引落口座貯金種目（hikiotoshi_yokin_shubetsu）・引落口座番号（hikiotoshi_koza_no）・引落口座名義（hikiotoshi_koza_meigi）を空欄のまま「承認・登録」ボタンを押下

ステップ2：
支払方法を「現金集金」に変更し、上記4項目を空欄のまま「承認・登録」ボタンを押下

ステップ3：
引落口座支店プルダウンの選択肢を確認

### 期待結果

ステップ1：
引落口座支店・引落口座貯金種目・引落口座番号・引落口座名義の下にエラーが表示されること（メッセージ `口座引落の場合、〇〇は必須です。`）

ステップ2：
上記4項目のエラーが表示されないこと（口座引落以外の支払方法では任意であること）

ステップ3：
金融機関支店フラグ（kinyu_shiten_flg=TRUE）の支店のみが選択肢に表示されること

補足：
・支払方法=口座引落（shiharai_hoho=1）の場合、引落口座クラスタ4項目を必須とすること（機能定義 §10.1）
・口座引落必須エラーは ACSMS-MSG-011-006 メッセージ `口座引落の場合、〇〇は必須です。` を表示すること
・引落口座支店プルダウンは `m_shiten.kinyu_shiten_flg=TRUE` のもののみ表示すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-020 — 配達先 条件付き必須（購読者情報と同じが未チェック）

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読種別=紙版
  - ・「購読者情報と同じ」（haitatsu_same_flg）が未チェックの状態

### 手順

ステップ1：
配達先郵便番号（haitatsu_yubin_no）・配達先都道府県（haitatsu_todofuken_code）・配達先市町村郡（haitatsu_shikuchoson）・配達先丁目番地（haitatsu_chome_banchi）を空欄のまま「承認・登録」ボタンを押下

ステップ2：
配達先苗字漢字（haitatsu_shimei_sei）・配達先名前漢字（haitatsu_shimei_mei）・配達先苗字かな（haitatsu_shimei_kana_sei）・配達先名前かな（haitatsu_shimei_kana_mei）を空欄のまま「承認・登録」ボタンを押下

ステップ3：
「購読者情報と同じ」をチェックし、配達先項目を空欄のまま「承認・登録」ボタンを押下

### 期待結果

ステップ1：
配達先郵便番号・配達先都道府県・配達先市町村郡・配達先丁目番地の下に必須入力エラーが表示されること（メッセージ `必須項目です。`）

ステップ2：
配達先苗字漢字・配達先名前漢字・配達先苗字かな・配達先名前かなの下に必須入力エラーが表示されること（メッセージ `必須項目です。`）

ステップ3：
配達先項目の必須入力エラーが表示されないこと（チェック時は購読者情報をコピーするため必須スキップとなること）

補足：
・「購読者情報と同じ」未チェック時は配達先郵便番号〜丁目番地および配達先氏名（漢字・かな）を必須とすること（機能定義 §9.2）
・チェック時はDB保存時に購読者情報からコピーすること（機能定義 §9.1）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-021 — 備考 最大文字数チェック・読者情報変更適用日 未来日チェック

- 観点ID: VP-B-02
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者情報登録画面（新規作成モード）を開いた状態
  - ・当日: 2026-06-03

### 手順

ステップ1：
備考（biko）に501文字を入力し「承認・登録」ボタンを押下

ステップ2：
備考（biko）に500文字を入力し「承認・登録」ボタンを押下

ステップ3：
読者情報変更適用日（joho_henko_tekiyo_date）に過去日（2026-06-02）を入力し「承認・登録」ボタンを押下

### 期待結果

ステップ1：
桁数超過エラーが表示されること（メッセージ `備考は500文字以内で入力してください。`）

ステップ2：
備考の桁数超過エラーが表示されないこと（500文字以内で登録できること）

ステップ3：
読者情報変更適用日の下にエラーが表示されること（未来日のみ入力可能であること）

補足：
・備考は500文字以内とすること（ACSMS-MSG-011-010 メッセージ `備考は500文字以内で入力してください。`）
・読者情報変更適用日（joho_henko_tekiyo_date）は入力時は未来日であることをチェックすること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-022 — 農業者分類 条件付き必須（購読者層=農業者）

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者情報登録画面（新規作成モード）を開いた状態

### 手順

ステップ1：
購読者層分類（dokusyaso_bunrui）で「農業者」をチェックし、農業者分類（nogyosya_bunrui）を空欄のまま「承認・登録」ボタンを押下

ステップ2：
農業者分類（nogyosya_bunrui）で「水稲」を選択し「承認・登録」ボタンを押下

ステップ3：
購読者層分類「農業者」のチェックを解除し、農業者分類を空欄のまま「承認・登録」ボタンを押下

### 期待結果

ステップ1：
農業者分類（nogyosya_bunrui）の下に必須入力エラーが表示されること（メッセージ `必須項目です。`）

ステップ2：
農業者分類の必須入力エラーが表示されないこと

ステップ3：
農業者分類の必須入力エラーが表示されないこと（農業者未選択時は任意であること）

補足：
・購読者層分類で「農業者」を選択した場合、農業者分類（nogyosya_bunrui）を必須とすること（API リクエストパラメータ仕様）
・農業者を未選択の場合は農業者分類を任意とすること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 4: 業務ロジック登録（Function — Create）

## ACSMS-TC-011-023 — 新規登録 正常系（DB 永続化 + 履歴作成 + 監査ログ）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・全必須項目に有効な値を入力済み（購読種別=紙版、手続種類=新規、支払方法=口座引落）

### 手順

ステップ1：
全必須項目を入力し「承認・登録」ボタンを押下、確認ダイアログで「はい」を押下

ステップ2：
画面遷移とメッセージを確認

ステップ3：
DBで以下クエリを実行し、本体・履歴レコードを確認
```sql
SELECT dokusya_id, rireki_no, tetsuzuki_shurui, dokusya_busu
FROM t_dokusya
WHERE dokusya_id = :new_dokusya_id;

SELECT rireki_no, saishin_data_flg, shinki_flg, kaiyaku_flg, zougen_hokoku_flg
FROM t_dokusya_rireki
WHERE dokusya_id = :new_dokusya_id;
```

ステップ4：
DBで以下クエリを実行し、監査ログを確認
```sql
SELECT operation, result_status, target_table, ja_id
FROM t_log
WHERE target_table = 't_dokusya' AND target_id = :new_dokusya_id
ORDER BY log_datetime DESC
LIMIT 1;
```

### 期待結果

ステップ1：
確認ダイアログが表示され「はい」押下で登録処理が実行されること

ステップ2：
HTTPステータスコード201が返却されること、登録完了メッセージ `登録しました。`（ACSMS-MSG-011-011）が表示されること、購読者明細検索画面へ遷移すること

ステップ3：
t_dokusya に新規レコードが登録されること、t_dokusya_rireki に履歴レコード（rireki_no=1, saishin_data_flg=true, shinki_flg=true, kaiyaku_flg=false, zougen_hokoku_flg=true）が登録されること

ステップ4：
t_log に operation=`CREATE`, result_status=1, target_table=`t_dokusya`, ja_id=100 のレコードが記録されること

補足：
・t_dokusya + t_dokusya_rireki を1トランザクションで作成すること（機能定義 §2.6）
・bank_shiten_id が指定された場合、支払方法を問わず（口座引落以外も含む） `m_shiten.jastem_toriatsukai_tenpo_code` / `jastem_tenpo_name` を逆引きし、`t_dokusya.bank_branch_code` / `bank_branch_name` へ非正規化保存すること（顧客要件 2026-06）
・登録（業務書き込み）と監査ログは単一トランザクションで実行されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-024 — 新規登録 — 手続種類「解約」時の購読部数0強制

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・手続種類（tetsuzuki_shurui）=解約 を選択

### 手順

ステップ1：
手続種類で「解約」を選択し、購読部数（dokusya_busu）に「5」を入力した状態で「承認・登録」ボタンを押下

ステップ2：
DBで以下クエリを実行し、購読部数と解約フラグを確認
```sql
SELECT dokusya_busu FROM t_dokusya WHERE dokusya_id = :new_dokusya_id;

SELECT kaiyaku_flg, shinki_flg
FROM t_dokusya_rireki
WHERE dokusya_id = :new_dokusya_id AND saishin_data_flg = TRUE;
```

### 期待結果

ステップ1：
登録処理が実行されること、登録完了メッセージ `登録しました。` が表示されること

ステップ2：
t_dokusya の dokusya_busu が0で保存されること、t_dokusya_rireki の kaiyaku_flg が TRUE、shinki_flg が FALSE で保存されること

補足：
・解約を選んで登録する場合は購読部数を0にし、kaiyaku_flg=TRUE に設定すること（機能定義 §8.1）
・二重請求防止のため購読部数は解約時に強制的に0となること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-025 — 新規登録 — 配達先「購読者情報と同じ」時のコピー保存

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読種別=紙版
  - ・「購読者情報と同じ」（haitatsu_same_flg）をチェック

### 手順

ステップ1：
購読者氏名・住所等を入力し、「購読者情報と同じ」をチェックして「承認・登録」ボタンを押下

ステップ2：
DBで以下クエリを実行し、配達先情報を確認
```sql
SELECT haitatsu_same_flg, haitatsu_yubin_no, haitatsu_shimei_sei
FROM t_dokusya WHERE dokusya_id = :new_dokusya_id;
```

### 期待結果

ステップ1：
登録処理が実行されること、登録完了メッセージ `登録しました。` が表示されること

ステップ2：
haitatsu_same_flg が true で保存されること、配達先項目（haitatsu_yubin_no・haitatsu_shimei_sei 等）に購読者情報からコピーされた値が保存されること

補足：
・「購読者情報と同じ」チェック時はDB保存時に購読者情報からコピーすること（機能定義 §9.1）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-026 — 新規登録 — 全項目未入力時の統合バリデーション

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者情報登録画面（新規作成モード）を開いた状態

### 手順

ステップ1：
全項目を空欄のまま「承認・登録」ボタンを押下

ステップ2：
DevTools の Network タブで POST `/api/v1/dokusya` のレスポンスを確認

### 期待結果

ステップ1：
各必須項目の下に必須入力エラー `必須項目です。` が表示されること、登録処理が実行されないこと

ステップ2：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、メッセージ `入力値が不正です。詳細はerrorsフィールドを確認してください。`、`errors` 配列に shimei_sei・yubin_no 等の項目別メッセージが含まれること）

補足：
・クライアント側のデータチェック（必須・フォーマット・相関）後、サーバー側でも再度バリデーションを行うこと（機能定義 §2.1, §2.5）
・バリデーションエラーは `errors` 配列で項目別に返却されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-027 — 新規登録 — 戻るボタンによるキャンセル

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者情報登録画面（新規作成モード）で一部項目を入力済み

### 手順

ステップ1：
「前の画面に戻る」ボタンを押下

ステップ2：
遷移先画面と、入力したデータの保存有無を確認

### 期待結果

ステップ1：
購読者明細検索画面へ遷移すること

ステップ2：
入力したデータが保存されないこと（t_dokusya に新規データが登録されないこと）

補足：
・「前の画面に戻る」ボタンを押下すると購読者明細検索画面へ遷移すること（機能定義 §16）
・フォームに入力されたデータは保存されないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 5: 業務ロジック更新（編集・承認・否認）

## ACSMS-TC-011-028 — 編集モード フォームロード（既存データ反映）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読種別=紙版 の購読者（dokusya_id=100）が存在

### 手順

ステップ1：
購読者明細検索画面の一覧から対象購読者の行を押下し、編集モードで購読者情報登録画面を開く

ステップ2：
DevTools の Network タブで GET `/api/v1/dokusya/100` のレスポンスを確認

ステップ3：
フォーム各項目の値と、購読者ID（dokusya_id）・履歴No（rireki_no）の表示を確認

### 期待結果

ステップ1：
編集モードで購読者情報登録画面が表示されること

ステップ2：
HTTPステータスコード200が返却されること（対象購読者の全項目が `data` に含まれること、bank_shiten_id がプルダウン再ハイドレーション用に返却されること）

ステップ3：
フォーム各項目に既存データが反映されること、購読者ID（dokusya_id）が変更不可で表示されること

補足：
・編集モードでは IDで購読者のデータを取得し、フォームに反映すること（機能定義 §15.1）
・購読者IDは変更不可とすること
・データ取得失敗の場合は ACSMS-MSG-011-016 `購読者ID #{id} が見つかりません。` を表示すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-029 — 更新 正常系（履歴追記方式 + 監査ログ）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読種別=紙版 の購読者（dokusya_id=100, 現 rireki_no=1）が存在

### 手順

ステップ1：
編集モードで購読者情報登録画面を開き、購読部数（dokusya_busu）を1から2へ変更して「承認・登録」ボタンを押下

ステップ2：
画面遷移とメッセージを確認

ステップ3：
DBで以下クエリを実行し、本体・履歴レコードを確認
```sql
SELECT rireki_no, dokusya_busu FROM t_dokusya WHERE dokusya_id = 100;

SELECT rireki_no, saishin_data_flg, zougen_hokoku_flg
FROM t_dokusya_rireki
WHERE dokusya_id = 100
ORDER BY rireki_no DESC;
```

ステップ4：
DBで以下クエリを実行し、監査ログを確認
```sql
SELECT operation, result_status, before_value, after_value
FROM t_log
WHERE target_table = 't_dokusya' AND target_id = 100
ORDER BY log_datetime DESC
LIMIT 1;
```

### 期待結果

ステップ1：
更新処理が実行されること

ステップ2：
HTTPステータスコード200が返却されること、更新完了メッセージ `更新しました。`（ACSMS-MSG-011-015）が表示されること、購読者明細検索画面へ戻ること

ステップ3：
t_dokusya の rireki_no が2に更新され dokusya_busu が2となること、t_dokusya_rireki に新履歴（rireki_no=2, saishin_data_flg=true, zougen_hokoku_flg=true）が追記され、旧履歴（rireki_no=1）の saishin_data_flg が false に更新されること

ステップ4：
t_log に operation=`UPDATE`, result_status=1 のレコードが記録され、before_value に更新前データ、after_value に更新後データが格納されること

補足：
・更新は既存レコードの物理更新ではなく、新しい履歴番号でのレコード追記を行うこと（機能定義 §15.3）
・旧履歴の最新データフラグを false に更新し、新履歴の最新データフラグを true とすること
・更新（業務書き込み）と監査ログは単一トランザクションで実行されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-030 — 純電子版（クレカ決済）・併読読者の読み取り専用制御

- 観点ID: VP-C-05
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読種別=電子版（クレカ決済）の購読者（dokusya_id=300）が存在

### 手順

ステップ1：
購読者明細検索画面の一覧から電子版クレカ決済者の行を押下し、編集モードで画面を開く

ステップ2：
フォーム各項目の入力可否を確認

ステップ3：
DevTools の Network タブで PUT `/api/v1/dokusya/300` に編集内容を含めて直接送信

### 期待結果

ステップ1：
編集モードで購読者情報登録画面が参照モードで表示されること

ステップ2：
購読開始日（dokusya_kaishi_date）・購読中止日（dokusya_chushi_date）等が読み取り専用で表示されること（電子版読者管理システムからの連携データ）

ステップ3：
バックエンド側で電子版クレカ決済者の編集が拒否されること（業務ルールにより編集不可）

補足：
・純粋な電子版（クレカ決済）読者は読者管理が外部Webシステム側で主導されるため、本画面では参照のみ（読み取り専用）とすること（機能定義 §15.1）
・電子版クレカ決済者・併読者は編集・削除不可（業務ルール、機能定義 §1.2）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-031 — 同時編集競合（楽観ロック）

- 観点ID: VP-C-02
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者（dokusya_id=100）を2つのブラウザタブで編集モードで開いた状態

### 手順

ステップ1：
タブAで購読部数（dokusya_busu）を変更し「承認・登録」ボタンを押下（更新成功）

ステップ2：
タブBで（タブA更新前のデータのまま）市町村郡（shikuchoson）を変更し「承認・登録」ボタンを押下

ステップ3：
DBで以下クエリを実行し、最新履歴の内容を確認
```sql
SELECT rireki_no, dokusya_busu, shikuchoson
FROM t_dokusya_rireki
WHERE dokusya_id = 100 AND saishin_data_flg = TRUE;
```

### 期待結果

ステップ1：
タブAの更新処理が実行されること、更新完了メッセージ `更新しました。` が表示されること

ステップ2：
タブBの更新時に競合が検知されること（後勝ち更新の場合はタブAの変更が失われないこと）

ステップ3：
データ整合性が保たれること、saishin_data_flg=TRUE の履歴が1件のみであること

補足：
・履歴テーブルは追記専用であり、最新データフラグ（saishin_data_flg）が TRUE のレコードは購読者IDごとに1件のみとすること（機能定義 §14.2）
・楽観ロックの実装有無は設計仕様に従い、未実装の場合は別途バグチケットを起票すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

楽観ロック競合時の専用メッセージは設計仕様に未定義のため、本TCではHTTPコードとデータ整合性のみ検証する（メッセージは仕様確認待ち）。

## ACSMS-TC-011-032 — 電子版承認 正常系（denshi_shonin_status 0→1）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属、電子版取扱フラグあり）
  - ・購読種別=電子版 かつ denshi_shonin_status=0（承認待ち）の購読者（dokusya_id=200, 現 rireki_no=1）が存在

### 手順

ステップ1：
承認待ちの購読者を編集モードで開き、「承認・登録」ボタンを押下

ステップ2：
画面遷移とメッセージを確認

ステップ3：
DBで以下クエリを実行し、ステータスと履歴を確認
```sql
SELECT denshi_shonin_status, rireki_no FROM t_dokusya WHERE dokusya_id = 200;

SELECT rireki_no, denshi_shonin_status, saishin_data_flg
FROM t_dokusya_rireki
WHERE dokusya_id = 200
ORDER BY rireki_no DESC;
```

### 期待結果

ステップ1：
承認処理が実行されること

ステップ2：
HTTPステータスコード200が返却されること（`data.denshi_shonin_status = 1`）、メッセージ `承認しました。` が表示されること

ステップ3：
t_dokusya の denshi_shonin_status が1に更新されること、t_dokusya_rireki に新履歴（denshi_shonin_status=1, saishin_data_flg=true）が追記されること

補足：
・「承認」ボタン押下で APIが denshi_shonin_status=1（承認済み）に設定し、新規履歴レコードを作成すること（機能定義 §3.3）
・PUT `/api/v1/dokusya/{dokusya_id}/approve` を呼び出すこと
・承認（業務書き込み）と監査ログは単一トランザクションで実行されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-033 — 電子版否認 正常系（否認確認 + denshi_shonin_status 0→2）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属、電子版取扱フラグあり）
  - ・購読種別=電子版 かつ denshi_shonin_status=0（承認待ち）の購読者（dokusya_id=201）が存在

### 手順

ステップ1：
承認待ちの購読者を編集モードで開き、「承認しない」ボタンの表示を確認

ステップ2：
「承認しない」ボタンを押下し、否認確認ダイアログで「はい」を押下

ステップ3：
画面遷移とメッセージを確認

ステップ4：
DBで以下クエリを実行し、ステータスと履歴を確認
```sql
SELECT denshi_shonin_status FROM t_dokusya WHERE dokusya_id = 201;

SELECT denshi_shonin_status
FROM t_dokusya_rireki
WHERE dokusya_id = 201 AND saishin_data_flg = TRUE;
```

### 期待結果

ステップ1：
denshi_shonin_status=0（承認待ち）のため「承認しない」ボタンが表示されること

ステップ2：
否認確認ダイアログが表示されること（メッセージ `電子版読者の登録を否認します。よろしいですか？`）、「はい」押下で否認処理が実行されること

ステップ3：
HTTPステータスコード200が返却されること（`data.denshi_shonin_status = 2`）、メッセージ `否認しました。` が表示されること、購読者明細検索画面へ遷移すること

ステップ4：
t_dokusya の denshi_shonin_status が2に更新されること、t_dokusya_rireki の最新履歴に denshi_shonin_status=2 が記録されること

補足：
・denshi_shonin_status=0 の場合のみ「承認しない」ボタンを表示すること（機能定義 §4.1）
・「承認しない」押下で否認確認ダイアログ（ACSMS-MSG-011-014 `電子版読者の登録を否認します。よろしいですか？`）を表示すること（機能定義 §4.2）
・「はい」押下で PUT `/api/v1/dokusya/{dokusya_id}/reject` を呼び出し denshi_shonin_status=2 に設定すること（機能定義 §4.3）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-034 — 否認確認ダイアログ「いいえ」押下時の処理中断

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属、電子版取扱フラグあり）
  - ・denshi_shonin_status=0（承認待ち）の購読者（dokusya_id=202）が存在

### 手順

ステップ1：
承認待ちの購読者を編集モードで開き、「承認しない」ボタンを押下

ステップ2：
否認確認ダイアログで「いいえ」を押下

ステップ3：
DBで以下クエリを実行し、ステータスを確認
```sql
SELECT denshi_shonin_status FROM t_dokusya WHERE dokusya_id = 202;
```

### 期待結果

ステップ1：
否認確認ダイアログが表示されること

ステップ2：
否認処理が実行されないこと、現在の画面が維持されること

ステップ3：
t_dokusya の denshi_shonin_status が0（承認待ち）のまま変更されないこと

補足：
・否認確認ダイアログで「いいえ」を押下する場合、何も行わず現在の画面を維持すること（機能定義 §4.4）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-035 — 変更履歴一覧の取得・表示

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者（dokusya_id=100）に2件以上の履歴（rireki_no=1, 2）が存在

### 手順

ステップ1：
編集モードで購読者情報登録画面を開き、「履歴表示」ボタンを押下

ステップ2：
DevTools の Network タブで GET `/api/v1/dokusya/100/history` のレスポンスを確認

ステップ3：
履歴一覧の並び順と各行の項目（rireki_no・tetsuzuki_shurui・saishin_data_flg）を確認

### 期待結果

ステップ1：
購読者履歴情報画面（変更履歴一覧）が表示されること

ステップ2：
HTTPステータスコード200が返却されること（`data` 配列に履歴レコードが含まれること）

ステップ3：
履歴一覧が rireki_no の降順で表示されること、各行に手続種類（tetsuzuki_shurui の m_code ラベル）・最新データフラグ（saishin_data_flg）が表示されること

補足：
・「履歴表示」ボタン押下で購読者履歴情報画面（ACSMS-SCR-013）へ遷移すること（機能定義 §5.1）
・履歴一覧は rireki_no 降順で取得すること
・tetsuzuki_shurui 値はラベル（m_code.code_category='TETSUZUKI_SHURUI'）にマッピングすること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 6: 共通エラーハンドリング（Common Error Handling）

## ACSMS-TC-011-036 — 共通エラー — UNAUTHORIZED — セッション切れ時の処理

- 観点ID: VP-A-05
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・購読者情報登録画面を表示中
  - ・最終操作から24時間以上経過し、Redis セッション TTL 失効済

### 手順

ステップ1：
購読者情報登録画面で「承認・登録」ボタンを押下し、POST `/api/v1/dokusya` リクエストを送信

ステップ2：
レスポンス内容と画面遷移を確認

### 期待結果

ステップ1：
HTTPステータスコード401が返却されること（`error_code: UNAUTHORIZED`、メッセージ `セッションが切れました。再度ログインしてください。`）

ステップ2：
フロントエンド側で Pinia auth store の user 状態がクリアされ、`/login?redirect=...` へ自動遷移すること

補足：
・セッション失効時はバックエンド側の SessionAuthGuard が401を返却し、フロントエンド側 axios インターセプタで一律ログイン画面へ誘導すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-037 — 共通エラー — FORBIDDEN — 権限なしでの登録API直接呼び出し

- 観点ID: VP-A-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・権限「dokusya.create」を持たない

### 手順

ステップ1：
DevTools の Console から `fetch('/api/v1/dokusya', { method: 'POST', credentials: 'include', headers: { 'Content-Type': 'application/json' }, body: '{}' })` を実行

ステップ2：
レスポンスの HTTP ステータスと error_code を確認

### 期待結果

ステップ1：
リクエストがバックエンド側の権限チェックで拒否されること

ステップ2：
HTTPステータスコード403が返却されること（`error_code: FORBIDDEN`、メッセージ `この画面へのアクセス権限がありません。`）

補足：
・`dokusya.create` 権限を持たないロールが登録APIを直接呼び出してもバックエンド側で403を返却すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-038 — 共通エラー — DATA_SCOPE_VIOLATION — 他JAスコープでの登録試行

- 観点ID: VP-A-02
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・他JA（ja_id=200）の管理支店ID・販売店IDを保持

### 手順

ステップ1：
DevTools の Console から POST `/api/v1/dokusya` を実行し、リクエストボディに他JA（ja_id=200）配下の kanri_shiten_id・hanbaiten_id を指定して送信

ステップ2：
レスポンスの HTTP ステータスと error_code を確認

### 期待結果

ステップ1：
リクエストがバックエンド側の DataScope チェックで拒否されること

ステップ2：
HTTPステータスコード403が返却されること（`error_code: DATA_SCOPE_VIOLATION`、メッセージ `このデータへのアクセス権限がありません。`）

補足：
・登録時の ja_id はサーバ側で自動設定し、リクエストボディの ja_id は信頼しないこと
・他JA配下のFK参照（管理支店・販売店）を指定した登録は DATA_SCOPE_VIOLATION（403）で拒否されること
・参照系（詳細・履歴取得）の DataScope 違反は404で存在隠蔽されるが、登録・更新系は403でアクセス拒否を明示すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-039 — 共通エラー — VALIDATION_ERROR — 不正値統合送信

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・ブラウザ DevTools の Network タブを開いている

### 手順

ステップ1：
DevTools の Console から POST `/api/v1/dokusya` を実行し、shimei_sei を空・yubin_no を「123」（不正桁数）として送信

ステップ2：
レスポンスの HTTP ステータス・error_code・errors 配列を確認

### 期待結果

ステップ1：
リクエストがバックエンド側のバリデーションで拒否されること

ステップ2：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、メッセージ `入力値が不正です。詳細はerrorsフィールドを確認してください。`、`errors` 配列に shimei_sei・yubin_no の項目別メッセージが含まれること）

補足：
・バリデーションエラーは `errors` 配列（field + message）形式で返却されること
・項目別メッセージはフロントエンド側で各項目下に表示されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-040 — 共通エラー — NOT_FOUND — 削除済／存在しない購読者の取得

- 観点ID: VP-D-02
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・存在しない購読者ID（dokusya_id=999999）

### 手順

ステップ1：
ブラウザのアドレスバーに `/dokusya/999999` を入力し、編集モードで直接アクセス

ステップ2：
DevTools の Network タブで GET `/api/v1/dokusya/999999` のレスポンスを確認

ステップ3：
画面側のメッセージ表示を確認

### 期待結果

ステップ1：
編集モードの購読者情報登録画面が開くこと

ステップ2：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`、メッセージ `指定された購読者が見つかりません。`）

ステップ3：
データ取得失敗のメッセージ ACSMS-MSG-011-016 `購読者ID #{id} が見つかりません。` が表示されること

補足：
・存在しない購読者IDの取得は404（NOT_FOUND）を返却すること
・データ取得失敗時はフロントエンド側で ACSMS-MSG-011-016 を表示すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-041 — 共通エラー — DUPLICATE_EMAIL — メールアドレス重複

- 観点ID: VP-B-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・自JA内に email=`existing@example.com` の購読者が既に存在

### 手順

ステップ1：
新規作成モードで購読種別=電子版を選択し、メールアドレス（email）に `existing@example.com` を入力して全必須項目を入力後「承認・登録」ボタンを押下

ステップ2：
レスポンスの HTTP ステータスと error_code を確認

ステップ3：
画面側のメッセージ表示を確認

### 期待結果

ステップ1：
登録処理が拒否されること

ステップ2：
HTTPステータスコード400が返却されること（`error_code: DUPLICATE_EMAIL`、メッセージ `このメールアドレスは既に登録されています。`）

ステップ3：
メッセージ `このメールアドレスは既に登録されています。`（ACSMS-MSG-011-009）が表示されること、重複データが登録されないこと

補足：
・メールアドレスが入力されている場合、自JA内（`ja_id = :ja_id`、空文字除く）で重複チェックを行うこと（機能定義 §2.5）
・重複の場合は DUPLICATE_EMAIL（400）を返却すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-042 — 共通エラー — INVALID_STATUS — 承認待ち以外の読者への承認試行

- 観点ID: VP-C-05
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属、電子版取扱フラグあり）
  - ・denshi_shonin_status=1（承認済み）の購読者（dokusya_id=210）が存在

### 手順

ステップ1：
DevTools の Console から PUT `/api/v1/dokusya/210/approve` を実行

ステップ2：
レスポンスの HTTP ステータスと error_code を確認

ステップ3：
DevTools の Console から PUT `/api/v1/dokusya/210/reject` を実行し、レスポンスを確認

### 期待結果

ステップ1：
承認処理が拒否されること

ステップ2：
HTTPステータスコード400が返却されること（`error_code: INVALID_STATUS`、メッセージ `承認待ちの読者ではありません。`）

ステップ3：
HTTPステータスコード400が返却されること（`error_code: INVALID_STATUS`、メッセージ `承認待ちの読者ではありません。`）

補足：
・denshi_shonin_status が0でない場合、承認・否認API は INVALID_STATUS（400）を返却すること（機能定義 §3.1, §4.1）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-043 — 共通エラー — BAD_REQUEST — 不正リクエストパラメータ

- 観点ID: VP-D-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・ブラウザ DevTools の Network タブを開いている

### 手順

ステップ1：
DevTools の Console から `fetch('/api/v1/dokusya/abc', { credentials: 'include' })` を実行（dokusya_id に数値以外を指定）

ステップ2：
レスポンスの HTTP ステータスと error_code を確認

### 期待結果

ステップ1：
リクエストがバックエンド側のバリデーションで拒否されること

ステップ2：
HTTPステータスコード400が返却されること（`error_code: BAD_REQUEST`、メッセージ `リクエストパラメータが不正です。`）

補足：
・dokusya_id パスパラメータは数値型の検証が行われ、不正値の場合 BAD_REQUEST にマップされること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-044 — 共通エラー — TOO_MANY_REQUESTS — レート制限超過

- 観点ID: VP-A-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・テスト用スクリプトで `/api/v1/dokusya/1` GET を1分間に上限超で送信

### 手順

ステップ1：
スクリプトを起動、購読者取得APIへ高頻度リクエスト送信

ステップ2：
レスポンスの HTTP ステータスと error_code を確認

ステップ3：
画面側のトースト表示を確認

### 期待結果

ステップ1：
上限件数までは正常応答、それ以降は429を返却すること

ステップ2：
HTTPステータスコード429が返却されること（`error_code: TOO_MANY_REQUESTS`、メッセージ `リクエスト回数が上限を超えました。しばらくしてから再度お試しください。`）

ステップ3：
トースト `リクエスト回数が上限を超えました。しばらくしてから再度お試しください。` が表示されること

補足：
・NestJS @Throttle デコレータ + AWS WAF レート制限がインフラ／アプリ両層でブロックすること
・観点 VP-A-08「レート制限・Throttling」準拠（testcase-viewpoints.md v1.1）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-045 — 共通エラー — INTERNAL_SERVER_ERROR — サーバーエラー

- 観点ID: VP-D-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・DB接続エラー等のサーバー障害を模擬できる状態

### 手順

ステップ1：
サーバー障害（DB接続エラー等）を模擬した状態で「承認・登録」ボタンを押下

ステップ2：
POST `/api/v1/dokusya` のレスポンスの HTTP ステータスと error_code を確認

ステップ3：
画面側のメッセージ表示と、エラーログの記録を確認

### 期待結果

ステップ1：
登録処理が失敗すること

ステップ2：
HTTPステータスコード500が返却されること（`error_code: INTERNAL_SERVER_ERROR`、メッセージ `システムエラーが発生しました。しばらくしてから再度お試しください。`）

ステップ3：
メッセージ `システムエラーが発生しました。しばらくしてから再度お試しください。`（ACSMS-MSG-011-012）が表示されること、t_log にエラーログ（log_type=3, result_status=2）が記録されること、スタックトレースが利用者へ表示されないこと

補足：
・DB接続エラー等のサーバー障害は INTERNAL_SERVER_ERROR にマップされること
・エラー発生時はエラーログ（log_type=3）をトランザクション外で記録すること（業務書き込みはロールバックされること）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-011-046 — 共通エラー — ネットワーク切断時の処理

- 観点ID: VP-D-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・ブラウザ DevTools の Network タブを開いている

### 手順

ステップ1：
DevTools の Network タブでネットワーク切断（offline）を設定

ステップ2：
「承認・登録」ボタンを押下し、POST `/api/v1/dokusya` を送信

ステップ3：
画面側のトースト表示を確認

### 期待結果

ステップ1：
ネットワーク切断状態となること

ステップ2：
APIリクエストがネットワークエラーとなること

ステップ3：
トースト `ネットワークエラーが発生しました。しばらくしてから再度お試しください。` が表示されること、入力内容が保存されること

補足：
・ネットワーク切断時はフロントエンド側 axios インターセプタでネットワークエラーのトーストを表示すること
・想定外エラーが発生しないこと、入力内容が消えないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

ネットワークエラーのトースト文言はフロントエンド側 axios インターセプタの実装に準拠。

---

## ACSMS-TC-011-047 — 純電子版の編集導線（参照モードで開く・予約変更は非活性）

- 観点ID: VP-C-05
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属・`dokusya.update` 権限あり）
  - ・購読種別=電子版（クレカ決済**以外**・承認ステータス=1 承認済）の購読者（dokusya_id=301）が存在
  - ・購読種別=紙版の購読者（dokusya_id=100）が存在（比較用）

### 手順

ステップ1：
購読者明細検索画面の一覧から電子版（dokusya_id=301）の行を押下し、編集モードで画面を開く

ステップ2：
モードバーの「当日変更」「予約変更」ボタンの状態を確認

ステップ3：
「当日変更」を押下し、何も入力を変更せずに「更新」を押下

ステップ4：
比較のため紙版（dokusya_id=100）を編集モードで開き、「予約変更」ボタンの状態を確認

### 期待結果

ステップ1：
紙版と同じく**参照モード**（全項目 read-only）で表示され、モードバーが表示されること。更新ボタンは表示されないこと

ステップ2：
「当日変更」は押下可能、「予約変更」は**非活性（グレーアウト）**で表示されること。ボタン自体は**非表示にならない**こと。あわせて「電子版は当日変更のみです（予約変更は使用できません）。」の補足が表示されること

ステップ3：
当日変更モードに切り替わり、情報変更適用日が本日になること。業務項目を変更していないため ACSMS-MSG-011-018（変更がありません。）が表示され、更新（PUT）・履歴（t_dokusya_rireki）が発生しないこと

ステップ4：
紙版では「予約変更」が押下可能であること（制限は電子版のみ）

補足：
・従来は電子版のみモードバーを表示せず、開いた瞬間から編集可能（当日変更固定）だった。同じ編集画面が購読種別によって参照/編集で開き分かれ、誤操作で保存しやすかったため、導線を紙版と統一した（機能定義 §15.0）
・「予約変更」を非表示ではなく非活性にするのは、隠すと「この画面に予約変更機能が無い」と誤読され、紙版との違いが画面から伝わらないため
・非活性はUIの制御に過ぎないため、FEは selectMode 側でも予約変更への遷移を弾き、BEも `change_mode='reserved'` を VALIDATION_ERROR で拒否する（三重の防御）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

## ACSMS-TC-011-048 — 販売店ドロップダウンの購読種別連動（ダミー販売店の絞込）

- 観点ID: VP-C-05
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属・`dokusya.create` / `dokusya.update` 権限あり）
  - ・ja_id=100 に販売店が3件存在：`1000000001`（営業中）、`1000000002`（営業中）、`9999999999`（電子版ダミー・営業中）
  - ・購読種別=電子版の購読者（dokusya_id=301・販売店=`9999999999`）が存在

### 手順

ステップ1：
購読者情報登録画面（新規）を開き、購読種別=1:紙版（既定）の状態で販売店コードのドロップダウンを開く

ステップ2：
販売店コード=`1000000001` を選択したうえで、購読種別を 2:電子版 へ切り替え、販売店コードの表示と候補を確認

ステップ3：
購読種別を 3:併読 へ切り替え、販売店コードの候補を確認

ステップ4：
電子版の購読者（dokusya_id=301）を編集モードで開き、販売店コードの表示と候補を確認

### 期待結果

ステップ1：
`1000000001` と `1000000002` のみが候補に並び、ダミー販売店 `9999999999` は候補に**現れない**こと

ステップ2：
販売店コードの選択が**解除**され（空欄になり）、候補が `9999999999` の**1件のみ**に入れ替わること。切替前に選んでいた `1000000001` は候補に残らないこと

ステップ3：
候補が `1000000001` / `1000000002` に戻り、`9999999999` は現れないこと。購読種別を切り替えたため選択は再び解除されていること

ステップ4：
購読種別は編集で不変のため、候補は `9999999999` の1件のみで、現在の選択 `9999999999` がそのまま表示されること

補足：
・電子版は紙を配達しないため実在の販売店に紐づかないが、`t_dokusya.hanbaiten_id` は NOT NULL。そのため JA ごとに `hanbaiten_code = 9999999999` のダミー販売店を受け皿として用意し、電子版はそこへ紐づける（顧客要件 2026-08）
・絞込は BE（`GET /api/v1/hanbaiten/dropdown` の `dummy=only` / `dummy=exclude`）の SQL 側で行い、FE はどちら側かを送るだけ。FE で候補配列を後から filter すると、ページング時に「絞込後0件」のページが返り無限スクロールが止まる
・切替時に選択を解除するのは、残すと画面には候補に無いコードが表示され、そのまま更新すると BE が別種別の販売店を受け付けてしまうため
・営業中フィルタ（`active_only`）とは独立。編集で既に廃店の販売店へ紐づく購読者は従来どおり `include_id` ピンで現在の選択を復元する

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

## ACSMS-TC-011-049 — 承認/否認前に支払方法・引落口座4項目を編集して保存（#56524）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属、電子版取扱フラグあり）
  - ・購読種別=電子版 かつ denshi_shonin_status=0（承認待ち）の購読者（dokusya_id=200）が存在
  - ・支払方法=口座引落（shiharai_hoho=1）、自JAに金融機関支店（kinyu_shiten_flg=true）が2件以上存在

### 手順

ステップ1：
承認待ちの購読者を編集モードで開き、各項目の活性状態を確認

ステップ2：
支払方法=口座引落のまま引落口座支店を別の支店へ変更し、引落口座貯金種目=当座、引落口座番号=`9876543210`、引落口座名義=`ﾀﾅｶ ﾀﾛｳ` を入力して「承認・登録」ボタンを押下

ステップ3：
DBで保存結果を確認
```sql
SELECT denshi_shonin_status, shiharai_hoho, bank_branch_code, bank_branch_name,
       hikiotoshi_yokin_shubetsu, hikiotoshi_koza_no, hikiotoshi_koza_meigi
FROM t_dokusya WHERE dokusya_id = 200;
```

ステップ4：
別の承認待ち購読者を開き、引落口座番号を空欄にして「承認・登録」ボタンを押下

ステップ5：
別の承認待ち購読者を開き、引落口座名義を修正してから「承認しない」ボタンを押下し、確認ダイアログで「はい」を選択

ステップ6：
別の承認待ち購読者（支払方法=現金集金・引落先未設定）を開き、支払方法を口座引落へ変更し、引落口座支店を未選択のまま「承認・登録」ボタンを押下

### 期待結果

ステップ1：
支払方法・引落口座支店・引落口座貯金種目・引落口座番号・引落口座名義の5項目と新聞単価が**活性**であること。それ以外の項目（購読者氏名、メールアドレス、組合員コードなど）は**非活性**のままであること。支払方法の選択肢のうちクレジットカードは**非活性**であること

ステップ2：
「承認しました。」が表示され、購読者明細検索画面へ遷移すること

ステップ3：
`denshi_shonin_status=1` に加え、入力した4項目が保存されていること。`bank_branch_code` / `bank_branch_name` は選択した支店から逆引きされた値になっていること

ステップ4：
承認APIが呼ばれず、引落口座番号の直下に「必須項目です。」が表示され、当該項目へフォーカスが移動すること

ステップ5：
「否認しました。」が表示され、`denshi_shonin_status=2` と修正後の引落口座名義がともに保存されていること

ステップ6：
承認APIが呼ばれず、引落口座支店の直下に「必須項目です。」が表示されること

補足：
・電子版申込の支払方法・口座情報は読者本人の自己申告のため誤りが多い。従来は承認画面で新聞単価しか直せず「承認 → 再編集」の2操作が必要だった（顧客要件 2026-08 / #56524）
・電子版はクレジットカードを選択できない（電子版読者管理システム連携専用）。APIへ直接 `shiharai_hoho=6` を送っても `VALIDATION_ERROR`（shiharai_hoho）となる
・引落口座支店は自JAの金融機関支店のみ選択可。他JAの支店IDを直接APIへ送っても `VALIDATION_ERROR`（bank_shiten_id）となる
・支払方法=口座引落以外の場合、4項目は任意（空欄のまま承認できる）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---
