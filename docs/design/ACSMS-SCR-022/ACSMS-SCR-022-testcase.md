---
customer_name: 日本農業新聞様
system_name: クラウド版購読者管理システム
document_name: テスト仕様書
screen_id: ACSMS-SCR-022
screen_name: ファイルダウンロード画面
format_code: 16-BM/PM/VTI
format_version: "1.1"
issue_date: 2026-07-02
test_level: 結合テスト
test_environment: Windows 10/11, Chrome, Edge
author: Tran Duc Tuyen
reviewer: Nguyen Huy Dat
---


## 変更履歴

| No. | 発行日 | 版数 | 担当者 | 変更内容 | 確認者 | 承認者 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-05-27 | 1.0 | Kieu Thi Diem | 新規作成 | Nguyen Huy Dat |  |
| 2 | 2026-07-02 | 1.1 | Tran Duc Tuyen | データソースを t_file_download に変更。一覧列を再構成(対象年月列削除、ダウンロード種別追加)。ダウンロード実行はt_file_download未INSERT・t_log(log_type=4)のみ。日農ダウンロード許可フラグ(403制御)と複数ファイル一括ダウンロード(ZIP)のテストケースを追加。 |  |  |
| 3 | 2026-08-06 | 1.2 | Tran Duc Tuyen | 実装との差分是正（顧客要件2026-07の2件が未反映だった）：①TC-003 を中央会の新DataScope（同一都道府県の全JA／#52132）へ書き換え。従来の「自中央会+管轄JA」記述は実装と一致しなくなっていた。②カテゴリ9を新設し2件追加（TC-059〜060）。059＝県スコープ拡大の一覧・個別DL・プレビュー・ZIP、`todofuken_code` 未設定セッションの自JAフォールバック、JA本店/JA管理支店へ広がらないこと、FEのJA絞り込み候補。060＝日農DL許可=false のDL制限ロールに中央会を追加したこと、および自分が出力したファイルは本フラグを見ない例外（`created_by` 空文字は本人扱いしない） |  |  |


## システム概要

本システムは、JA向けのクラウド型読者管理システムであり、
購読者情報の管理、購読履歴の管理、口座振替データの管理などの機能を提供する。
主な機能として、購読者情報の登録・更新・検索、
購読内容の変更履歴管理、口座振替データの作成および管理、
ファイルのアップロード・ダウンロード機能、システムのお知らせ管理などを提供する。
また、ユーザーのログイン管理、ログイン履歴の記録、
ユーザー操作ログの記録などのセキュリティ・監査機能をサポートする。

## 資料目的

「ファイルダウンロード画面（ACSMS-SCR-022）」において、システム上で新規作成されるテスト仕様書の詳細を記述した資料です。
本資料はISTQBおよびIEEE 829を参考とし、以下の品質基準を満たすものとする。

- 各テストケースは単一のシナリオに基づき作成する（単一責任）。
- 手順は再現可能な粒度で記述し、テストデータを明示する。
- 期待結果は測定可能とする（メッセージ内容、DBクエリ結果、HTTPステータスコード等）。

## 関連資料

| No | 資料コード | 資料名 |
| --- | --- | --- |
| 1 | ACSMS-SCR-022 | ファイルダウンロード画面設計書 |
| 2 | ACSMS-SCR-022-api | ファイルダウンロード API設計書 |
| 3 | testcase-viewpoints | 全システム共通 テスト観点一覧 |


## テストカテゴリ一覧

| # | カテゴリ | テストケース数 |
| --- | --- | --- |
| 1 | アクセス権限制御（Access Control） | 9 |
| 2 | 画面表示・レスポンシブ（Layout & Responsive） | 6 |
| 3 | ヘッダー・パンくず（Header & Breadcrumb） | 3 |
| 4 | 入力バリデーション — 検索条件 | 9 |
| 5 | 業務ロジック — 検索・一覧（Function — Search） | 9 |
| 6 | 業務ロジック — プレビュー（Function — Preview） | 4 |
| 7 | 業務ロジック — ダウンロード（Function — Download） | 10 |
| 8 | 共通エラーハンドリング（Common Error Handling） | 8 |
| 9 | DataScope拡大・DL許可の例外（Scope / Download Gate） | 2 |
|  | 合計 | 60 |

---

# カテゴリ 1: アクセス権限制御（Access Control）

## ACSMS-TC-022-001 — NICHINO_ADMIN によるファイルダウンロード画面アクセス + 全件参照

- 観点ID: VP-A-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・権限「file.download」を保持

### 手順

ステップ1：
ダッシュボードを開き、サイドバーまたはメニューから「ファイルダウンロード」へ遷移

ステップ2：
URL `/file-download` に直接アクセス

ステップ3：
DevTools の Network タブで GET `/api/v1/file-download` のレスポンスを確認

ステップ4：
DB で `SELECT COUNT(*) FROM t_file_download WHERE deleted_at IS NULL` を実行し、返却件数と一致するか確認

### 期待結果

ステップ1：
ファイルダウンロード画面へ遷移すること、画面タイトル `ファイルダウンロード画面` が表示されること

ステップ2：
画面が正常にレンダリングされること、アクセス拒否トーストが表示されないこと

ステップ3：
HTTPステータスコード200が返却されること、`data` 配列に複数 JA を跨ぐファイルが含まれること

ステップ4：
DB 全件数とレスポンス `meta.total` が一致すること（NICHINO_ADMIN は DataScope フィルタなしで全件参照可能）

補足：
・NICHINO_ADMIN は `t_file_download` 全件を参照できる役割であり、`ja_id IS NULL`（全 JA 向け）ファイルおよび他 JA のファイルも見えること
・サイドバーにメニュー項目が表示され、誤って非表示にならないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-002 — NICHINO_STAFF によるファイルダウンロード画面アクセス + 全件参照

- 観点ID: VP-A-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_STAFF
  - ・ログイン済 + MFA認証済
  - ・権限「file.download」を保持

### 手順

ステップ1：
URL `/file-download` にアクセス

ステップ2：
GET `/api/v1/file-download` を呼び出し、レスポンス `data[].ja_id` の分布を確認

### 期待結果

ステップ1：
画面が正常に表示されること

ステップ2：
HTTPステータスコード200が返却されること、複数 JA のファイルおよび `ja_id IS NULL` のファイルが含まれること（NICHINO_STAFF も全件参照可能、DataScope フィルタなし）

補足：
・NICHINO_ADMIN と同等の参照範囲を持つこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-003 — CHUOKAI による DataScope 参照範囲確認

- 観点ID: VP-A-02
- 種類: Normal (正常)
- 前提条件:
  - ・role: CHUOKAI
  - ・ログイン済 + MFA認証済
  - ・自中央会と同一都道府県の JA、他県の JA が事前にデータベース上に登録済み
  - ・セッションに `todofuken_code` が設定されていること
  - ・テストデータ：自 JA のファイル 2 件、**同一都道府県の他 JA** のファイル 2 件、**他県 JA** のファイル 2 件、全 JA 向けファイル（`ja_id IS NULL`）1 件

### 手順

ステップ1：
URL `/file-download` にアクセス

ステップ2：
GET `/api/v1/file-download?per_page=100` を呼び出し、`data[].ja_id` の値を確認

ステップ3：
他県 JA のファイル（`ja_id` がスコープ外）を直接ダウンロード API GET `/api/v1/file-download/{file_download_id}/download` で呼び出し

### 期待結果

ステップ1：
画面が正常に表示されること

ステップ2：
自 JA のファイル + **同一都道府県の他 JA** のファイル + 全 JA 向けファイル（合計 5 件）のみ返却されること、他県 JA のファイルが含まれないこと（顧客要件2026-07 / #52132 で「自JAのみ」から拡大）

ステップ3：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`、メッセージ `指定されたファイルが見つかりません。`）— DataScope 違反時は存在隠蔽として 404 を返す仕様

補足：
・参照可能条件：`fu.ja_id IN (:managed_ja_ids) OR fu.ja_id IS NULL`
・スコープ外アクセスは 403 ではなく 404 を返却することで、ファイル ID の存在を漏らさない設計

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-004 — JA_HONTEN による DataScope 参照範囲確認

- 観点ID: VP-A-02
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：自 JA のファイル 3 件、他 JA のファイル 2 件、全 JA 向けファイル 1 件

### 手順

ステップ1：
URL `/file-download` にアクセスし、一覧を確認

ステップ2：
他 JA のファイル ID を DevTools から GET `/api/v1/file-download/{file_download_id}/preview` に渡して直接呼び出し

### 期待結果

ステップ1：
自 JA のファイル + 全 JA 向けファイル（合計 4 件）のみ表示されること、他 JA のファイルが表示されないこと

ステップ2：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`）

補足：
・参照可能条件：`fu.ja_id = :user_ja_id OR fu.ja_id IS NULL`

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-005 — JA_KANRI_SHITEN による DataScope 参照範囲確認

- 観点ID: VP-A-02
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_KANRI_SHITEN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：自 JA 内の自管理支店ファイル 2 件、自 JA 内の他管理支店ファイル 2 件、他 JA のファイル 2 件、全 JA 向けファイル 1 件

### 手順

ステップ1：
URL `/file-download` にアクセスし、一覧を確認

ステップ2：
他 JA のファイル ID を直接 GET `/api/v1/file-download/{file_download_id}/download` に渡して呼び出し

### 期待結果

ステップ1：
自 JA のファイル（自管理支店 + 他管理支店、合計 4 件）+ 全 JA 向けファイル 1 件、合計 5 件が表示されること、他 JA のファイルが表示されないこと

ステップ2：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`）

補足：
・JA 単位で絞り込み、管理支店単位での絞り込みは行わない（仕様）
・自 JA 配下のすべての管理支店ファイルが参照可能であること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-006 — 未認証ユーザーによる URL 直接アクセス禁止

- 観点ID: VP-A-04
- 種類: Abnormal (異常)
- 前提条件:
  - ・セッションを保持しない（ブラウザのプライベートモード等）

### 手順

ステップ1：
ブラウザのアドレスバーに直接 `/file-download` を入力してアクセス

ステップ2：
DevTools から GET `/api/v1/file-download` を呼び出し

### 期待結果

ステップ1：
ログイン画面 `/login` へ遷移すること、URL クエリパラメータに `redirect=/file-download` が付与されること

ステップ2：
HTTPステータスコード401が返却されること（`error_code: UNAUTHORIZED`、メッセージ `セッションが切れました。再度ログインしてください。`）

補足：
・ルーターガードでセッション未保有を検知し、ログイン後の遷移先を URL クエリに保持すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-050 — 日農ロール × nichino_download_allowed_flg=false のダウンロード禁止（403 + 行 disabled）

- 観点ID: VP-A-03
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN（役割 ID = 1）
  - ・ログイン済 + MFA認証済
  - ・テストデータ：`t_file_download` に `nichino_download_allowed_flg = false`（日農ダウンロード不許可）のファイル 1 件（S3 上に実体存在）

### 手順

ステップ1：
URL `/file-download` にアクセスし、該当ファイルが一覧に表示されるか確認

ステップ2：
該当ファイル行のチェックボックスおよび操作ボタン（プレビュー / ダウンロード）の状態を確認

ステップ3：
DevTools から GET `/api/v1/file-download/{file_download_id}/download` を直接送信

ステップ4：
DevTools から GET `/api/v1/file-download/{file_download_id}/preview` を直接送信

### 期待結果

ステップ1：
該当ファイルが一覧に表示されること（行自体は非表示にならないこと）

ステップ2：
該当行のチェックボックス・操作が無効（disabled）状態であること（`nichino_download_allowed_flg = false` の日農ロールは操作不可）

ステップ3：
HTTPステータスコード403が返却されること（`error_code: FORBIDDEN`、メッセージ `このファイルは日農のダウンロードが許可されていません。`）、ダウンロード処理が実行されないこと

ステップ4：
HTTPステータスコード403が返却されること（`error_code: FORBIDDEN`、メッセージ `このファイルは日農のダウンロードが許可されていません。`）、プレビューが実行されないこと

補足：
・**DL制限ロール**（NICHINO_ADMIN 役割1 / NICHINO_STAFF 役割2 / **CHUOKAI 役割3**）は `nichino_download_allowed_flg = false` のファイルをダウンロード / プレビュー不可（中央会の追加は顧客要件2026-07）。ただし**自分が出力したファイルは本フラグを見ない**（#52132・ACSMS-TC-022-060 参照）
・行は一覧に表示されるが、操作は無効（disabled）となること
・行 disabled 条件：(a) 論理削除済（`deleted_at ≠ NULL`）OR (b) DL制限ロール（日農 + 中央会）かつ `nichino_download_allowed_flg = false` かつ **自分が出力したファイルでない**

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-051 — 日農ロール × nichino_download_allowed_flg=true のダウンロード許可

- 観点ID: VP-A-03
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_STAFF（役割 ID = 2）
  - ・ログイン済 + MFA認証済
  - ・テストデータ：`t_file_download` に `nichino_download_allowed_flg = true`（日農ダウンロード許可）のファイル 1 件（S3 上に実体存在）

### 手順

ステップ1：
URL `/file-download` にアクセスし、該当ファイル行のチェックボックス・操作の状態を確認

ステップ2：
該当ファイルのチェックボックスをオンにし、ダウンロード実行ボタンを押下、GET `/api/v1/file-download/{file_download_id}/download` のレスポンスを確認

### 期待結果

ステップ1：
該当行のチェックボックス・操作が有効（disabled でない）状態であること

ステップ2：
HTTPステータスコード200が返却されること、ファイルがダウンロードされ、成功メッセージ `ダウンロードが完了しました。`（ACSMS-MSG-022-005）が表示されること

補足：
・`nichino_download_allowed_flg = true` のファイルは日農ロールでも通常どおりダウンロード可能であること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-052 — JA系ロールは nichino_download_allowed_flg の影響を受けない

- 観点ID: VP-A-03
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：自 JA に紐づく `nichino_download_allowed_flg = false` のファイル 1 件（S3 上に実体存在、DataScope 内）

### 手順

ステップ1：
URL `/file-download` にアクセスし、該当ファイル行のチェックボックス・操作の状態を確認

ステップ2：
該当ファイルのチェックボックスをオンにし、ダウンロード実行ボタンを押下、GET `/api/v1/file-download/{file_download_id}/download` のレスポンスを確認

### 期待結果

ステップ1：
該当行のチェックボックス・操作が有効（disabled でない）状態であること（JA系ロールは日農ダウンロード許可フラグの影響を受けない）

ステップ2：
HTTPステータスコード200が返却されること、ファイルがダウンロードされ、成功メッセージ `ダウンロードが完了しました。`（ACSMS-MSG-022-005）が表示されること

補足：
・`nichino_download_allowed_flg` はDL制限ロール（日農 役割1 / 役割2 + **中央会 役割3**）に作用する制御であり、JA_HONTEN / JA_KANRI_SHITEN は自身の DataScope 範囲内であればフラグ値に関わらずダウンロード / プレビュー可能であること。中央会は顧客要件2026-07 で制限対象に追加された（ACSMS-TC-022-060 参照）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 2: 画面表示・レスポンシブ（Layout & Responsive）

## ACSMS-TC-022-007 — 画面タイトルおよびページレイアウトの初期表示

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
URL `/file-download` にアクセス

ステップ2：
画面構成（タイトル、検索エリア、結果一覧、ページネーション）を視覚確認

### 期待結果

ステップ1：
画面が表示されること

ステップ2：
・画面タイトル `ファイルダウンロード画面` が左上に表示されること
・上部に検索エリア（ファイル名・都道府県）が表示されること
・中央に結果一覧テーブルが表示されること
・テーブル下部にページネーションが表示されること
・画面崩れが発生しないこと

補足：
・デザイントークン（色・余白・フォント）に従ったレイアウトであること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-008 — 検索エリアの項目および主ボタンの表示

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
画面を表示し、検索エリアの構成要素を確認

ステップ2：
各ボタンのラベルと配置を確認

### 期待結果

ステップ1：
・ファイル名テキストボックスが表示されること（プレースホルダー：任意）
・都道府県プルダウンが表示されること（全 47 都道府県の選択肢を保持）
・ダウンロード種別プルダウンが表示されること（m_code `DOWNLOAD_TYPE` の 5 選択肢：口座振替 / その他 / 増減連絡票 / 増減通知書 / 購読者名簿）
・JA プルダウンが表示されること（NICHINO_ADMIN / NICHINO_STAFF のみ。JA系ロールでは非表示）

ステップ2：
・主ボタン `検索` が表示されること
・副ボタン `検索クリア` が表示されること
・副ボタン `プレビュー` が表示されること
・主ボタン `ダウンロード実行` が結果一覧ヘッダーに表示されること

補足：
・主ボタンは塗りつぶしの強調色、副ボタンは枠線のみで配色を区別すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-009 — 結果一覧テーブルのカラム表示順序

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：`t_file_download` に 5 件以上のファイルが存在

### 手順

ステップ1：
画面を表示し、テーブルのカラムヘッダーを確認

ステップ2：
1 行のデータが各カラムに対応する値で表示されることを確認

### 期待結果

ステップ1：
カラムが左から `選択（チェックボックス） / ダウンロード日時 / 作成者 / JA名 / ダウンロード種別 / ファイル名 / サイズ / 操作` の順序で表示されること（旧「対象年月」カラムは廃止され、「JA名」「ダウンロード種別」カラムが追加されていること）

ステップ2：
・ダウンロード日時カラムに `YYYY/MM/DD HH:mm:ss` 形式の値（`download_datetime`）が表示されること
・作成者カラムに `created_by_name` の値が表示されること
・JA名カラムに `ja_code` + `ja_name` の結合表示（`ja_id IS NULL` の全 JA 向けファイルは空欄）が表示されること
・ダウンロード種別カラムに m_code `DOWNLOAD_TYPE`（1:口座振替 / 2:その他 / 3:増減連絡票 / 4:増減通知書 / 5:購読者名簿）のラベルが表示されること
・ファイル名カラムが強調色のリンク表示であること
・サイズカラムに人間可読のサイズ（例: `1.2 MB`）が表示されること

補足：
・結果一覧は各帳票出力画面（SCR-020/021/026/028/029）が `t_file_download` に登録したファイルを表示すること
・作成者名（`created_by_name`）の元データが存在しない場合、作成者は空欄表示されること
・旧実装にあった「対象年月」カラムは現行仕様では存在しないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-010 — ページネーションおよび件数選択の表示

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：`t_file_download` に 100 件以上のファイルが存在

### 手順

ステップ1：
画面を表示し、テーブル下部のページネーション要素を確認

ステップ2：
ページ番号ボタンと「次へ」「前へ」ボタンを順に確認

ステップ3：
件数選択プルダウンの選択肢を確認

### 期待結果

ステップ1：
・「全 N 件」の表示があること
・ページ番号ボタン（1, 2, 3, ..., 末尾）が表示されること
・「前へ」「次へ」のアイコンボタンが表示されること

ステップ2：
ボタンのフォーカス状態（現ページは強調色塗りつぶし）が正しいこと

ステップ3：
件数選択プルダウンに `10 / 頁`、`20 / 頁`、`50 / 頁`、`100 / 頁` の 4 選択肢が表示され、初期値が `20 / 頁` であること

補足：
・デフォルト表示件数は 20 件／頁（screen-design.md §機能 7）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-011 — レスポンシブレイアウト（タブレット幅）

- 観点ID: VP-E-02
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
ブラウザのウィンドウサイズを 768px 幅（タブレット相当）に変更

ステップ2：
画面全体のレイアウトおよび結果一覧テーブルの表示を確認

ステップ3：
ブラウザの幅を 375px 幅（モバイル相当）に変更し、同様に確認

### 期待結果

ステップ1：
画面崩れが発生しないこと

ステップ2：
テーブルが横スクロール可能となること、検索エリアの項目が複数行に折り返されること

ステップ3：
モバイル幅でも画面要素が重ならず、横スクロールにより全カラムが閲覧可能であること

補足：
・タブレット ブレークポイント 768px、モバイル ブレークポイント 375px を基準とする

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-012 — キーボード操作によるフォーカス移動

- 観点ID: VP-E-03
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
画面を表示し、ファイル名テキストボックスにフォーカスを置く

ステップ2：
Tab キーを順次押下し、フォーカス順序を確認

ステップ3：
チェックボックス行で Space キーを押下

### 期待結果

ステップ1：
ファイル名テキストボックスに入力フォーカスが当たること

ステップ2：
フォーカス順序が `ファイル名 → 都道府県 → 検索 → 検索クリア → プレビュー → 全選択チェックボックス → 各行チェックボックス → ファイル名リンク → ダウンロード実行 → ページネーション → 件数選択` の順で正しく移動すること

ステップ3：
チェックボックスのオン／オフが切り替わること

補足：
・キーボードのみで全機能が操作可能であること（A11y 要件）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 3: ヘッダー・パンくず（Header & Breadcrumb）

## ACSMS-TC-022-013 — パンくず階層の表示と遷移

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
画面を表示し、パンくずナビゲーションを確認

ステップ2：
「ホーム」をクリック

### 期待結果

ステップ1：
パンくずが `ホーム > その他 > ファイルダウンロード` の階層で表示されること、最終要素 `ファイルダウンロード` が強調色で表示されること

ステップ2：
ダッシュボード画面 `/dashboard` へ遷移すること

補足：
・パンくず最終要素は現在ページを示すためリンクとして動作しないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-014 — ヘッダーのログインユーザー名表示

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン ID `admin01`、表示名「日農（管理者）」でログイン済み

### 手順

ステップ1：
画面右上のユーザー情報エリアを確認

### 期待結果

ステップ1：
ログインユーザー名が `admin01:日農（管理者）` の形式で表示されること、ユーザーアイコンが表示されること

補足：
・表示形式 `{login_id}:{account_name}`（screen-design 共通ヘッダー定義）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-015 — ダークモード切替

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
サイドバー下部のダークモード切替ボタンを押下

ステップ2：
画面色および各 UI 要素のコントラストを確認

ステップ3：
画面を再読み込みし、テーマ設定が維持されることを確認

### 期待結果

ステップ1：
画面背景・テキスト色・テーブル罫線などがダークテーマに切り替わること

ステップ2：
全テキストおよびボタンのコントラスト比が WCAG AA 基準（4.5:1）以上であること、画面崩れが発生しないこと

ステップ3：
再読み込み後もダークモードが維持されること（ローカルストレージで保持）

補足：
・ライトモードへの再切替も同様に動作すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 4: 入力バリデーション — 検索条件

## ACSMS-TC-022-016 — ファイル名（部分一致）正常入力

- 観点ID: VP-B-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：ファイル名 `zougen_tsuchi_202604.pdf`、`zougen_renraku_202604.csv`、`kouza_furikae_202604.csv` の 3 件が登録済み

### 手順

ステップ1：
ファイル名テキストボックスに `zougen` を入力

ステップ2：
検索ボタンを押下し、GET `/api/v1/file-download?file_name=zougen` を確認

### 期待結果

ステップ1：
入力値がそのままテキストボックスに表示されること（入力可能文字数以内で登録できること）

ステップ2：
HTTPステータスコード200が返却されること、`data` に `zougen_tsuchi_202604.pdf` と `zougen_renraku_202604.csv` の 2 件のみ表示されること（部分一致検索ができること）

補足：
・SQL は `file_name ILIKE '%zougen%'` を発行すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-017 — ファイル名 — 前後空白の自動トリム

- 観点ID: VP-B-04
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
ファイル名テキストボックスに `  zougen  `（前後に半角空白）を入力

ステップ2：
検索ボタンを押下し、DevTools でリクエストパラメータを確認

### 期待結果

ステップ1：
入力値がテキストボックス上に表示されること

ステップ2：
リクエスト送信時に前後空白が除去され、サーバー側で `file_name=zougen` として処理されること、入力値がトリムされること

補足：
・前後空白除去は フロントエンド側でも サーバー側でも行われ、両層で一貫した挙動になること（api.md §4.1）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-018 — ファイル名 — 最大桁数 255 文字 境界値

- 観点ID: VP-B-02
- 種類: Boundary (境界)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
ファイル名テキストボックスに 255 文字の半角英数字を入力し、検索ボタンを押下

ステップ2：
DevTools 経由で GET `/api/v1/file-download?file_name={256文字}` を直接送信

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること、入力可能文字数以内で登録できること

ステップ2：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、`errors` 配列に `field: file_name` と桁数超過エラーが含まれること）

補足：
・フロントエンド側 maxlength=255 で 256 文字目以降の入力を抑制すること
・サーバー側でもバリデーションが行われ、不正な値が登録されないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-019 — 都道府県プルダウン 47 件表示と選択

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
画面表示時に GET `/api/v1/todofuken` が呼び出されることを DevTools で確認

ステップ2：
都道府県プルダウンを開き、選択肢の数と内容を確認

ステップ3：
「東京都」を選択し、選択値を確認

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること、レスポンスに 47 件の都道府県情報が含まれること

ステップ2：
プルダウンに 47 件の選択肢が表示されること（`01:北海道` から `47:沖縄県` まで）

ステップ3：
プルダウンの表示が `13:東京都` となること、選択値 `todofuken_code = 13` が内部状態に保持されること

補足：
・共用 API `ACSMS-API-COMMON-001` を使用、役割別の絞り込みは行わないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-020 — 都道府県コード 形式不正 直接送信

- 観点ID: VP-B-03
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
DevTools から GET `/api/v1/file-download?todofuken_code=AB` を直接送信

ステップ2：
GET `/api/v1/file-download?todofuken_code=1` を直接送信

ステップ3：
GET `/api/v1/file-download?todofuken_code=999` を直接送信

### 期待結果

ステップ1：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、`errors` に `field: todofuken_code` の形式エラーメッセージ `都道府県コードは半角数字2桁で入力してください。` が含まれること）

ステップ2：
HTTPステータスコード400が返却されること（桁数不足、形式エラーが表示されること）

ステップ3：
HTTPステータスコード400が返却されること（桁数超過、形式エラーが表示されること）

補足：
・正規表現 `^\d{2}$` で検証すること（api.md §4.1）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-021 — per_page パラメータ 境界値（1 / 100 / 101）

- 観点ID: VP-B-02
- 種類: Boundary (境界)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
GET `/api/v1/file-download?per_page=1` を送信

ステップ2：
GET `/api/v1/file-download?per_page=100` を送信

ステップ3：
GET `/api/v1/file-download?per_page=101` を送信

ステップ4：
GET `/api/v1/file-download?per_page=0` を送信

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること、`data` の長さが 1 件以下であること、`meta.per_page = 1` であること

ステップ2：
HTTPステータスコード200が返却されること、`meta.per_page = 100` であること

ステップ3：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、メッセージ `1ページあたりの件数は1〜100の範囲で指定してください。`）

ステップ4：
HTTPステータスコード400が返却されること（最小 1 を下回るため）

補足：
・許容範囲 `1〜100`（api.md §4.1）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-022 — sort_by 許容外値 直接送信

- 観点ID: VP-B-07
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
DevTools から GET `/api/v1/file-download?sort_by=password` を送信

ステップ2：
GET `/api/v1/file-download?sort_by=DROP TABLE` を送信

### 期待結果

ステップ1：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`）— 許容値は `download_datetime / file_name / file_size / created_by / created_by_name` のみ

ステップ2：
HTTPステータスコード400が返却されること、SQL インジェクションとして実行されないこと（パラメータ化クエリにより遮断）

補足：
・許容値外を渡しても、サーバー側で安全に拒否されること（SQL インジェクション対策）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-023 — ファイル名 — SQL インジェクション安全性

- 観点ID: VP-A-10
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
ファイル名テキストボックスに `' OR '1'='1` を入力し、検索ボタンを押下

ステップ2：
ファイル名テキストボックスに `'; DROP TABLE t_file_download; --` を入力し、検索ボタンを押下

ステップ3：
DB で `SELECT COUNT(*) FROM t_file_download` を実行し、テーブルが存在することを確認

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること、入力値が ILIKE パラメータとして安全に扱われ、検索結果が空 0 件で表示されること

ステップ2：
HTTPステータスコード200が返却されること、テーブルへの破壊操作が実行されないこと

ステップ3：
`t_file_download` テーブルが存在すること（破壊されていないこと）、データ不整合が発生しないこと

補足：
・パラメータ化クエリ（`ILIKE '%' || :file_name || '%'`）により SQL インジェクションが遮断されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-053 — ダウンロード種別（download_type）許容外値 直接送信

- 観点ID: VP-B-03
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
DevTools から GET `/api/v1/file-download?download_type=1` を直接送信（有効値）

ステップ2：
GET `/api/v1/file-download?download_type=9` を直接送信（m_code DOWNLOAD_TYPE に存在しない値）

ステップ3：
GET `/api/v1/file-download?download_type=ABC` を直接送信（数値以外）

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること、`download_type = 1`（口座振替）のファイルのみに絞り込まれること

ステップ2：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、`errors` に `field: download_type` の値不正エラーが含まれること）— 許容値は m_code `DOWNLOAD_TYPE`（1:口座振替 / 2:その他 / 3:増減連絡票 / 4:増減通知書 / 5:購読者名簿）のみ

ステップ3：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、形式エラー）

補足：
・ダウンロード種別は m_code `DOWNLOAD_TYPE` の 5 種類のいずれかのみ許容すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 5: 業務ロジック — 検索・一覧（Function — Search）

## ACSMS-TC-022-024 — 初期表示 — デフォルトソート（download_datetime DESC）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：`t_file_download` に異なるダウンロード日時のレコードが 5 件以上存在

### 手順

ステップ1：
URL `/file-download` にアクセス

ステップ2：
結果一覧のダウンロード日時カラムの並び順を確認

ステップ3：
GET `/api/v1/file-download` のレスポンスを確認

### 期待結果

ステップ1：
画面が表示されること

ステップ2：
ダウンロード日時が新しい順（DESC）に並んでいること

ステップ3：
レスポンスの `data` 配列が `download_datetime` DESC で返却されること、`meta.page = 1`、`meta.per_page = 20` であること

補足：
・デフォルト ソート順は `download_datetime DESC`（api.md §4.1）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-025 — ファイル名検索 + 都道府県検索の組み合わせ

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：東京都の JA に紐づくファイル 3 件、大阪府の JA に紐づくファイル 2 件、全 JA 向けファイル 1 件（全てファイル名に `meibo` を含む）

### 手順

ステップ1：
ファイル名に `meibo` を入力、都道府県プルダウンで「東京都」を選択

ステップ2：
検索ボタンを押下し、結果一覧と GET リクエストパラメータを確認

### 期待結果

ステップ1：
入力値が画面に反映されること

ステップ2：
HTTPステータスコード200が返却されること、リクエスト `?file_name=meibo&todofuken_code=13` で送信されること、東京都の JA に紐づく 3 件のみ表示されること（全 JA 向けファイルは都道府県絞り込み時は除外）

補足：
・JOIN チェーン `t_file_download.ja_id → m_ja.ja_id → m_ja.todofuken_code` を経由
・`ja_id IS NULL` のファイルは LEFT JOIN により `j.todofuken_code IS NULL` となり、`NULL = 13` は FALSE となるため除外（api.md §4.3）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-026 — 都道府県未指定時に全 JA 向けファイル表示

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：`ja_id IS NULL` のファイル 2 件、東京都 JA のファイル 1 件

### 手順

ステップ1：
都道府県プルダウンを未選択にし、検索ボタンを押下

ステップ2：
結果一覧の `ja_id` 値の分布を確認

### 期待結果

ステップ1：
画面が表示されること

ステップ2：
`ja_id IS NULL` のファイル 2 件と東京都 JA のファイル 1 件、合計 3 件が表示されること（都道府県絞り込みなしのため全件参照）

補足：
・全 JA 向けファイルは都道府県絞り込みなしのデフォルト一覧でのみ表示されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-027 — 検索結果 0 件 — ACSMS-MSG-022-001 表示

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：`t_file_download` 内に `'XYZNOTFOUND'` を含むファイル名が存在しない

### 手順

ステップ1：
ファイル名テキストボックスに `XYZNOTFOUND` を入力し、検索ボタンを押下

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること、`data: []` + `meta.total: 0` が返却されること、画面にメッセージ `該当するデータがありません。`（ACSMS-MSG-022-001）が表示されること、検索結果が0件で表示されること

補足：
・0 件時は HTTP 200 を返却し、エラー扱いとはしないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-028 — 検索クリアによる条件リセットとデフォルト一覧再表示

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
ファイル名に `zougen`、都道府県に「東京都」を入力／選択し、検索ボタンを押下

ステップ2：
検索クリアボタン（上）を押下

### 期待結果

ステップ1：
検索結果が条件にマッチする件数で表示されること

ステップ2：
・ファイル名テキストボックスが空になること
・都道府県プルダウンが未選択状態になること
・結果一覧がデフォルト一覧（全件、`download_datetime DESC`）として再表示されること
・検索条件がクリアされること

補足：
・「クリア」ボタン（上）は検索条件 + 結果一覧をクリアし、デフォルト表示状態に戻すこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-029 — ページネーション — 検索条件維持

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：ファイル名に `zougen` を含むファイルが 25 件以上存在

### 手順

ステップ1：
ファイル名に `zougen` を入力、検索ボタンを押下（1 ページ目表示）

ステップ2：
ページネーションで「2」ページを押下

ステップ3：
DevTools で送信される GET リクエストのパラメータを確認

### 期待結果

ステップ1：
1 ページ目に 20 件のファイル一覧が表示されること

ステップ2：
2 ページ目のファイル一覧に切り替わること、検索条件 `zougen` がテキストボックスに保持されること

ステップ3：
リクエストが `?file_name=zougen&page=2&per_page=20&sort_by=download_datetime&sort_order=desc` で送信されること、検索条件が保持されること

補足：
・ページ番号変更時もユーザーが入力した検索条件が維持されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-030 — ソート — ファイル名カラムでの ASC / DESC 切替

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：ファイル名がアルファベット順で異なる 5 件以上のファイル

### 手順

ステップ1：
ファイル名カラムヘッダーをクリック

ステップ2：
ファイル名カラムヘッダーを再度クリック

ステップ3：
ファイル名カラムヘッダーを 3 度目クリック

### 期待結果

ステップ1：
ファイル名 ASC で並び替えられること、リクエストが `?sort_by=file_name&sort_order=asc` で送信されること

ステップ2：
ファイル名 DESC で並び替えられること、リクエストが `?sort_by=file_name&sort_order=desc` で送信されること

ステップ3：
ソート解除またはデフォルト（`download_datetime DESC`）に戻ること（FE 実装ポリシーに依存、ソート順が正しいこと）

補足：
・ソート対象カラムは `download_datetime / file_name / file_size / created_by / created_by_name` の 5 種類（api.md §4.1）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-031 — 論理削除済ファイル（deleted_at IS NOT NULL）の除外

- 観点ID: VP-C-03
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：通常ファイル 3 件、`deleted_at` に過去日時を設定した論理削除済ファイル 2 件

### 手順

ステップ1：
URL `/file-download` にアクセスし、結果一覧の件数を確認

ステップ2：
DB で `SELECT COUNT(*) FROM t_file_download WHERE deleted_at IS NULL` を実行

### 期待結果

ステップ1：
通常ファイル 3 件のみ表示されること、論理削除済ファイル 2 件は表示されないこと

ステップ2：
DB の有効件数が 3 件であること、画面表示件数と一致すること（削除済データが表示されないこと）

補足：
・WHERE 句に `fu.deleted_at IS NULL` を必ず含めること（api.md §4.3）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-054 — ダウンロード種別による絞り込み検索

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：`t_file_download` に `download_type = 1`（口座振替）2 件、`download_type = 4`（増減通知書）2 件、`download_type = 5`（購読者名簿）1 件

### 手順

ステップ1：
ダウンロード種別プルダウンで「増減通知書」を選択し、検索ボタンを押下

ステップ2：
GET リクエストパラメータおよび結果一覧を確認

ステップ3：
ダウンロード種別プルダウンを未選択に戻し、検索ボタンを押下

### 期待結果

ステップ1：
入力値が画面に反映されること

ステップ2：
HTTPステータスコード200が返却されること、リクエストが `?download_type=4` で送信されること、`download_type = 4`（増減通知書）の 2 件のみ表示されること

ステップ3：
ダウンロード種別の絞り込みが解除され、全 5 件が表示されること

補足：
・ダウンロード種別はプルダウンで m_code `DOWNLOAD_TYPE`（1:口座振替 / 2:その他 / 3:増減連絡票 / 4:増減通知書 / 5:購読者名簿）から選択すること
・他の検索条件（ファイル名・都道府県・JA）と AND 条件で組み合わせ可能であること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 6: 業務ロジック — プレビュー（Function — Preview）

## ACSMS-TC-022-032 — ファイル選択 + プレビューによる S3 署名付き URL モーダル表示

- 観点ID: VP-D-05
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：プレビュー可能な PDF ファイル 1 件（S3 上に実体存在）

### 手順

ステップ1：
結果一覧から PDF ファイル 1 件のチェックボックスをオンにする

ステップ2：
プレビューボタンを押下し、GET `/api/v1/file-download/{file_download_id}/preview` のレスポンスを確認

ステップ3：
プレビューモーダル内に PDF が描画されることを確認

### 期待結果

ステップ1：
チェックボックスがオン状態になり、該当行がハイライト表示されること

ステップ2：
HTTPステータスコード200が返却されること、レスポンス `data.preview_url` に S3 署名付き URL（`X-Amz-Signature` クエリ付き）が含まれること、`data.expires_at` が 1 時間後の ISO 8601 形式タイムスタンプであること

ステップ3：
モーダルが表示されること、`<iframe>` 内に PDF コンテンツが描画されること

補足：
・S3 presigned URL の有効期限は 3600 秒（api.md §4.4）
・モーダル外クリック / 閉じるボタンでモーダルを閉じられること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-033 — ファイル未選択時のプレビュー押下 — ACSMS-MSG-022-002

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
全チェックボックスを未選択の状態にする

ステップ2：
プレビューボタンを押下

### 期待結果

ステップ1：
全チェックボックスが未選択であること、行のハイライトが解除されていること

ステップ2：
警告メッセージ `ファイルを選択してください。`（ACSMS-MSG-022-002）が表示されること、プレビュー API が呼び出されないこと

補足：
・フロントエンド側で選択件数 0 を検知し、API リクエストを発行しないこと（不要な通信防止）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-034 — プレビュー対象ファイル不在 — ACSMS-MSG-022-003

- 観点ID: VP-D-05
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：DB 上のレコードは存在するが、S3 上の実ファイルが事前に削除されているファイル 1 件、または DB 上で `deleted_at` を直前に NOW() に更新したファイル 1 件

### 手順

ステップ1：
該当ファイルのチェックボックスをオンにし、プレビューボタンを押下

ステップ2：
GET `/api/v1/file-download/{file_download_id}/preview` のレスポンスを確認

### 期待結果

ステップ1：
警告メッセージ `ファイルが存在していません。`（ACSMS-MSG-022-003）が表示されること、モーダルが開かないこと

ステップ2：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`、メッセージ `指定されたファイルが見つかりません。`）

補足：
・FE は 404 を受け取り、ACSMS-MSG-022-003 にマッピングして表示すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-035 — フッタークリアボタンによる選択解除 + モーダル閉じ

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
複数行（2 件以上）のチェックボックスをオンにし、プレビューボタンを押下、モーダル表示

ステップ2：
フッターの「クリア」ボタン（下）を押下

### 期待結果

ステップ1：
モーダルが表示されること、選択行がハイライト状態であること

ステップ2：
全チェックボックスが未選択になること、全行のハイライトが解除されること、モーダルが閉じられること

補足：
・フッタークリアボタン（下）の動作は「選択解除 + プレビュークリア」（screen-design.md §機能 6）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 7: 業務ロジック — ダウンロード（Function — Download）

## ACSMS-TC-022-036 — 単一ファイルダウンロード成功 + t_file_download 未INSERT + 監査ログ記録

- 観点ID: VP-D-05
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：各帳票出力画面が `t_file_download` に登録した、自 JA に紐づく PDF ファイル 1 件（S3 上に実体存在、ファイル名 `zougen_tsuchi_202604.pdf`、`download_type = 4`）

### 手順

ステップ0（事前計測）：
DB で `SELECT COUNT(*) FROM t_file_download` を実行し、テスト前の総件数を記録

ステップ1：
結果一覧から該当ファイルのチェックボックスをオンにし、ダウンロード実行ボタンを押下

ステップ2：
GET `/api/v1/file-download/{file_download_id}/download` のレスポンスを確認

ステップ3：
ブラウザの保存ダイアログでファイルを保存し、内容が正しいことを確認

ステップ4：
DB で `SELECT COUNT(*) FROM t_file_download` を再実行し、ステップ0 の件数と比較

ステップ5：
DB で `SELECT * FROM t_log WHERE log_type = 4 AND operation = 'DOWNLOAD' AND target_id = :file_download_id ORDER BY log_datetime DESC LIMIT 1` を実行

### 期待結果

ステップ1：
チェックボックスがオンになり、ダウンロード処理が開始されること

ステップ2：
HTTPステータスコード200が返却されること、レスポンスヘッダに `Content-Type: application/pdf`、`Content-Disposition: attachment; filename="zougen_tsuchi_202604.pdf"; filename*=UTF-8''zougen_tsuchi_202604.pdf`、`Cache-Control: no-store` が設定されること

ステップ3：
ブラウザでダウンロードが完了すること、成功メッセージ `ダウンロードが完了しました。`（ACSMS-MSG-022-005）が表示されること、保存されたファイルが S3 上の実体と一致すること

ステップ4：
`t_file_download` の総件数がステップ0 から増加していないこと（ダウンロード実行時に `t_file_download` への新規 INSERT は行わない仕様であること）

ステップ5：
監査ログが 1 件だけ記録されること、`log_type = 4`（ファイル操作）、`operation = 'DOWNLOAD'`、`result_status = 1`、`after_value` にファイルメタ情報 JSON が含まれること

補足：
・`t_file_download` の行は各帳票出力画面（SCR-020/021/026/028/029）が登録するものであり、SCR-022 のダウンロード実行では INSERT しないこと
・ダウンロード実行で記録されるのは操作ログ `t_log`（`log_type = 4` / `operation = 'DOWNLOAD'`）の 1 件のみであること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-037 — 複数ファイル選択時のダウンロード実行（ZIP 一括ダウンロード）

- 観点ID: VP-D-05
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：各帳票出力画面が `t_file_download` に登録した、自 JA に紐づくファイル 3 件（PDF / CSV / Excel、S3 上に実体存在）

### 手順

ステップ0（事前計測）：
DB で `SELECT COUNT(*) FROM t_file_download` を実行し、テスト前の総件数を記録

ステップ1：
3 件のチェックボックスをオンにし、ダウンロード実行ボタンを押下

ステップ2：
POST `/api/v1/file-download/download-zip`（body `{ "file_download_ids": [id1, id2, id3] }`）のレスポンスを確認

ステップ3：
ブラウザでダウンロードされた ZIP ファイルを解凍し、3 ファイルが含まれることを確認

ステップ4：
DB で `SELECT COUNT(*) FROM t_file_download` を再実行し、ステップ0 の件数と比較

ステップ5：
DB で `SELECT COUNT(*) FROM t_log WHERE log_type = 4 AND operation = 'DOWNLOAD' AND account_id = :account_id AND log_datetime >= :test_start_time` を実行

### 期待結果

ステップ1：
一括ダウンロード処理が開始されること

ステップ2：
HTTPステータスコード200が返却されること、レスポンスが ZIP 形式（`Content-Type: application/zip`）で返却されること、成功メッセージ `ダウンロードが完了しました。`（ACSMS-MSG-022-005）が表示されること

ステップ3：
ZIP を解凍すると 3 ファイルすべてが含まれること、各ファイルが S3 上の実体と一致すること

ステップ4：
`t_file_download` の総件数がステップ0 から増加していないこと（ZIP 一括ダウンロードでも `t_file_download` への新規 INSERT は行わないこと）

ステップ5：
操作ログ `t_log`（`log_type = 4` / `operation = 'DOWNLOAD'`）が 1 件だけ記録されること（複数ファイルでも一括ダウンロードにつき 1 件）

補足：
・複数ファイル選択時は POST `/api/v1/file-download/download-zip` で ZIP 一括ダウンロードを行うこと
・`file_download_ids` は 1〜50 件・重複不可（詳細は ACSMS-TC-022-055〜058 参照）
・ZIP 一括ダウンロードでも `t_file_download` 未 INSERT、`t_log` は 1 件のみ記録すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-038 — ファイル未選択時のダウンロード実行 — ACSMS-MSG-022-002

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
全チェックボックスを未選択の状態にする

ステップ2：
ダウンロード実行ボタンを押下

### 期待結果

ステップ1：
全チェックボックスが未選択であること

ステップ2：
警告メッセージ `ファイルを選択してください。`（ACSMS-MSG-022-002）が表示されること、ダウンロード API が呼び出されないこと

補足：
・フロントエンド側で選択件数 0 を検知し、API リクエストを発行しないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-039 — DataScope 違反 ID の直接ダウンロード試行 — 404 存在隠蔽

- 観点ID: VP-A-04
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（自 JA の ja_id = 5）
  - ・ログイン済 + MFA認証済
  - ・テストデータ：他 JA（ja_id = 10）に紐づくファイル file_download_id = 999 が存在

### 手順

ステップ1：
ログインユーザに見えない他 JA のファイル ID（999）を DevTools 経由で GET `/api/v1/file-download/999/download` に渡して呼び出し

ステップ2：
DB で `SELECT * FROM t_log WHERE target_id = 999 AND account_id = :account_id AND log_type = 4 AND operation = 'DOWNLOAD' AND result_status = 1` を実行

ステップ3：
DB で `SELECT * FROM t_log WHERE target_id = 999 AND account_id = :account_id AND log_type = 3` を実行

### 期待結果

ステップ1：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`、メッセージ `指定されたファイルが見つかりません。`）— DataScope 違反は存在隠蔽として 404 を返す

ステップ2：
成功扱いのダウンロード操作ログ（`log_type = 4` / `operation = 'DOWNLOAD'` / `result_status = 1`）が記録されないこと（ダウンロードが実行されていないこと）

ステップ3：
エラーログが記録されること（`log_type = 3`、`result_status = 2`、URL 直接攻撃の追跡用）

補足：
・DataScope 違反は 403 ではなく 404 を返却（存在隠蔽 — api.md §4.2、`security.md assertJaScope` 規則）
・URL 直接アクセス攻撃も監査ログとして記録すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-040 — ダウンロード時のトランザクションロールバック

- 観点ID: VP-D-04
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：通常ダウンロード可能なファイル 1 件、ダウンロード操作ログ（`t_log`、`log_type = 4`）の INSERT が失敗するよう一時的に細工（テスト用）

### 手順

ステップ0（事前計測）：
DB で `SELECT COUNT(*) FROM t_log WHERE log_type = 4 AND operation = 'DOWNLOAD'` を実行し、テスト前の件数を記録

ステップ1：
ファイルを選択し、ダウンロード実行ボタンを押下

ステップ2：
DB で `SELECT COUNT(*) FROM t_log WHERE log_type = 4 AND operation = 'DOWNLOAD' AND result_status = 1` を再実行し、ステップ0 の件数と比較

ステップ3：
細工を解除し、テスト終了

### 期待結果

ステップ1：
HTTPステータスコード500が返却されること（`error_code: INTERNAL_SERVER_ERROR`、メッセージ `システムエラーが発生しました。しばらくしてから再度お試しください。`）、ファイルダウンロードが行われないこと

ステップ2：
成功扱いのダウンロード操作ログ（`log_type = 4` / `result_status = 1`）が増加していないこと（操作ログ INSERT がロールバックされ、トランザクションが正しく巻き戻ること）

ステップ3：
細工解除後は通常どおり動作すること

補足：
・ダウンロード実行では `t_file_download` への INSERT は行わず、記録されるのは操作ログ `t_log`（`log_type = 4`）1 件のみであること
・その 1 件の操作ログ INSERT が失敗した場合はトランザクションがロールバックされ、成功扱いの中途半端なログが残らないこと（api.md §4.8）
・例外処理中のエラーログ（`log_type = 3`）はトランザクション外で別途記録されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-041 — ダウンロード種別カラムの m_code ラベル表示（口座振替 / 増減連絡票 / 増減通知書 / 購読者名簿 / その他）

- 観点ID: VP-D-05
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：各帳票出力画面が `t_file_download` に登録した以下 5 ファイル（`download_type` は登録時に各画面が設定済み）
    - `kouza_furikae_202604.csv`（`download_type = 1`：口座振替）
    - `zougen_renraku_202604.csv`（`download_type = 3`：増減連絡票）
    - `zougen_tsuchi_202604.pdf`（`download_type = 4`：増減通知書）
    - `meibo_202604.pdf`（`download_type = 5`：購読者名簿）
    - `report_misc_202604.pdf`（`download_type = 2`：その他）

### 手順

ステップ1：
URL `/file-download` にアクセスし、上記 5 ファイルの「ダウンロード種別」カラムの表示を確認

ステップ2：
DB で `SELECT file_name, download_type FROM t_file_download WHERE file_name IN (...)` を実行し、画面表示のラベルと DB 値の対応を確認

### 期待結果

ステップ1：
「ダウンロード種別」カラムに m_code `DOWNLOAD_TYPE` のラベルが表示されること
・`kouza_furikae_202604.csv` → `口座振替`
・`zougen_renraku_202604.csv` → `増減連絡票`
・`zougen_tsuchi_202604.pdf` → `増減通知書`
・`meibo_202604.pdf` → `購読者名簿`
・`report_misc_202604.pdf` → `その他`

ステップ2：
DB 値と表示ラベルが対応していること（`1`→口座振替 / `2`→その他 / `3`→増減連絡票 / `4`→増減通知書 / `5`→購読者名簿）

補足：
・`download_type` は SCR-022 のダウンロード実行時に判定するものではなく、各帳票出力画面（SCR-020/021/026/028/029）が `t_file_download` 登録時に設定する値であること
・画面のラベルは m_code `DOWNLOAD_TYPE` を参照して表示すること（値そのものは表示しない）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-055 — 複数ファイル一括ダウンロード（ZIP）正常

- 観点ID: VP-D-05
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：各帳票出力画面が `t_file_download` に登録したダウンロード可能ファイル 5 件（S3 上に実体存在）

### 手順

ステップ0（事前計測）：
DB で `SELECT COUNT(*) FROM t_file_download` を実行し、テスト前の総件数を記録

ステップ1：
結果一覧から 5 件のチェックボックスをオンにし、ダウンロード実行ボタンを押下

ステップ2：
POST `/api/v1/file-download/download-zip`（body `{ "file_download_ids": [id1, id2, id3, id4, id5] }`）のレスポンスを確認

ステップ3：
ダウンロードされた ZIP を解凍して内容を確認

ステップ4：
DB で `SELECT COUNT(*) FROM t_file_download` を再実行し、ステップ0 と比較

ステップ5：
DB で `SELECT COUNT(*) FROM t_log WHERE log_type = 4 AND operation = 'DOWNLOAD' AND account_id = :account_id AND log_datetime >= :test_start_time` を実行

### 期待結果

ステップ1：
一括ダウンロード処理が開始されること

ステップ2：
HTTPステータスコード200が返却されること、`Content-Type: application/zip` の ZIP が返却されること、成功メッセージ `ダウンロードが完了しました。`（ACSMS-MSG-022-005）が表示されること

ステップ3：
ZIP を解凍すると選択した 5 ファイルすべてが含まれること

ステップ4：
`t_file_download` の総件数がステップ0 から増加していないこと（ZIP 一括ダウンロードでも新規 INSERT を行わないこと）

ステップ5：
操作ログ `t_log`（`log_type = 4` / `operation = 'DOWNLOAD'`）が 1 件だけ記録されること

補足：
・`file_download_ids` は 1〜50 件・重複不可
・ZIP 一括ダウンロードでも `t_file_download` 未 INSERT、`t_log` は 1 件のみ

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-056 — ZIP 一括ダウンロード — 0 件選択（ファイル未選択）

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
全チェックボックスを未選択の状態でダウンロード実行ボタンを押下

ステップ2：
（BE 直接検証）DevTools から POST `/api/v1/file-download/download-zip`（body `{ "file_download_ids": [] }`）を送信

### 期待結果

ステップ1：
警告メッセージ `ファイルを選択してください。`（ACSMS-MSG-022-002）が表示されること、ZIP ダウンロード API が呼び出されないこと（FE 側で選択件数 0 を検知）

ステップ2：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、メッセージ `ファイルを選択してください。`）

補足：
・`file_download_ids` は最低 1 件必須であること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-057 — ZIP 一括ダウンロード — 51 件選択（上限超過）

- 観点ID: VP-B-02
- 種類: Boundary (境界)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：ダウンロード可能ファイル 51 件以上が存在

### 手順

ステップ1：
（BE 直接検証）DevTools から POST `/api/v1/file-download/download-zip`（body `{ "file_download_ids": [51 件の一意な ID] }`）を送信

ステップ2：
POST `/api/v1/file-download/download-zip`（body `{ "file_download_ids": [50 件の一意な ID] }`）を送信（境界値：上限ちょうど）

### 期待結果

ステップ1：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、メッセージ `一括ダウンロードは最大50件までです。`）

ステップ2：
HTTPステータスコード200が返却されること、ZIP が正常に返却されること（50 件は許容範囲内）

補足：
・`file_download_ids` の上限は 50 件（1〜50 件）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-058 — ZIP 一括ダウンロード — 重複 ID 指定

- 観点ID: VP-B-03
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：ダウンロード可能ファイル 2 件以上が存在

### 手順

ステップ1：
（BE 直接検証）DevTools から POST `/api/v1/file-download/download-zip`（body `{ "file_download_ids": [id1, id1, id2] }`：`id1` が重複）を送信

### 期待結果

ステップ1：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、メッセージ `ファイルIDが重複しています。`）、ZIP が生成されないこと

補足：
・`file_download_ids` は重複不可であること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 8: 共通エラーハンドリング（Common Error Handling）

## ACSMS-TC-022-042 — UNAUTHORIZED — セッション切れ時の自動ログイン画面遷移

- 観点ID: VP-A-05
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
画面を表示後、Redis 上のセッションを管理者操作で削除（または有効期限 0 へ更新）してセッション切れを模擬

ステップ2：
画面で検索ボタンを押下

ステップ3：
DevTools で GET `/api/v1/file-download` のレスポンスを確認

### 期待結果

ステップ1：
セッションが無効化されたこと

ステップ2：
ログイン画面 `/login` へ遷移すること、URL クエリパラメータに `redirect=/file-download` が付与されること

ステップ3：
HTTPステータスコード401が返却されること（`error_code: UNAUTHORIZED`、メッセージ `セッションが切れました。再度ログインしてください。`）

補足：
・フロントエンド側 axios インターセプターで 401 を検知し、ログイン画面へリダイレクトすること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-043 — BAD_REQUEST — 不正リクエストパラメータの直接送信

- 観点ID: VP-D-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
DevTools から GET `/api/v1/file-download?page=abc` を送信（page は整数のはず）

ステップ2：
GET `/api/v1/file-download?per_page=-1` を送信

### 期待結果

ステップ1：
HTTPステータスコード400が返却されること（`error_code: BAD_REQUEST` または `VALIDATION_ERROR`、メッセージ `リクエストパラメータが不正です。` または同等の詳細メッセージ）

ステップ2：
HTTPステータスコード400が返却されること（最小 1 を下回るため）

補足：
・想定外の型・範囲のパラメータは サーバー側で正規にバリデーション拒否されること、想定外エラーが発生しないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-044 — VALIDATION_ERROR — 複数フィールドエラーの統合送信と errors 配列構造

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
DevTools から GET `/api/v1/file-download?todofuken_code=AB&per_page=999&sort_by=password` を送信（3 項目を同時に不正値で送信）

ステップ2：
レスポンス JSON の構造を確認

### 期待結果

ステップ1：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、メッセージ `入力値が不正です。詳細はerrorsフィールドを確認してください。`）

ステップ2：
レスポンス内の `errors` 配列に 3 件のエラーが含まれること、各要素が `{ field: string, message: string }` 形式であり、`todofuken_code` / `per_page` / `sort_by` がそれぞれ含まれること

補足：
・複数バリデーションエラーは 1 リクエストで統合送信され、フロントエンド側で項目ごとの inline 表示にマッピングされること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-045 — TOO_MANY_REQUESTS — レート制限超過

- 観点ID: VP-A-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・レート制限：60 秒あたり 100 リクエスト（運用設定値）

### 手順

ステップ1：
スクリプトで GET `/api/v1/file-download` を 60 秒以内に 101 回連続実行

ステップ2：
101 回目のレスポンスを確認

### 期待結果

ステップ1：
100 回目までは HTTPステータスコード200が返却されること

ステップ2：
HTTPステータスコード429が返却されること（`error_code: TOO_MANY_REQUESTS`、メッセージ `リクエスト回数が上限を超えました。しばらくしてから再度お試しください。`）

補足：
・レート制限値はインフラ設定により変動するため、許容しきい値は環境ごとに確認すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-046 — INTERNAL_SERVER_ERROR — DB 接続障害の模擬

- 観点ID: VP-D-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テスト環境にて DB 接続を一時的に切断可能

### 手順

ステップ1：
DB 接続を切断（または BE プロセスから `t_file_download` テーブルアクセスを拒否）

ステップ2：
画面で検索ボタンを押下

ステップ3：
DB 接続を復旧

### 期待結果

ステップ1：
DB 接続が無効化されていること

ステップ2：
HTTPステータスコード500が返却されること（`error_code: INTERNAL_SERVER_ERROR`、メッセージ `システムエラーが発生しました。しばらくしてから再度お試しください。`）、画面に ACSMS-MSG-022-004 と同等のメッセージがトースト表示されること

ステップ3：
復旧後の検索が正常に動作すること

補足：
・サーバー側エラー時にも `t_log`（log_type=3、エラーログ）が記録されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-047 — NOT_FOUND — 存在しない file_download_id の指定

- 観点ID: VP-D-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
DevTools から GET `/api/v1/file-download/9999999/preview` を送信（DB に存在しない ID）

ステップ2：
GET `/api/v1/file-download/9999999/download` を送信

### 期待結果

ステップ1：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`、メッセージ `指定されたファイルが見つかりません。`）

ステップ2：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`）、ダウンロード処理が実行されないこと

補足：
・DataScope 違反と 物理的不在は同一のレスポンス（404）を返却し、内部状態を漏らさないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-048 — NOT_FOUND — 論理削除済ファイルの直接アクセス（ACSMS-MSG-022-003）

- 観点ID: VP-C-03
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・テストデータ：直前まで存在していたファイル ID = 500 を `UPDATE t_file_download SET deleted_at = NOW() WHERE file_download_id = 500` で論理削除

### 手順

ステップ1：
画面リフレッシュ前のキャッシュ状態でファイル ID = 500 をチェックしダウンロード実行

ステップ2：
レスポンス確認後、フロントエンド側のトースト内容を確認

### 期待結果

ステップ1：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`、メッセージ `指定されたファイルが見つかりません。`）、ダウンロード処理が実行されないこと

ステップ2：
画面にメッセージ `ファイルが存在していません。`（ACSMS-MSG-022-003）が表示されること

補足：
・FE は 404 (NOT_FOUND) を ACSMS-MSG-022-003 にマッピングして表示すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-049 — ネットワーク切断時の FE エラーハンドリング

- 観点ID: VP-D-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
DevTools の Network タブで ネットワーク切断（Offline モード）に設定

ステップ2：
画面で検索ボタンを押下

ステップ3：
ネットワークを復旧し、再度検索ボタンを押下

### 期待結果

ステップ1：
ネットワーク切断状態となること

ステップ2：
フロントエンド側でネットワークエラーを検知し、エラートースト `システムエラーが発生しました。しばらくしてから再度お試しください。`（ACSMS-MSG-022-004 と同等）が表示されること、画面崩れが発生しないこと、想定外エラーが発生しないこと

ステップ3：
ネットワーク復旧後の検索が正常に動作すること、業務影響がないこと

補足：
・フロントエンド側 axios インターセプターでネットワークエラーを補足し、ユーザー向け汎用メッセージを表示すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

# カテゴリ 9: DataScope拡大・DL許可の例外（Scope / Download Gate）

## ACSMS-TC-022-059 — 中央会のDataScope拡大 — 同一都道府県の全JA（#52132）

- 観点ID: VP-A-02
- 種類: Normal (正常)
- 前提条件:
  - ・role: CHUOKAI（自JA=ja_id 5、`todofuken_code='13'`）
  - ・同一都道府県（13）に自JA以外のJAが 2 件存在し、それぞれファイルを出力済み
  - ・他県（例：14）のJAのファイルが 1 件存在する
  - ・同県他JAのファイルには `nichino_download_allowed_flg = true` のものと `false` のものを両方用意する
  - ・`todofuken_code` を**持たない**セッション（本機能デプロイ前に発行されたもの）も用意する

### 手順

ステップ1：
一覧を表示し、返却されるファイルの `ja_id` を確認する

ステップ2：
JA絞り込みプルダウンの候補を確認する

ステップ3：
都道府県プルダウンの状態を確認する

ステップ4：
同県他JAのファイル（`nichino_download_allowed_flg = true`）を個別ダウンロード・プレビュー・ZIP一括でそれぞれ取得する

ステップ5：
同県他JAのファイル（`nichino_download_allowed_flg = false`）を個別ダウンロードする

ステップ6：
他県JAのファイルの `file_download_id` を直接指定してダウンロードAPIを呼び出す

ステップ7：
`todofuken_code` を持たないセッションで一覧を表示する

ステップ8：
role が JA_HONTEN / JA_KANRI_SHITEN のアカウント（`todofuken_code` を持つ）で一覧を表示する

### 期待結果

ステップ1：
自JA + 同県他JA2件 + 全JA向け（`ja_id IS NULL`）のファイルが返ること。他県JAのファイルは含まれないこと

ステップ2：
自県の全JAが候補として表示されること（`GET /api/v1/ja/dropdown?scope=todofuken`）。他県のJAは候補に出ないこと

ステップ3：
都道府県が**自県で固定**されていること（他県は選択できない）。BE 側でも他県は弾かれるため、選ばせない方針

ステップ4：
いずれも成功すること。**一覧に出た行は個別ダウンロード・プレビュー・ZIPでも通る**こと（同じ ja_id 集合で判定する不変条件）

ステップ5：
HTTPステータスコード403（`FORBIDDEN`、メッセージ `このファイルは日農のダウンロードが許可されていません。`）が返ること。**一覧には見えるがDLはできない**（スコープ判定とDL可否判定は別）

ステップ6：
HTTPステータスコード404（`NOT_FOUND`）が返ること（スコープ外は存在隠蔽）

ステップ7：
**自JAのファイルのみ**が返ること（従来動作へフォールバック）。エラーにはならないこと

ステップ8：
自JAのファイルのみが返ること。`todofuken_code` を持っていても県内全JAへは広がらないこと

補足：
・拡大先の県は**クライアント指定ではなくセッションの `todofuken_code`** から決まるため、他県を覗くことはできない
・`todofuken_code` を optional 扱いにしているのは、本機能のデプロイ前に発行された Redis 上の既存セッションに当該項目が存在しないため。必須型にすると実行時の実態と食い違う
・判定を `role_code = CHUOKAI` に限定しているのは、JA_HONTEN / JA_KANRI_SHITEN も `todofuken_code` を持つため。コードの有無だけで分岐すると両ロールまで県内全JAへ広がる（ステップ8がその回帰確認）
・FE で JA絞り込みを自JA固定＋disable のままにすると検索条件が常に `ja_id = 自JA` となり、BE の拡大が一切効かない（ステップ2がその回帰確認）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-022-060 — 日農DL許可 — 対象ロールへの中央会追加と、自作成ファイルの例外（#52132）

- 観点ID: VP-A-03
- 種類: Abnormal (異常)
- 前提条件:
  - ・以下のファイルを用意すること
    - ・(a) `nichino_download_allowed_flg = false`・`created_by` = **他アカウント**
    - ・(b) `nichino_download_allowed_flg = false`・`created_by` = **ログイン中のアカウントの `account_id`**
    - ・(c) `nichino_download_allowed_flg = false`・`created_by` = **空文字**
    - ・(d) `nichino_download_allowed_flg = true`・`created_by` = 他アカウント
  - ・role: NICHINO_ADMIN / NICHINO_STAFF / CHUOKAI / JA_HONTEN / JA_KANRI_SHITEN の各アカウント

### 手順

ステップ1：
CHUOKAI で一覧を表示し、(a)〜(d) の行の無効化（チェックボックス・ファイル名リンク）状態を確認する

ステップ2：
CHUOKAI で (a) をダウンロード／プレビューする

ステップ3：
CHUOKAI で (b) をダウンロードする

ステップ4：
CHUOKAI で (c) をダウンロードする

ステップ5：
NICHINO_ADMIN / NICHINO_STAFF でも (a) が403、(b) が成功することを確認する

ステップ6：
JA_HONTEN / JA_KANRI_SHITEN で (a) をダウンロードする

ステップ7：
(a) を含む複数ファイルを選択してZIP一括ダウンロードする

### 期待結果

ステップ1：
(a) と (c) が無効化されること。**(b) は無効化されないこと**（自分が出力したファイル）。(d) は無効化されないこと

ステップ2：
HTTPステータスコード403（`FORBIDDEN`、メッセージ `このファイルは日農のダウンロードが許可されていません。`）が返ること。中央会が対象ロールに追加されている（顧客要件2026-07。従来は日農のみ）

ステップ3：
**ダウンロードが成功すること**。本フラグは「他組織へ自組織のファイルを見せてよいか」を JA 側が決めるもので、出力した本人まで締め出す意図は無い

ステップ4：
403が返ること（`created_by` が空文字の行は**本人扱いしない**＝安全側に倒す）

ステップ5：
例外はDL制限3ロール共通であること（中央会だけの特例ではない）

ステップ6：
ダウンロードが成功すること（JA系ロールは本フラグの影響を受けない）

ステップ7：
403となり、ZIPが生成されないこと（ZIPも個別DLと同じチェックを通る）

補足：
・既定値が FALSE のため、この例外が無いと中央会が自分で出力した帳票をその場でダウンロードできなかった（#52132 の発端）
・本人判定は `t_file_download.created_by`（`m_account.account_id` を varchar で保持）で行い、両辺を trim 済み文字列に揃えて突合する。一覧SQLの `m_account.account_id::text = fd.created_by` と同じ前提
・行は一覧に表示されるため、存在を隠す404ではなく403を返す

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---
