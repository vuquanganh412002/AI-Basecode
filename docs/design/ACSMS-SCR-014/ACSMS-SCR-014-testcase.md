---
customer_name: 日本農業新聞様
system_name: クラウド版購読者管理システム
document_name: テスト仕様書
screen_id: ACSMS-SCR-014
screen_name: 購読者明細検索画面
format_code: 16-BM/PM/VTI
format_version: "1.0"
issue_date: 2026-06-03
test_level: 結合テスト
test_environment: Windows 10/11, Chrome, Edge
author: Kieu Thi Diem
reviewer: Nguyen Huy Dat
---


## 変更履歴

| No. | 発行日 | 版数 | 担当者 | 変更内容 | 確認者 | 承認者 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-06-03 | 1.0 | Kieu Thi Diem | 新規作成 | Nguyen Huy Dat |  |


## システム概要

本システムは、JA向けのクラウド型読者管理システムであり、
購読者情報の管理、購読履歴の管理、口座振替データの管理などの機能を提供する。
主な機能として、購読者情報の登録・更新・検索、
購読内容の変更履歴管理、口座振替データの作成および管理、
ファイルのアップロード・ダウンロード機能、システムのお知らせ管理などを提供する。
また、ユーザーのログイン管理、ログイン履歴の記録、
ユーザー操作ログの記録などのセキュリティ・監査機能をサポートする。

## 資料目的

「購読者明細検索画面（ACSMS-SCR-014）」において、システム上で新規作成されるテスト仕様書の詳細を記述した資料です。
本資料はISTQBおよびIEEE 829を参考とし、以下の品質基準を満たすものとする。

- 各テストケースは単一のシナリオに基づき作成する（単一責任）。
- 手順は再現可能な粒度で記述し、テストデータを明示する。
- 期待結果は測定可能とする（メッセージ内容、DBクエリ結果、HTTPステータスコード等）。

## 関連資料

| No | 資料コード | 資料名 |
| --- | --- | --- |
| 1 | ACSMS-SCR-014 | 購読者明細検索画面設計書 |
| 2 | ACSMS-SCR-014-api | 購読者明細検索 API設計書 |
| 3 | testcase-viewpoints | 全システム共通 テスト観点一覧 |


## テストカテゴリ一覧

| # | カテゴリ | テストケース数 |
| --- | --- | --- |
| 1 | アクセス権限制御（Access Control） | 5 |
| 2 | 画面表示・レスポンシブ（Layout & Responsive） | 7 |
| 3 | 入力チェック（Input Validation） | 8 |
| 4 | 業務ロジック — 検索・削除・Excel出力（Function — Search / Delete / Export） | 16 |
| 5 | 共通エラーハンドリング（Common Error Handling） | 7 |
| 6 | 購読中止・総部数表示・削除制限（Function — Stop / Total-busu / Delete） | 7 |
|  | 合計 | 50 |

---

# カテゴリ 1: アクセス権限制御（Access Control）

## ACSMS-TC-014-001 — NICHINO_ADMIN による購読者明細検索画面アクセス禁止

- 観点ID: VP-A-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
ダッシュボードを開き、サイドバー「購読者管理」セクションで「購読者明細検索画面」項目を確認

ステップ2：
ブラウザのアドレスバーに `/dokusya` を直接入力し、直接アクセス

ステップ3：
DevTools で GET `/api/v1/dokusya?page=1&per_page=20` を直接呼び出し

ステップ4：
DB 確認: `SELECT * FROM t_log WHERE result_status = 2 AND target_table = 't_dokusya' ORDER BY log_datetime DESC LIMIT 1`

### 期待結果

ステップ1：
サイドバーに「購読者明細検索画面」項目が表示されないこと（NICHINO_ADMIN は `dokusya.view` 権限を持たない）

ステップ2：
トースト `アクセス権がありません。` が表示されること、かつ `/dashboard` へ遷移すること

ステップ3：
HTTPステータスコード403が返却されること（`error_code: FORBIDDEN`、メッセージ `この画面へのアクセス権限がありません。`）

ステップ4：
・エラーログが1件以上記録されること
・`account_id` がテストアカウントと一致すること

補足：
・`account_concept.md` 権限マトリクスにより、購読者管理機能は日農（管理者・担当者）は対象外（×）
・FEメニュー、FEルーターガード、BE APIガードの3層すべてでブロックされること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

クリティカル — 失敗時はRBACバイパスを意味し、セキュリティインシデントとして扱う。

## ACSMS-TC-014-002 — NICHINO_STAFF による購読者明細検索画面アクセス禁止

- 観点ID: VP-A-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_STAFF
  - ・ログイン済 + MFA認証済

### 手順

ステップ1：
サイドバーで「購読者明細検索画面」項目を確認

ステップ2：
URL `/dokusya` に直接アクセス

ステップ3：
DevTools で GET `/api/v1/dokusya?page=1&per_page=20` を直接呼び出し

### 期待結果

ステップ1：
サイドバーに「購読者明細検索画面」項目が表示されないこと（NICHINO_STAFF は `dokusya.view` 権限を持たない）

ステップ2：
トースト `アクセス権がありません。` が表示されること、かつ `/dashboard` へ遷移すること

ステップ3：
HTTPステータスコード403が返却されること（`error_code: FORBIDDEN`、メッセージ `この画面へのアクセス権限がありません。`）

補足：
・`account_concept.md` 権限マトリクスにより、購読者管理機能は日農（担当者）は対象外（×）
・日農（担当者）は販売店代行入力のみ可能であり、購読者の参照・削除・出力は不可

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-003 — CHUOKAI による購読者明細検索画面アクセス（自中央会管轄JAのみ閲覧 + 削除可）

- 観点ID: VP-A-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: CHUOKAI（chuokai-001 所属）
  - ・事前データ：自中央会管轄JA（ja-001）の購読者、他中央会管轄JA（ja-101）の購読者が登録済

### 手順

ステップ1：
サイドバーで「購読者明細検索画面」項目を確認

ステップ2：
「購読者明細検索画面」を押下 → 一覧画面の表示内容を確認

ステップ3：
一覧テーブルの「操作」列を確認

ステップ4：
画面右上の「購読者情報登録」ボタンおよび「Excel出力」ボタンの状態を確認

ステップ5：
DevTools で GET `/api/v1/dokusya?page=1&per_page=20` を直接呼び出し、レスポンスを確認

### 期待結果

ステップ1：
サイドバーに「購読者明細検索画面」項目が表示されること（権限 `dokusya.view` を保有）

ステップ2：
購読者明細検索画面が正常に表示されること。一覧には自中央会管轄JA（ja-001）の購読者のみ表示され、ja-101 の購読者は表示されないこと

ステップ3：
「削除」リンクが活性状態で表示されること（権限 `dokusya.delete` を保有）。ただし `is_read_only=true`（電子版クレジットカード決済者・併読者）の行は非活性であること

ステップ4：
「購読者情報登録」ボタンが活性状態で表示されること（権限 `dokusya.create` を保有）。「Excel出力」ボタンが活性状態で表示されること（権限 `dokusya.view` を保有）

ステップ5：
HTTPステータスコード200が返却されること（`data` 配列に自中央会管轄JAの購読者のみ含まれる、`meta.total` がDataScope適用後の件数と一致）

補足：
・CHUOKAI は `dokusya.view` / `dokusya.create` / `dokusya.delete` を保有
・DataScope により自中央会管轄JA（`d.ja_id IN (SELECT ja_id FROM m_ja WHERE chuokai_id = :user_chuokai_id)`）の購読者のみ閲覧可
・FEメニュー、FEルーターガード、BE APIガードの3層すべてで許可されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-004 — JA_HONTEN による購読者明細検索画面アクセス（自JAのみ閲覧 + 削除可）

- 観点ID: VP-A-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja-001 所属、自JA = ja-001）
  - ・事前データ：自JA（ja-001）の購読者、他JA（ja-002）の購読者が登録済

### 手順

ステップ1：
サイドバーで「購読者明細検索画面」項目を確認

ステップ2：
「購読者明細検索画面」を押下 → 一覧画面の表示内容を確認

ステップ3：
「操作」列の「削除」リンク、「購読者情報登録」ボタン、「Excel出力」ボタンの状態を確認

ステップ4：
DevTools で GET `/api/v1/dokusya?page=1&per_page=20` を直接呼び出し

### 期待結果

ステップ1：
サイドバーに「購読者明細検索画面」項目が表示されること（権限 `dokusya.view` を保有）

ステップ2：
自JA（ja-001）の購読者のみ表示され、他JA（ja-002）の購読者は表示されないこと

ステップ3：
「削除」リンク（`is_read_only=false` の行）、「購読者情報登録」ボタン、「Excel出力」ボタンがすべて活性状態で表示されること（権限 `dokusya.view` / `dokusya.create` / `dokusya.delete` を保有）

ステップ4：
HTTPステータスコード200が返却されること（`data` 配列に自JAの購読者のみ含まれる）

補足：
・JA_HONTEN は `dokusya.view` / `dokusya.create` / `dokusya.delete` を保有
・DataScope により自JA（`d.ja_id = :user_ja_id`）の購読者のみ閲覧可

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-005 — JA_KANRI_SHITEN による購読者明細検索画面アクセス（自管理支店のみ閲覧 + 削除可）

- 観点ID: VP-A-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_KANRI_SHITEN（ja-001 / 管理支店 kanri_shiten-001 所属）
  - ・事前データ：自管理支店（kanri_shiten-001）の購読者、自JA他管理支店（kanri_shiten-002）の購読者が登録済

### 手順

ステップ1：
サイドバーで「購読者明細検索画面」項目を確認

ステップ2：
「購読者明細検索画面」を押下 → 一覧画面の表示内容を確認

ステップ3：
DevTools で GET `/api/v1/dokusya?page=1&per_page=20` を直接呼び出し

ステップ4：
他管理支店（kanri_shiten-002）の購読者IDを指定し、DevTools で DELETE `/api/v1/dokusya/{他管理支店購読者id}` を直接呼び出し

### 期待結果

ステップ1：
サイドバーに「購読者明細検索画面」項目が表示されること（権限 `dokusya.view` を保有）

ステップ2：
自管理支店（kanri_shiten-001）の購読者のみ表示され、他管理支店（kanri_shiten-002）の購読者は表示されないこと

ステップ3：
HTTPステータスコード200が返却されること（`data` 配列に自管理支店の購読者のみ含まれる）

ステップ4：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`、メッセージ `指定された購読者が見つかりません。`（DataScope により存在隠蔽））

補足：
・JA_KANRI_SHITEN は `dokusya.view` / `dokusya.create` / `dokusya.delete` を保有
・DataScope により自管理支店（`d.ja_id = :user_ja_id AND d.kanri_shiten_id = :user_kanri_shiten_id`）の購読者のみ閲覧可
・出スコープレコードへの削除操作は存在隠蔽のため HTTP 404 として返却されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 2: 画面表示・レスポンシブ（Layout & Responsive）

## ACSMS-TC-014-006 — 検索画面 全体レイアウトが設計仕様に一致

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・ブラウザ: Chrome (latest)、解像度 1920×1080

### 手順

ステップ1：
サイドバーから「購読者明細検索画面」を押下し、`/dokusya` 画面を開く

ステップ2：
設計仕様書（screen-design.md / index.html）と画面を並べて比較

ステップ3：
各セクションの位置を確認（ヘッダー、パンくず、検索フォームカード、一覧テーブルカード、ページネーション）

### 期待結果

ステップ1：
購読者明細検索画面が表示されること

ステップ2：
背景色、フォント、フォントサイズ、余白、ボタン色、入力欄スタイルがすべて一致すること

ステップ3：
設計仕様通り、ヘッダー上部 → パンくず → 検索フォームカード → 一覧テーブルカード（ヘッダー＋データ＋ページネーション）の縦配置であること

補足：
・設計仕様との視覚差異がないこと
・design tokens（`design-tokens.ts`）が適用されていること（ハードコードされた色がないこと）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-007 — 検索フォーム（常時表示エリア）要素表示が設計仕様通り

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・検索画面を開いた状態

### 手順

ステップ1：
常時表示エリアの入力項目を確認（管理支店、支店、組合員コード、氏名、かな氏名、配達先住所、配達販売店、手続種類、購読開始日、購読中止日、購読種別、電子版承認ステータス）

ステップ2：
プルダウン項目（管理支店 / 支店 / 配達販売店）の選択肢、ラジオボタン項目（手続種類 / 購読種別 / 電子版承認ステータス）の選択肢を確認

ステップ3：
日付範囲項目（購読開始日 / 購読中止日）の開始・終了の2入力欄を確認

### 期待結果

ステップ1：
各入力項目が設計仕様通り表示されること。プレースホルダーおよびラベルが画面項目定義シート通りであること

ステップ2：
・手続種類のラジオは「解約」「新規」が表示されること
・購読種別のラジオは「紙版」「電子版」「併読」が表示されること
・電子版承認ステータスのラジオは「Web申込以外」「未承認」「承認済み」「否認」が表示されること
・プルダウンの選択肢は `m_code` および共用ドロップダウンAPIから取得され、DataScopeが反映されること

ステップ3：
購読開始日・購読中止日それぞれに「開始日」「終了日」の2入力欄が「-」区切りで横並びに表示されること

補足：
・全フォーム要素が設計仕様と一致すること
・各検索項目はすべて任意項目（必須マーカー `*` なし）であること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-008 — 詳細検索エリアの展開・折り畳み

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・検索画面を開いた状態

### 手順

ステップ1：
初期表示時の詳細検索エリアの状態を確認

ステップ2：
「詳細検索を表示」ボタンを押下

ステップ3：
展開された詳細検索エリアの項目を確認（引落元口座支店、連絡先、メールアドレス、請求開始月、適用日、支払方法、郵送区分、新聞単価、備考）

ステップ4：
「詳細検索を非表示」ボタンを押下

### 期待結果

ステップ1：
詳細検索エリアが折り畳まれた状態であること（初期状態は非表示）

ステップ2：
詳細検索エリアが展開されること、ボタン表記が「詳細検索を非表示」に変化すること

ステップ3：
引落元口座支店、連絡先、メールアドレス、請求開始月（開始・終了の2入力欄・YYYYMM月選択）、適用日（開始・終了の2入力欄）、支払方法（ラジオ7択：口座引落・現金集金・振込集金・JA施設等・給与天引き・クレジットカード・その他）、郵送区分（プルダウン：0:空 / 1:郵送）、新聞単価（プルダウン）、備考（テキスト・部分一致）が表示されること

ステップ4：
詳細検索エリアが再び折り畳まれること、ボタン表記が「詳細検索を表示」に戻ること

補足：
・詳細検索の展開・折り畳みは画面状態のみの変更であり、API呼び出しを伴わないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-009 — 検索結果テーブル列表示が設計仕様通り

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・検索画面を開いた状態
  - ・複数の購読者レコードが登録済

### 手順

ステップ1：
一覧テーブルのヘッダー行を確認

ステップ2：
データ行の各セル表示を確認

ステップ3：
カードヘッダー右上の「購読者情報登録」ボタンを確認

### 期待結果

ステップ1：
列見出しは左から「管理支店」「支店」「組合員コード」「購読者名」「連絡先1」「連絡先2」「配達先郵便」「配達先住所」「販売店コード」「販売店名」「購読開始日」「購読中止日」「操作」の順に表示されること。「管理支店」「支店」「組合員コード」「販売店コード」「購読開始日」「購読中止日」列にはソートアイコンが表示されること

ステップ2：
DBの各カラム値が対応セルに表示されること。「購読者名」セルは主色のリンクとして表示され、クリックで購読者情報登録画面（編集モード）へ遷移可能であること。「操作」セルには「削除」リンク（赤色）が表示されること

ステップ3：
カードヘッダー左に「購読者一覧」タイトル、右に「購読者情報登録」ボタンが表示されること

補足：
・全列が設計仕様の検索結果テーブル（項目#24-#37）と一致すること
・ソートアイコンは初期状態では中立表示、ソート適用時のみ ▲（昇順）／▼（降順）表示

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-010 — 読み取り専用購読者（電子版クレジットカード決済者・併読者）の操作列表示

- 観点ID: VP-C-05
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：電子版クレジットカード決済者（`dokusya_shubetsu=2 AND shiharai_hoho=6`）1件、併読者（`dokusya_shubetsu=3`）1件、紙版（`dokusya_shubetsu=1`）1件が登録済

### 手順

ステップ1：
一覧テーブルで電子版クレジットカード決済者の行の「削除」リンク状態を確認

ステップ2：
併読者の行の「削除」リンク状態を確認

ステップ3：
紙版購読者の行の「削除」リンク状態を確認

### 期待結果

ステップ1：
電子版クレジットカード決済者の行は「削除」リンクが非活性状態で表示されること（`is_read_only=true`）

ステップ2：
併読者の行は「削除」リンクが非活性状態で表示されること（`is_read_only=true`）

ステップ3：
紙版購読者の行は「削除」リンクが活性状態で表示されること（`is_read_only=false`）

補足：
・`is_read_only` フラグはバックエンド側で算出され、レスポンスの各行に含まれること
・観点 VP-C-05「購読者種別ルール（読み取り専用）」準拠

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-011 — レスポンシブ動作 — PC／タブレット／モバイル ブレークポイント

- 観点ID: VP-E-02
- 種類: Normal (正常)
- 前提条件:
  - ・検索画面を開いた状態

### 手順

ステップ1：
ウィンドウサイズを 1920×1080 (PC) に設定

ステップ2：
768×1024 (Tablet) に変更

ステップ3：
375×667 (Mobile) に変更

### 期待結果

ステップ1：
サイドバー固定表示、検索フォーム横並び、一覧テーブル全列が画面幅内に収まること

ステップ2：
サイドバー折り畳み、検索フォーム配置維持、テーブルは横スクロール可能であること

ステップ3：
サイドバーはオーバーレイ表示、検索フォームは縦スタック、テーブルは横スクロール可能であること

補足：
・全3ブレークポイントでレイアウト崩れが発生しないこと
・テーブルは `overflow-x-auto` により横スクロールで全列を閲覧可能であること
・主要操作（検索、削除、Excel出力、ページ移動）はすべてのブレークポイントで実行可能であること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-012 — パンくず・ヘッダー表示およびキーボード操作

- 観点ID: VP-E-03
- 種類: Normal (正常)
- 前提条件:
  - ・検索画面を開いた状態

### 手順

ステップ1：
画面上部のヘッダータイトルおよびパンくず領域を確認

ステップ2：
パンくずの「ホーム」を押下

ステップ3：
検索フォーム先頭の入力欄にフォーカスを当て、Tab キーで各項目を順に移動

ステップ4：
ページネーション・検索結果テーブルのフォーカス遷移を確認

### 期待結果

ステップ1：
ヘッダータイトル「購読者明細検索画面」が表示されること。パンくずは `ホーム > 購読者管理 > 購読者明細検索画面` の階層で表示され、現在画面（購読者明細検索画面）はリンクなしであること

ステップ2：
`/dashboard` へ遷移すること

ステップ3：
Tab 順序が自然なフロー（上から下、左から右）であること。フォーカスされた要素に枠線（outline）が表示されること

ステップ4：
ページネーション・テーブルの操作要素がキーボードで到達可能であること

補足：
・パンくずの中間「購読者管理」はサイドバーグループ名であり、本プロジェクトのパンくず規約により遷移リンクとしては扱わないこと
・観点 VP-E-03「キーボード操作」準拠

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 3: 入力チェック（Input Validation）

## ACSMS-TC-014-013 — メールアドレス形式チェック

- 観点ID: VP-B-03
- 種類: Abnormal (異常)
- 前提条件:
  - ・検索画面を開き、詳細検索エリアを展開した状態

### 手順

ステップ1：
メールアドレス入力欄に `test@example.com` を入力し、「検索」を押下

ステップ2：
メールアドレス入力欄に `invalid-email` を入力し、「検索」を押下

ステップ3：
DevTools でステップ2のレスポンスボディを確認

### 期待結果

ステップ1：
HTTPステータスコード200が返却され、正常に検索が実行されること

ステップ2：
形式エラーが表示されること（該当項目下にメッセージ `正しいメール形式を入力してください。`）

ステップ3：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、`errors[]` 配列に `{ field: 'email', message: '正しいメール形式を入力してください。' }` が含まれる）

補足：
・ACSMS-MSG-014-008 該当
・メールアドレスは部分一致検索だが、形式チェックを通過した値のみ検索条件として受け付けること
・HTML5 `type="email"` のブラウザ標準バリデーションに依存せず、プロジェクトの正規表現チェックと日本語メッセージで判定すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-014 — 購読開始日 範囲の相関チェック（from ≦ to）

- 観点ID: VP-B-05
- 種類: Abnormal (異常)
- 前提条件:
  - ・検索画面を開いた状態

### 手順

ステップ1：
購読開始日の開始日に `2024/12/31`、終了日に `2024/01/01` を入力し、「検索」を押下（from > to）

ステップ2：
購読開始日の開始日に `2024/01/01`、終了日に `2024/12/31` を入力し、「検索」を押下（from ≦ to）

ステップ3：
DevTools でステップ1のレスポンスボディを確認

### 期待結果

ステップ1：
形式エラーが表示されること（開始日が終了日より後のため、相関チェックエラー）

ステップ2：
HTTPステータスコード200が返却され、正常に検索が実行されること

ステップ3：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、`errors[]` 配列に `field: shoki_dokusya_kaishi_date_to`（または `shoki_dokusya_kaishi_date_from`）の項目が含まれる）

補足：
・API設計書「処理手順 4.1」の相関チェック（`shoki_dokusya_kaishi_date_from ≦ shoki_dokusya_kaishi_date_to`）準拠
・日付は `YYYY/MM/DD` 形式
・観点 VP-B-05「日付ロジック」準拠

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-015 — 購読中止日・適用日 範囲の相関チェック（from ≦ to）

- 観点ID: VP-B-05
- 種類: Abnormal (異常)
- 前提条件:
  - ・検索画面を開き、詳細検索エリアを展開した状態

### 手順

ステップ1：
購読中止日の開始日に `2025/06/30`、終了日に `2025/01/01` を入力し、「検索」を押下（from > to）

ステップ2：
適用日の開始日に `2025/12/31`、終了日に `2025/01/01` を入力し、「検索」を押下（from > to）

ステップ3：
DevTools で各レスポンスボディを確認

### 期待結果

ステップ1：
形式エラーが表示されること（相関チェックエラー）

ステップ2：
形式エラーが表示されること（相関チェックエラー）

ステップ3：
両ケースともHTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、`errors[]` 配列に `field: dokusya_chushi_date_to` および `field: joho_henko_tekiyo_date_to`（または対応する `_from`）の項目が含まれる）

補足：
・購読中止日（`dokusya_chushi_date_from ≦ dokusya_chushi_date_to`）、適用日（`joho_henko_tekiyo_date_from ≦ joho_henko_tekiyo_date_to`）それぞれに相関チェックが適用されること
・適用日が両方空欄の場合は最新データフラグ（`saishin_data_flg=TRUE`）のレコードを抽出すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-016 — 検索項目 最大長境界（組合員コード20文字、氏名100文字）

- 観点ID: VP-B-02
- 種類: Boundary (境界)
- 前提条件:
  - ・検索画面を開いた状態

### 手順

ステップ1：
組合員コードに 20 文字を入力し、「検索」を押下

ステップ2：
組合員コードに 21 文字を入力し、「検索」を押下

ステップ3：
氏名に 100 文字を入力し、「検索」を押下

ステップ4：
氏名に 101 文字を入力し、「検索」を押下

### 期待結果

ステップ1：
HTTPステータスコード200が返却され、正常に検索が実行されること

ステップ2：
桁数超過エラーが表示されること（HTTPステータスコード400、`error_code: VALIDATION_ERROR`、`errors[]` 配列に `field: kumiaiin_code` の項目が含まれる）

ステップ3：
HTTPステータスコード200が返却され、正常に検索が実行されること

ステップ4：
桁数超過エラーが表示されること（HTTPステータスコード400、`error_code: VALIDATION_ERROR`、`errors[]` 配列に `field: full_name` の項目が含まれる）

補足：
・組合員コード 最大 20 文字、氏名 最大 100 文字（API設計書 リクエストパラメータ準拠）
・最大値で正常受付、最大値+1 で拒否（同値分割／境界値分析）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-017 — 詳細検索項目 最大長境界（引落元口座支店100文字、請求開始月（開始）YYYYMM 6桁、連絡先 15文字）

- 観点ID: VP-B-02
- 種類: Boundary (境界)
- 前提条件:
  - ・検索画面を開き、詳細検索エリアを展開した状態

### 手順

ステップ1：
引落元口座支店に 100 文字を入力し、「検索」を押下、続けて 101 文字を入力し、「検索」を押下

ステップ2：
請求開始月（開始）に 6 桁（YYYYMM）を入力し、「検索」を押下、続けて 7 桁を入力し、「検索」を押下

ステップ3：
連絡先に 15 文字を入力し、「検索」を押下、続けて 16 文字を入力し、「検索」を押下

### 期待結果

ステップ1：
100 文字は HTTPステータスコード200で正常に検索されること、101 文字は桁数超過エラーが表示されること（`error_code: VALIDATION_ERROR`、`errors[]` に `field: bank_branch`）

ステップ2：
6 桁（YYYYMM）は HTTPステータスコード200で正常に検索されること、7 桁は形式エラー（`請求開始月はYYYYMMの形式で指定してください。`）が表示されること（`errors[]` に `field: seikyu_kaishi_month_from`）

ステップ3：
15 文字は HTTPステータスコード200で正常に検索されること、16 文字は桁数超過エラーが表示されること（`errors[]` に `field: renrakusaki`）

補足：
・引落元口座支店は物理カラム `bank_branch_code` / `bank_branch_name`（最大100文字）に対する OR 部分一致
・各最大長はAPI設計書 リクエストパラメータ準拠

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-018 — ページサイズ境界値（per_page 1 / 100 / 101）

- 観点ID: VP-B-02
- 種類: Boundary (境界)
- 前提条件:
  - ・role: JA_HONTEN
  - ・DevTools の Console を開いている

### 手順

ステップ1：
DevTools の Console から `fetch('/api/v1/dokusya?page=1&per_page=1', { credentials: 'include' })` を実行

ステップ2：
`fetch('/api/v1/dokusya?page=1&per_page=100', { credentials: 'include' })` を実行

ステップ3：
`fetch('/api/v1/dokusya?page=1&per_page=101', { credentials: 'include' })` を実行

ステップ4：
`fetch('/api/v1/dokusya?page=1&per_page=0', { credentials: 'include' })` を実行

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること（`meta.per_page=1`）

ステップ2：
HTTPステータスコード200が返却されること（`meta.per_page=100`）

ステップ3：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR` または `BAD_REQUEST`、`per_page` 上限 100 超過）

ステップ4：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR` または `BAD_REQUEST`、`per_page` 最小 1 未満）

補足：
・per_page は 1〜100 の範囲（API設計書「処理手順 4.1」準拠）
・page は 1 以上

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-019 — 検索項目 前後空白除去・部分一致

- 観点ID: VP-B-09
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：`kumiaiin_code='K000001'`、`kumiaiin_code='K000010'`、`kumiaiin_code='M000001'` の購読者が登録済

### 手順

ステップ1：
組合員コードに前後空白を付与した `  K0000  ` を入力し、「検索」を押下

ステップ2：
DevTools で送信されたリクエストパラメータを確認

ステップ3：
一覧テーブルの結果を確認

### 期待結果

ステップ1：
入力値が前後空白除去され、検索が実行されること（入力欄の前後空白が除去された表示に更新されること）

ステップ2：
クエリパラメータに `kumiaiin_code=K0000`（前後空白なし）が含まれること

ステップ3：
`K0000` を含む2件（`K000001`、`K000010`）のみ表示されること。`M000001` は表示されないこと

補足：
・部分一致検索（ILIKE '%K0000%'）
・大文字小文字を区別しないこと
・前後空白は検索前に除去されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-020 — 検索項目 XSS／SQLインジェクション安全性

- 観点ID: VP-A-10
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：複数の購読者レコードが登録済

### 手順

ステップ1：
氏名に `<script>alert('XSS')</script>` を入力し、「検索」を押下

ステップ2：
組合員コードに `' OR '1'='1` を入力し、「検索」を押下

ステップ3：
氏名に `'; DROP TABLE t_dokusya; --` を入力し、「検索」を押下

ステップ4：
DB 確認: `SELECT COUNT(*) FROM t_dokusya WHERE deleted_at IS NULL`

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること（`data: []`（該当レコードなし））。JavaScriptアラートは表示されず、入力欄にはエスケープされた文字列がテキストとして安全に描画されること

ステップ2：
HTTPステータスコード200が返却されること（`data: []` — インジェクションペイロードはリテラル文字列として ILIKE 句に渡される）

ステップ3：
HTTPステータスコード200が返却されること（`data: []` — テーブル削除は発生しない）

ステップ4：
購読者レコード件数はテスト前と同じであること（テーブルは削除されていない）

補足：
・Vue の `{{ }}` 補間による自動エスケープにより XSS 攻撃は無効化されること（観点 VP-A-06）
・TypeORM のパラメータ化クエリ（`:kumiaiin_code` バインド変数）により SQL インジェクションは無効化されること（観点 VP-A-10）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 4: 業務ロジック — 検索・削除・Excel出力（Function — Search / Delete / Export）

## ACSMS-TC-014-021 — 初期表示 — 空白の検索フォーム + デフォルトの購読者一覧（最新順）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：自JAの購読者レコードが複数件登録済

### 手順

ステップ1：
ダッシュボードから「購読者明細検索画面」を押下

ステップ2：
画面の検索フォーム状態を確認

ステップ3：
一覧テーブルの内容を確認

ステップ4：
DevTools の Network タブで GET `/api/v1/dokusya` のクエリパラメータを確認

### 期待結果

ステップ1：
URL `/dokusya` に遷移すること

ステップ2：
全検索項目が空白で表示されること、詳細検索エリアは折り畳まれていること

ステップ3：
DataScope適用後の購読者レコードが最新順（`updated_at` 降順）で 20 件まで表示されること。論理削除済（`deleted_at IS NOT NULL`）のレコードは表示されないこと

ステップ4：
クエリパラメータは `?page=1&per_page=20&sort_by=updated_at&sort_order=desc`（既定値）であること

補足：
・初期表示時は検索条件なしの全件取得（DataScope適用後）
・per_page 既定値は 20、sort_by 既定値は `updated_at` 降順
・適用日が両方空欄のため、最新データフラグ（`saishin_data_flg=TRUE`）のレコードを抽出

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-022 — 検索 — 組合員コード部分一致

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：`kumiaiin_code='K000001'`、`kumiaiin_code='K000002'`、`kumiaiin_code='M000099'` の3件登録済

### 手順

ステップ1：
組合員コード入力欄に `K0000` を入力

ステップ2：
「検索」ボタンを押下

ステップ3：
DevTools で送信されたリクエストパラメータを確認

ステップ4：
一覧テーブルの結果を確認

### 期待結果

ステップ1：
入力受付が完了すること

ステップ2：
HTTPステータスコード200が返却されること（検索条件が適用され画面が更新される）

ステップ3：
クエリパラメータに `kumiaiin_code=K0000` が含まれること

ステップ4：
`K0000` を含む2件（`K000001`、`K000002`）のみ表示されること。`M000099` は表示されないこと

補足：
・部分一致検索（`d.kumiaiin_code ILIKE '%K0000%'`）
・大文字小文字を区別しないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-023 — 検索 — 氏名・かな氏名 部分一致（姓名連結）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：`shimei_sei='山田', shimei_mei='太郎'`、`shimei_sei='山田', shimei_mei='花子'`、`shimei_sei='佐藤', shimei_mei='次郎'` の3件登録済

### 手順

ステップ1：
氏名入力欄に `山田` を入力し、「検索」を押下

ステップ2：
一覧テーブルの結果を確認

ステップ3：
氏名をクリアし、かな氏名入力欄に `ヤマダ` を入力し、「検索」を押下

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること（検索条件が適用される）

ステップ2：
`山田` を含む2件（`山田 太郎`、`山田 花子`）のみ表示されること。`佐藤 次郎` は表示されないこと

ステップ3：
かな氏名による部分一致検索が実行され、対応する購読者が表示されること

補足：
・氏名は `(d.shimei_sei || ' ' || d.shimei_mei)` の連結文字列に対する部分一致
・かな氏名は `(d.shimei_kana_sei || ' ' || d.shimei_kana_mei)` の連結文字列に対する部分一致
・全角・ひらがな・カタカナはそのままの形で照合されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-024 — 検索 — プルダウン・ラジオ条件のAND組み合わせ

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：管理支店・支店・購読種別・支払方法の異なる複数の購読者が登録済

### 手順

ステップ1：
管理支店プルダウンで任意の管理支店を選択

ステップ2：
購読種別ラジオで「紙版」を選択

ステップ3：
「検索」ボタンを押下

ステップ4：
DevTools でリクエストパラメータと一覧テーブルの結果を確認

### 期待結果

ステップ1：
管理支店が選択され、「支店」プルダウンの選択肢が選択した管理支店配下の支店に絞り込まれること

ステップ2：
購読種別が選択されること

ステップ3：
HTTPステータスコード200が返却されること（AND 条件で適用される）

ステップ4：
クエリパラメータに `kanri_shiten_id` と `dokusya_shubetsu=1` が含まれ、両条件を満たす購読者のみ表示されること

補足：
・複数検索条件は AND で結合されること
・空白項目はクエリに含まれないこと（undefined として扱われる）
・支店プルダウンは管理支店の選択値に応じて連動すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-025 — 検索 — 適用日による履歴抽出

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：購読者履歴（`t_dokusya_rireki`）に複数の変更適用日のレコードが登録済

### 手順

ステップ1：
適用日を両方空欄のまま「検索」を押下

ステップ2：
一覧テーブルの結果を確認

ステップ3：
詳細検索エリアで適用日の開始日に `2025/01/01`、終了日に `2025/06/30` を入力し、「検索」を押下

ステップ4：
一覧テーブルの結果を確認

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること

ステップ2：
購読者履歴の最新データフラグ（`saishin_data_flg=TRUE`）のレコードが抽出されること

ステップ3：
HTTPステータスコード200が返却されること

ステップ4：
購読者履歴（`t_dokusya_rireki`）の変更適用日が `[2025/01/01, 2025/06/30]` の範囲内のレコードが抽出されること

補足：
・適用日が両方空欄の場合：`saishin_data_flg=TRUE` のレコードを抽出（API設計書「処理手順 4.3」準拠）
・適用日が入力されている場合：履歴テーブルの変更適用日が範囲内のレコードを抽出（from のみ → `>= from`、to のみ → `<= to`、両方 → 範囲内）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-026 — 検索クリア — 全条件をリセットして1ページ目に戻る

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・検索画面を開いた状態
  - ・組合員コード `K0000` で検索済、2ページ目を表示中、ソート 組合員コード降順を適用済

### 手順

ステップ1：
「検索クリア」ボタンを押下

ステップ2：
検索フォームの内容を確認

ステップ3：
ページネーションとソートアイコンの状態を確認

ステップ4：
一覧テーブルの内容を確認

### 期待結果

ステップ1：
検索条件がクリアされること、トースト `検索条件をクリアしました。` が表示されること

ステップ2：
全検索項目が空白にリセットされること

ステップ3：
1ページ目に戻ること（現在ページが「1」表示）、ソートアイコンが初期状態に戻ること

ステップ4：
DataScope適用後の購読者レコードが既定順（最新順）で表示されること

補足：
・ACSMS-MSG-014-004（検索条件をクリアしました。）該当
・全検索条件 + ソート + ページが初期状態にリセットされること
・検索クリア時の確認モーダルは表示されないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-027 — 検索結果なし — ACSMS-MSG-014-002 表示

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・検索画面を開いた状態
  - ・事前データ：`kumiaiin_code='ZZZ99999'` に該当する購読者未登録

### 手順

ステップ1：
組合員コードに `ZZZ99999` を入力し、「検索」を押下

ステップ2：
画面表示を確認

ステップ3：
一覧テーブル領域を確認

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること（`data: []`、`meta.total: 0` を返却）

ステップ2：
検索結果なしのメッセージ `検索結果が見つかりませんでした。` が一覧テーブル外に表示されること

ステップ3：
データ行が表示されないこと、ページネーションは「全 0 件」と表示されること

補足：
・ACSMS-MSG-014-002 検索結果が見つかりませんでした。
・空メッセージは a-table の `#emptyText` スロットではなく、テーブル外の兄弟要素として表示されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-028 — ソート — 列ヘッダークリックの3状態切替（検索条件保持）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・検索画面を開いた状態
  - ・事前データ：複数の購読者レコードが登録済

### 手順

ステップ1：
組合員コード列ヘッダーを押下（1回目）

ステップ2：
組合員コード列ヘッダーを押下（2回目）

ステップ3：
組合員コード列ヘッダーを押下（3回目）

ステップ4：
DevTools で各押下時のリクエストパラメータを確認

### 期待結果

ステップ1：
昇順（ASC）でソートされること、ヘッダーに ▲ アイコンが表示されること（クエリパラメータ `sort_by=kumiaiin_code&sort_order=asc`）

ステップ2：
降順（DESC）でソートされること、ヘッダーに ▼ アイコンが表示されること（クエリパラメータ `sort_by=kumiaiin_code&sort_order=desc`）

ステップ3：
ソートなし（初期状態）に戻ること、アイコンが非表示になること（クエリパラメータからソート関連項目が除去される）

ステップ4：
各クリックで GET `/api/v1/dokusya` が実行され、ソート条件が反映されること。現在の検索条件およびページサイズが保持されること

補足：
・ソート対象カラム：管理支店・支店・組合員コード・販売店コード・購読開始日・購読中止日（API設計書 sort_by 許可カラム準拠）
・ソート切替ルール：1回目=昇順 → 2回目=降順 → 3回目=解除

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-029 — ページネーション — ページサイズ切替と検索条件保持

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・検索画面を開いた状態
  - ・事前データ：自JAの購読者を 124 件登録済

### 手順

ステップ1：
氏名に `山` を入力し、「検索」を押下

ステップ2：
ページサイズドロップダウンで「50 / 頁」を選択

ステップ3：
ページネーションで「2」を押下

ステップ4：
DevTools で各リクエストの `per_page` および検索条件パラメータを確認

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること（`山` を含む購読者が表示される）

ステップ2：
表示件数が 50 件に切り替わること、1ページ目に戻ること（`per_page=50`）

ステップ3：
51〜100 件目が表示されること

ステップ4：
クエリパラメータに `full_name=山&page=2&per_page=50` がすべて含まれること

補足：
・最大 per_page は 100（API設計書準拠）
・per_page 切替時は1ページ目に戻ること
・ページ移動時にも検索条件が保持されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-030 — 購読者削除 — 正常系（論理削除 + 監査ログ）

- 観点ID: VP-C-03
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：`dokusya_id=1001, kumiaiin_code='K000001', shimei_sei='山田', shimei_mei='太郎', dokusya_shubetsu=1, shiharai_hoho=1`（紙版、関連レコードなし）

### 手順

ステップ1：
一覧テーブルで `dokusya_id=1001` の行の「削除」リンクを押下

ステップ2：
確認モーダルの内容を確認

ステップ3：
「はい」ボタンを押下

ステップ4：
DB 確認: `SELECT deleted_at, updated_by FROM t_dokusya WHERE dokusya_id = 1001`

ステップ5：
DB 確認: `SELECT * FROM t_log WHERE target_table = 't_dokusya' AND target_id = 1001 AND operation = 'DELETE' ORDER BY log_datetime DESC LIMIT 1`

ステップ6：
画面の一覧テーブルを確認

### 期待結果

ステップ1：
削除確認ダイアログが表示されること

ステップ2：
モーダルにメッセージ `この購読者を削除してもよろしいですか？` が表示されること、ボタンは「はい」（赤色）／「いいえ」であること

ステップ3：
HTTPステータスコード200が返却されること（レスポンス `{ "message": "削除しました。" }`、トースト `削除しました。` 表示）

ステップ4：
1 行確認、`deleted_at` が現在時刻（NOW()）でセットされること、`updated_by` がテストアカウントの `account_id` と一致すること

ステップ5：
1 行確認、`log_type=1`、`gamen_name='購読者明細検索画面 (ACSMS-SCR-014)'`、`account_id` がテストアカウントと一致、`result_status=1`、`before_value` JSON に削除前のレコードデータ（パスワード等の機密情報を除く）が記録、`after_value` は空文字列であること

ステップ6：
削除された購読者が一覧から消えること、検索結果一覧が再読み込みされること

補足：
・ACSMS-MSG-014-010（確認ダイアログ）、ACSMS-MSG-014-011（削除完了）該当
・主DML（UPDATE t_dokusya SET deleted_at）+ 監査ログINSERT が単一トランザクション内でコミットされること
・物理削除ではなく論理削除（`deleted_at` をセット）
・観点 VP-C-03「論理削除 + FK 関連」準拠

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-031 — 購読者削除 — 確認モーダルで「いいえ」キャンセル

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：`dokusya_id=1001`（紙版、関連レコードなし）

### 手順

ステップ1：
一覧テーブルで対象行の「削除」リンクを押下

ステップ2：
確認モーダルで「いいえ」ボタンを押下

ステップ3：
DB 確認: `SELECT deleted_at FROM t_dokusya WHERE dokusya_id = 1001`

ステップ4：
DB 確認: `SELECT COUNT(*) FROM t_log WHERE target_id = 1001 AND target_table = 't_dokusya' AND operation = 'DELETE'`

### 期待結果

ステップ1：
確認モーダル `この購読者を削除してもよろしいですか？` が表示されること

ステップ2：
モーダルが閉じること、画面はそのまま（一覧表示維持）であること

ステップ3：
`deleted_at IS NULL`（削除されていない）であること

ステップ4：
0 行（DELETEログ追加なし）であること

補足：
・キャンセル動作で DB 変更なし、ログ記録なし
・キャンセル時には DELETE API は呼び出されないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-032 — 購読者削除 — 関連データありで削除拒否（CONFLICT）

- 観点ID: VP-C-03
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：`dokusya_id=1005`（紙版）、関連テーブルに当該購読者IDを持つレコードが1件以上存在

### 手順

ステップ1：
一覧テーブルで `dokusya_id=1005` の行の「削除」リンクを押下

ステップ2：
確認モーダルで「はい」ボタンを押下

ステップ3：
画面のメッセージ表示を確認

ステップ4：
DB 確認: `SELECT deleted_at FROM t_dokusya WHERE dokusya_id = 1005`

ステップ5：
DB 確認: `SELECT * FROM t_log WHERE target_id = 1005 AND target_table = 't_dokusya' AND log_type = 3 ORDER BY log_datetime DESC LIMIT 1`

### 期待結果

ステップ1：
削除確認ダイアログが表示されること

ステップ2：
削除リクエストが送信されること

ステップ3：
HTTPステータスコード409が返却されること（`error_code: CONFLICT`、トースト `この購読者は関連オブジェクトに紐づいているため削除できません。` 表示）

ステップ4：
`deleted_at IS NULL`（削除されていない）であること

ステップ5：
1 行確認（エラーログ記録、`log_type=3`、`error_message` に CONFLICT 事由）

補足：
・ACSMS-MSG-014-009 該当（API設計書 エラー一覧のCONFLICTメッセージは `関連データが存在するため削除できません。`）
・関連テーブルに対象 dokusya_id を持つレコードが存在する場合、削除を拒否すること
・トランザクションロールバック後、エラーログのみ別途記録されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

クリティカル — FK整合性違反による業務データ破損を防止する重要な検証。

## ACSMS-TC-014-033 — 購読者削除 — 読み取り専用購読者の削除拒否（DOKUSYA_READ_ONLY）

- 観点ID: VP-C-05
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：電子版クレジットカード決済者（`dokusya_id=1010, dokusya_shubetsu=2, shiharai_hoho=6`）、併読者（`dokusya_id=1011, dokusya_shubetsu=3`）が登録済

### 手順

ステップ1：
DevTools で DELETE `/api/v1/dokusya/1010` を直接呼び出し（電子版クレジットカード決済者）

ステップ2：
DevTools で DELETE `/api/v1/dokusya/1011` を直接呼び出し（併読者）

ステップ3：
DB 確認: `SELECT deleted_at FROM t_dokusya WHERE dokusya_id IN (1010, 1011)`

### 期待結果

ステップ1：
HTTPステータスコード403が返却されること（`error_code: DOKUSYA_READ_ONLY`、メッセージ `この購読者は編集・削除できません。（電子版クレジットカード決済者・併読者は読み取り専用）`）

ステップ2：
HTTPステータスコード403が返却されること（`error_code: DOKUSYA_READ_ONLY`、メッセージ `この購読者は編集・削除できません。（電子版クレジットカード決済者・併読者は読み取り専用）`）

ステップ3：
両レコードとも `deleted_at IS NULL`（削除されていない）であること

補足：
・読み取り専用判定：`(dokusya_shubetsu = 2 AND shiharai_hoho = 6) OR dokusya_shubetsu = 3`（API設計書「処理手順 4.3」準拠）
・フロントエンド側で「削除」リンクが非活性となるが、バックエンド側でも直接呼び出しを拒否すること（観点 VP-C-05）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-034 — Excel出力 — 正常系（ファイルダウンロード + 監査ログ）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：自JAの購読者が複数件登録済

### 手順

ステップ1：
管理支店プルダウンで任意の管理支店を選択し、「検索」を押下

ステップ2：
「Excel出力」ボタンを押下

ステップ3：
ダウンロードされたファイル名・形式・ヘッダー行を確認

ステップ4：
DB 確認: `SELECT * FROM t_log WHERE target_table = 't_dokusya' AND operation = 'EXPORT_EXCEL' AND result_status = 1 ORDER BY log_datetime DESC LIMIT 1`

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること（検索条件が適用される）

ステップ2：
HTTPステータスコード200が返却されること、Excelファイルがダウンロードされること、トースト `Excel出力が正常に完了しました。` が表示されること

ステップ3：
ファイル名が `購読者一覧出力_YYYYMMDD_HHmmss.xlsx`（JST）であること、`Content-Type` が `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet` であること、ヘッダー行が `管理支店, 支店, 組合員コード, 購読者名, 連絡先１, 連絡先２, 配達先郵便, 配達先住所, 販売店コード, 販売店名, 購読開始日, 購読中止日`（12列）であること

ステップ4：
1 行確認、`log_type=1`、`operation='EXPORT_EXCEL'`、`result_status=1`、`after_value` JSON にエクスポート条件と件数（`record_count`）が記録されること

補足：
・ACSMS-MSG-014-005（Excel出力が正常に完了しました。）該当
・Excel出力は現在の検索条件を引き継ぎ、page / per_page / sort_by / sort_order は無視されること
・日付は `YYYY/MM/DD` 形式でフォーマットされること（NULL の場合は空文字）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-035 — Excel出力 — 該当データなし（EXPORT_NO_DATA）

- 観点ID: VP-C-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：`kumiaiin_code='ZZZ99999'` に該当する購読者未登録

### 手順

ステップ1：
組合員コードに `ZZZ99999` を入力し、「検索」を押下

ステップ2：
「Excel出力」ボタンを押下

ステップ3：
画面の状態を確認

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること（`data: []`、`meta.total: 0`）

ステップ2：
HTTPステータスコード404が返却されること（`error_code: EXPORT_NO_DATA`、メッセージ `出力データがありません。`）

ステップ3：
トースト `出力データがありません。` が表示されること、Excelファイルはダウンロードされないこと

補足：
・ACSMS-MSG-014-006 該当
・出力対象件数が 0 の場合、API設計書「処理手順 4.3」により HTTP 404（EXPORT_NO_DATA）を返却すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-036 — Excel出力 — 出力件数30000件超過（EXPORT_LIMIT_EXCEEDED）

- 観点ID: VP-F-02
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：検索条件に一致する購読者が 30,001 件以上存在

### 手順

ステップ1：
検索条件を空欄のまま（または広範な条件で）「検索」を押下

ステップ2：
「Excel出力」ボタンを押下

ステップ3：
画面の状態を確認

ステップ4：
DB 確認: `SELECT COUNT(*) FROM t_dokusya WHERE deleted_at IS NULL`

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること（検索結果が表示される）

ステップ2：
HTTPステータスコード409が返却されること（`error_code: EXPORT_LIMIT_EXCEEDED`、メッセージ `出力データ件数が30000件を超えています。`）

ステップ3：
トースト `出力データ件数が30000件を超えています。` が表示されること、Excelファイルはダウンロードされず、処理が停止すること

ステップ4：
対象件数が 30,000 を超えていること

補足：
・ACSMS-MSG-014-012 該当
・出力件数が 30,000 を超える場合、API設計書「処理手順 4.3」により件数チェックで HTTP 409（EXPORT_LIMIT_EXCEEDED）を返却し、データ取得・Excel生成は行わないこと
・観点 VP-F-02「大量データ表示性能」準拠

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 5: 共通エラーハンドリング（Common Error Handling）

## ACSMS-TC-014-037 — 共通エラー — UNAUTHORIZED — セッション切れ時の処理

- 観点ID: VP-A-05
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・検索画面を表示中
  - ・最終操作から 24 時間以上経過し、Redis セッション TTL 失効済（または管理ツールで強制失効済）

### 手順

ステップ1：
「検索」ボタンを押下し、API リクエストを送信

ステップ2：
レスポンス内容と画面遷移を確認

ステップ3：
ブラウザのアドレスバーを確認

### 期待結果

ステップ1：
HTTPステータスコード401が返却されること（`error_code: UNAUTHORIZED`、メッセージ `セッションが切れました。再度ログインしてください。`）

ステップ2：
フロントエンド側で Pinia auth store の user 状態がクリアされ、`/login?redirect=/dokusya` へ自動遷移すること

ステップ3：
URL に `?redirect=/dokusya` が付いて `/login` に遷移済であること

補足：
・セッション失効時はバックエンド側の SessionAuthGuard が 401 を返却し、フロントエンド側 axios インターセプタで一律ログイン画面へ誘導すること
・再ログイン後は redirect パラメータの URL（検索画面）に戻ること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-038 — 共通エラー — BAD_REQUEST — 不正なクエリパラメータ

- 観点ID: VP-D-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・ブラウザ DevTools の Console を開いている

### 手順

ステップ1：
DevTools の Console から `fetch('/api/v1/dokusya?page=-1&sort_by=evil_column&sort_order=invalid', { credentials: 'include' })` を実行

ステップ2：
レスポンスの HTTP ステータスと error_code を確認

ステップ3：
DB 確認: `SELECT COUNT(*) FROM t_dokusya WHERE deleted_at IS NULL`

### 期待結果

ステップ1：
リクエストが送信され、処理が実行されること

ステップ2：
HTTPステータスコード400が返却されること（`error_code: BAD_REQUEST` または `VALIDATION_ERROR`、メッセージ `リクエストパラメータが不正です。` または `入力値が不正です。詳細はerrorsフィールドを確認してください。`）

ステップ3：
DBレコード件数に変化がないこと

補足：
・page < 1、sort_by が許可リスト外、sort_order が asc/desc 以外の場合はすべて 400 で拒否されること
・許可された sort_by 値：kanri_shiten_id, shiten_id, kumiaiin_code, hanbaiten_id, shoki_dokusya_kaishi_date, dokusya_chushi_date, updated_at

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-039 — 共通エラー — VALIDATION_ERROR — 複数検索条件の同時違反

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・検索画面を表示中、詳細検索エリアを展開

### 手順

ステップ1：
組合員コードに 21 文字、メールアドレスに `invalid-email` を入力

ステップ2：
「検索」ボタンを押下

ステップ3：
レスポンスボディを確認

### 期待結果

ステップ1：
入力受付が完了すること

ステップ2：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、メッセージ `入力値が不正です。詳細はerrorsフィールドを確認してください。`）

ステップ3：
レスポンスボディの `errors` 配列に2件含まれること：
・`{ field: 'kumiaiin_code', message: <最大長メッセージ> }`
・`{ field: 'email', message: '正しいメール形式を入力してください。' }`

補足：
・複数項目違反時は1リクエストで全違反を返却すること
・各違反は対応する検索項目下に表示されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-040 — 共通エラー — DATA_SCOPE_VIOLATION — 他スコープのExcel出力

- 観点ID: VP-A-02
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_KANRI_SHITEN（自管理支店 = kanri_shiten-001）
  - ・事前データ：他管理支店（kanri_shiten-002、自JA内）が存在

### 手順

ステップ1：
DevTools で GET `/api/v1/dokusya/export?kanri_shiten_id={kanri_shiten-002のid}` を直接呼び出し

ステップ2：
レスポンスを確認

ステップ3：
DevTools で GET `/api/v1/dokusya?kanri_shiten_id={kanri_shiten-002のid}` を直接呼び出し

### 期待結果

ステップ1：
リクエストが送信され、処理が実行されること

ステップ2：
HTTPステータスコード403が返却されること（`error_code: DATA_SCOPE_VIOLATION`、メッセージ `このデータへのアクセス権限がありません。`）

ステップ3：
検索（一覧取得）でも他管理支店を指定した場合、HTTPステータスコード403が返却されること（`error_code: DATA_SCOPE_VIOLATION`、メッセージ `このデータへのアクセス権限がありません。`）

補足：
・検索・Excel出力ともに DataScope が適用され、自スコープ外の `kanri_shiten_id` を条件に指定した場合は DATA_SCOPE_VIOLATION（HTTP 403）を返却すること
・削除（DELETE）は URL のIDが対象を名指しするため、出スコープアクセスは存在隠蔽の NOT_FOUND（HTTP 404）を返却する点が検索・出力と異なること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-041 — 共通エラー — NOT_FOUND — 存在しない／論理削除済 ID 削除

- 観点ID: VP-D-02
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：`dokusya_id=99999` は未登録、`dokusya_id=99998` は論理削除済（`deleted_at IS NOT NULL`、自JA）

### 手順

ステップ1：
DevTools で DELETE `/api/v1/dokusya/99999` を直接呼び出し

ステップ2：
DevTools で DELETE `/api/v1/dokusya/99998` を直接呼び出し

ステップ3：
各レスポンス内容を確認

### 期待結果

ステップ1：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`、メッセージ `指定された購読者が見つかりません。`）

ステップ2：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`、メッセージ `指定された購読者が見つかりません。`）

ステップ3：
両ケースともレスポンスボディが同一形式であること

補足：
・存在しないリソース／論理削除済リソースへのアクセスは一律 NOT_FOUND として返却されること
・論理削除済レコードは検索対象から除外されること（`deleted_at IS NULL` フィルタ）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-042 — 共通エラー — INTERNAL_SERVER_ERROR / TOO_MANY_REQUESTS

- 観点ID: VP-D-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・検索画面を表示中
  - ・テスト環境でバックエンド側に意図的に内部エラーをトリガー、別途レート制限超過スクリプトを準備

### 手順

ステップ1：
テスト環境でバックエンド側に内部エラーを発生させた状態で「検索」または「Excel出力」を実行

ステップ2：
DB 確認: `SELECT * FROM t_log WHERE log_type = 3 ORDER BY log_datetime DESC LIMIT 1`

ステップ3：
テスト用スクリプトで `/api/v1/dokusya` GET を 1 分間に 100 リクエスト超で送信

ステップ4：
レート制限超過時のレスポンスとトースト表示を確認

### 期待結果

ステップ1：
HTTPステータスコード500が返却されること（`error_code: INTERNAL_SERVER_ERROR`、トースト `システムエラーが発生しました。しばらくしてから再度お試しください。` 表示）。検索画面のまま（リダイレクトなし）、入力した検索条件は保持されること

ステップ2：
1 行確認、`log_type=3`（エラーログ）、`result_status=2`、`error_message` および `stack_trace` に詳細記録（クライアントには非公開）であること

ステップ3：
最初の 100 リクエストは正常応答、それ以降は HTTPステータスコード429が返却されること（`error_code: TOO_MANY_REQUESTS`、メッセージ `リクエスト回数が上限を超えました。しばらくしてから再度お試しください。`）

ステップ4：
トースト `リクエスト回数が上限を超えました。しばらくしてから再度お試しください。` が表示されること

補足：
・ACSMS-MSG-014-003（システムエラー）該当
・利用者向けエラーメッセージは詳細を含まず一般的な文言であること、スタックトレースはレスポンスボディに含まれないこと
・レート制限は NestJS `@Throttle` デコレータ + AWS WAF レート制限の二層でブロックされること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-043 — 共通エラー — ネットワーク切断時の処理

- 観点ID: VP-D-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・検索画面を表示中
  - ・DevTools → Network → ネットワーク切断モードに切替

### 手順

ステップ1：
「検索」ボタンを押下

ステップ2：
画面状態を確認

ステップ3：
ネットワークをオンラインに戻して再度「検索」ボタンを押下

### 期待結果

ステップ1：
トースト `ネットワークエラーが発生しました。接続をご確認ください。` が表示されること

ステップ2：
検索画面のまま、入力値および現在のリストデータが保持されること

ステップ3：
正常に検索結果が表示されること

補足：
・ネットワークエラー検出 → 利用者通知
・入力値が保持され、再送信可能であること
・ネットワークエラーは HTTPステータスコードを持たないため、別途トースト処理が行われること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

# カテゴリ 6: 購読中止・総部数表示・削除制限（Function — Stop / Total-busu / Delete）

## ACSMS-TC-014-044 — 購読中止 — 電子版：予約中の終了月がポップアップに復元される

- 観点ID: VP-B-05
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：電子版購読者（`dokusya_id=1020, dokusya_shubetsu=2, shiharai_hoho≠6, seikyu_kaishi_month='202604'`）に 2030/07 の解約予約が登録済（到来日バッチ未実行＝`kaiyaku_flg=false`）

### 手順

ステップ1：
購読者明細検索画面で該当行の「購読中止」リンクをクリックする

ステップ2：
ポップアップの「購読中止日」欄を確認する

ステップ3：
案内メッセージの表示を確認する

### 期待結果

ステップ1：
ポップアップ「購読中止」が表示されること（従来の `既に解約予約されています。…` 警告は表示されず、ポップアップが開くこと）

ステップ2：
終了月ピッカーに `2030/07` が設定されていること（予約中の購読中止日 2030-07-31 の年月）。クリア（×）ボタンが表示されること

ステップ3：
ACSMS-MSG-014-016 `解約予約中です。終了月を選び直すと予約を変更し、空にすると予約を取り消します。` が表示されること

補足：
・予約行は未来日で有効行にならないが、購読中止日のみ予約時点で `t_dokusya.dokusya_chushi_date` へ反映されるため、詳細取得の値から復元できる（API設計書 v1.5）
・紙版（購読種別=1）は従来どおりポップアップを開かず ACSMS-MSG-014-014 を表示すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-045 — 購読中止 — 電子版：終了月を選び直して予約を変更する

- 観点ID: VP-B-05
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：ACSMS-TC-014-044 と同じ（2030/07 の解約予約あり）
  - ・電子版連携（DENSHIBAN_PUSH_ENABLED=true）が有効

### 手順

ステップ1：
「購読中止」ポップアップで終了月を `2030/09` に変更し「確認」をクリックする

ステップ2：
DB 確認: `SELECT rireki_no, dokusya_chushi_date, torikeshi_flg, kaiyaku_flg FROM t_dokusya_rireki WHERE dokusya_id = 1020 ORDER BY rireki_no`

ステップ3：
DB 確認: `SELECT dokusya_chushi_date FROM t_dokusya WHERE dokusya_id = 1020`

ステップ4：
電子版連携ログを確認する

ステップ5：
DB 確認: `SELECT gamen_name, operation, target_table FROM t_log WHERE target_id = 1020 ORDER BY log_id DESC LIMIT 1`

### 期待結果

ステップ1：
トースト ACSMS-MSG-014-018 `購読停止を予約しました。` が表示され、ポップアップが閉じて一覧が再取得されること

ステップ2：
旧予約行の `torikeshi_flg` が `true` になり、打ち消し行（`torikeshi_flg=true`）が1件追加されていること。さらに `dokusya_chushi_date='2030-09-30'`・`kaiyaku_flg=false`・`torikeshi_flg=false` の新しい予約行が1件追加されていること

ステップ3：
`dokusya_chushi_date` が `2030-09-30` に更新されていること

ステップ4：
`action_kbn=cancel`・`cancel_ym=203009` で電子版へ連携されていること（1回のみ）

ステップ5：
`gamen_name='購読者明細検索画面 (ACSMS-SCR-014)'`・`operation='UPDATE'`・`target_table='t_dokusya'` の操作ログが記録されていること

補足：
・旧予約の無効化・新予約の挿入・電子版連携・操作ログは単一トランザクション（API設計書「処理手順 4.5／4.5.1」準拠）
・予約中と同じ終了月のまま「確認」した場合は ACSMS-MSG-014-017 `変更がありません。` を表示し、APIを呼ばないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-046 — 購読中止 — 電子版：終了月をクリアして予約を取り消す

- 観点ID: VP-B-05
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：ACSMS-TC-014-044 と同じ（2030/07 の解約予約あり）
  - ・電子版連携（DENSHIBAN_PUSH_ENABLED=true）が有効

### 手順

ステップ1：
「購読中止」ポップアップで終了月のクリア（×）をクリックし、欄を空にして「確認」をクリックする

ステップ2：
DB 確認: `SELECT rireki_no, dokusya_chushi_date, torikeshi_flg FROM t_dokusya_rireki WHERE dokusya_id = 1020 ORDER BY rireki_no`

ステップ3：
DB 確認: `SELECT dokusya_chushi_date, tetsuzuki_shurui FROM t_dokusya WHERE dokusya_id = 1020`

ステップ4：
電子版連携ログを確認する

ステップ5：
一覧の「購読中止日」列を確認する

### 期待結果

ステップ1：
トースト ACSMS-MSG-014-019 `購読中止を取り消しました。` が表示され、ポップアップが閉じて一覧が再取得されること

ステップ2：
旧予約行の `torikeshi_flg` が `true` になり、打ち消し行（`torikeshi_flg=true`）が1件追加されていること。新しい予約行は追加されていないこと

ステップ3：
`dokusya_chushi_date` が `NULL` に戻り、`tetsuzuki_shurui` は `1`（新規＝購読中）のままであること

ステップ4：
`action_kbn=cancel`・`cancel_ym=`（空文字）で電子版へ連携されていること

ステップ5：
該当行の「購読中止日」が空欄になっていること

補足：
・電子版APIには解約取消専用の処理区分が無いため、`cancel` + 空の `cancel_ym` を送る（顧客判断 2026-08）
・電子版が当該リクエストを拒否した場合は履歴・master・操作ログすべてロールバックされ、電子版が返した理由がポップアップ内に表示されること（クラウド側だけ取消済みにならないこと）
・予約が無い状態で空欄のまま「確認」した場合は ACSMS-MSG-014-020 が返ること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-047 — 購読中止 — 解約確定後は変更・取消できない

- 観点ID: VP-C-05
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：電子版購読者（`dokusya_id=1021, dokusya_shubetsu=2`）の解約が到来日バッチで確定済（`kaiyaku_flg=true` の実解約行あり・`tetsuzuki_shurui=0`）

### 手順

ステップ1：
検索結果で該当行の「購読中止」リンクの状態を確認する

ステップ2：
DevTools で POST `/api/v1/dokusya/1021/stop`（body: `{"dokusya_chushi_date": "2030-09-30"}`）を直接呼び出す

ステップ3：
DevTools で POST `/api/v1/dokusya/1021/stop`（body: `{"dokusya_chushi_date": ""}`）を直接呼び出す

ステップ4：
DB 確認: `SELECT COUNT(*) FROM t_dokusya_rireki WHERE dokusya_id = 1021`

### 期待結果

ステップ1：
「購読中止」リンクが非活性であること（手続種類=0:解約のため）

ステップ2：
HTTPステータスコード400が返却されること（`error_code: VALIDATION_ERROR`、`errors[0].message` が ACSMS-MSG-014-015 `解約が確定済みのため変更できません。再購読は購読者編集画面から行ってください。`）

ステップ3：
ステップ2と同じ400が返却されること

ステップ4：
履歴の件数が呼び出し前後で変化していないこと（行が追加・取消されていないこと）

補足：
・確定済み判定は解約予約行ではなく `kaiyaku_flg=true` を持つ実解約行の有無で行う（API設計書「処理手順 4.4」準拠）
・復帰は購読者編集画面（SCR-011）の再購読で行う
・フロントエンド側でリンクが非活性となるが、バックエンド側でも直接呼び出しを拒否すること（観点 VP-C-05）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-048 — 購読中止 — 最終確認ダイアログ（はい／いいえ）

- 観点ID: VP-E-02
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ：紙版購読者（`dokusya_id=1022, dokusya_shubetsu=1`／解約予約なし）、電子版購読者（`dokusya_id=1020, dokusya_shubetsu=2, seikyu_kaishi_month='202604'`／2030/07 の解約予約あり）

### 手順

ステップ1：
紙版（1022）の「購読中止」ポップアップで購読中止日に `2030/09/15` を選び「確認」をクリックする

ステップ2：
確認ダイアログで「いいえ」をクリックする

ステップ3：
再度「確認」をクリックし、確認ダイアログで「はい」をクリックする

ステップ4：
電子版（1020）の「購読中止」ポップアップで終了月を `2030/09` に変更し「確認」をクリックする

ステップ5：
電子版（1020）の「購読中止」ポップアップで終了月をクリアして「確認」をクリックする

ステップ6：
電子版（1020）の「購読中止」ポップアップで予約中と同じ終了月のまま「確認」をクリックする

### 期待結果

ステップ1：
確認ダイアログ「購読中止確認」が表示されること。本文が ACSMS-MSG-014-021 `2030/09/15で購読を中止します。よろしいですか？`、ボタンが「はい」（赤）／「いいえ」であること

ステップ2：
APIが呼ばれないこと。購読中止ポップアップが再表示され、選択した `2030/09/15` が保持されていること（確認ダイアログの表示中はポップアップが隠れていること）

ステップ3：
APIが呼ばれ、ACSMS-MSG-014-018 が表示されてポップアップが閉じること

ステップ4：
本文が ACSMS-MSG-014-022 `購読中止日を2030/09の月末（2030/09/30）に変更します。よろしいですか？` であること（電子版は選択月と実際に止まる日の両方が示されること）

ステップ5：
本文が ACSMS-MSG-014-023 `購読中止の予約を取り消します。よろしいですか？` であること

ステップ6：
確認ダイアログが表示されないこと。ACSMS-MSG-014-017 `変更がありません。` がポップアップ内に表示され、APIも呼ばれないこと

補足：
・確認ダイアログは入力チェック（選択範囲・無変更）を通過した後にのみ表示する。「はい」を押した後にフォームエラーで弾かれないこと
・確認ダイアログ表示中は購読中止ポップアップを非表示にする。「いいえ」「✕」「ESC」「マスククリック」いずれでも選択内容を保ったまま再表示されること
・APIがエラーを返した場合も購読中止ポップアップが再表示され、エラー内容が表示されること
・ボタンラベルは プロジェクト共通の はい／いいえ（破壊的意図は赤ボタンで示す・`.claude/rules/vue.md §Modal Confirmation`）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-014-049 — 検索結果 — 「全 N 件」の横に購読部数合計「全 M 部」を表示する（#56240）

- 観点ID: VP-B-02
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属）
  - ・自JAに購読者が 25 件以上存在し、`dokusya_busu` の合計が 1ページ分の合計と異なること（例：25件・合計 40 部、1ページ目20件の合計は 32 部）
  - ・そのうち複数の履歴行を持つ購読者が含まれること（適用日絞り込み時の重複検証用）

### 手順

ステップ1：
検索条件を指定せずに検索し、ページネーション部分の表示を確認

ステップ2：
API レスポンスの `meta` を確認
```
GET /api/v1/dokusya?page=1&per_page=20
```

ステップ3：
2ページ目に切り替え、「全 M 部」の値が変化しないことを確認

ステップ4：
検索条件（例：管理支店）を指定して絞り込み、「全 N 件」「全 M 部」の値を確認

ステップ5：
**適用日**を指定して検索し（`t_dokusya_rireki` を INNER JOIN する条件）、部数合計をDBの実値と突き合わせる
```sql
SELECT COALESCE(SUM(t.dokusya_busu), 0) FROM (
  SELECT DISTINCT d.dokusya_id, d.dokusya_busu
  FROM t_dokusya d /* 画面と同じ絞り込み条件 */
) t;
```

ステップ6：
検索結果が0件になる条件で検索し、表示を確認

### 期待結果

ステップ1：
ページネーションの「全 N 件」の横に「全 M 部」が併記されること

ステップ2：
`meta` に `total_busu` が含まれ、値が全件の `dokusya_busu` 合計（例：40）であること。1ページ目に表示されている20件の合計（例：32）**ではない**こと

ステップ3：
ページを切り替えても「全 M 部」の値が変わらないこと（ページングの影響を受けない）

ステップ4：
絞り込み後の件数・部数がいずれも条件に一致する全件で再計算されること

ステップ5：
複数の履歴行を持つ購読者がいても部数が水増しされず、DBの実値と一致すること

ステップ6：
「全 0 件　全 0 部」と表示されること

補足：
・素の `SUM(d.dokusya_busu)` では、適用日絞り込みで `t_dokusya_rireki` を INNER JOIN した際に1購読者が履歴行の数だけ重複して部数が水増しされる。件数側は `COUNT(DISTINCT)` のため影響を受けず、**合計だけがずれる**ため気付きにくい（API設計書 §4.4.1）
・ステップ5はこの不一致を検出するための回帰確認。履歴行が1件しかない購読者だけでは検出できないため、複数履歴を持つ購読者を必ず含めること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

## ACSMS-TC-014-050 — 削除 — 削除できるのは紙版のみ（#56422）

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja_id=100 所属、`dokusya.delete` 権限あり）
  - ・自JAに以下の購読者が存在すること
    - ・(a) 紙版（dokusya_shubetsu=1）
    - ・(b) 電子版・支払方法=口座引落（クレジットカード決済ではない）
    - ・(c) 電子版・クレジットカード決済
    - ・(d) 併読（dokusya_shubetsu=3）

### 手順

ステップ1：
検索結果一覧で (a)〜(d) の「削除」ボタンの活性状態を確認

ステップ2：
API レスポンスの `is_read_only` を確認
```
GET /api/v1/dokusya
```

ステップ3：
(b) 電子版・口座引落の購読者に対して削除APIを直接実行
```
DELETE /api/v1/dokusya/{dokusya_id}
```

ステップ4：
(c) 電子版・クレカ、(d) 併読 に対して削除APIを直接実行

ステップ5：
(a) 紙版の購読者に対して削除を実行

ステップ6：
上記実行後、DBで論理削除状態を確認
```sql
SELECT dokusya_id, dokusya_shubetsu, deleted_at FROM t_dokusya WHERE dokusya_id IN (...);
```

### 期待結果

ステップ1：
(a) のみ「削除」ボタンが活性であること。(b)(c)(d) は非活性であること

ステップ2：
(c) クレカ決済者と (d) 併読者は `is_read_only: true` であること。(b) 電子版・口座引落は `is_read_only: false` だが、削除ボタンは `dokusya_shubetsu = 1` の条件で非活性となること

ステップ3：
HTTPステータスコード400（`error_code: VALIDATION_ERROR`）が返却され、`errors` に `{"field": "dokusya_shubetsu", "message": "紙版の購読者のみ削除できます。"}` が含まれること。`is_read_only` は 併読 と 電子版クレカ しか弾かないため、このケースはここで初めて拒否される

ステップ4：
いずれも削除が拒否されること

ステップ5：
削除が成功し、`削除しました。` が表示されること

ステップ6：
(a) のみ `deleted_at` が設定され、(b)(c)(d) は `deleted_at IS NULL` のままであること

補足：
・電子版・併読の会員は電子版読者管理システムが正であり、クラウド版で行を消すと同期で復活するか、相手システムとの整合が崩れる（顧客要件2026-08）
・購読停止（解約予約）は電子版でも可能。削除できないのは**行**であって購読の停止ではない

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---
