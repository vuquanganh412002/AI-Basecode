---
customer_name: 日本農業新聞様
system_name: クラウド版購読者管理システム
document_name: テスト仕様書
screen_id: ACSMS-SCR-007
screen_name: 支店マスタ登録画面
format_code: 16-BM/PM/VTI
format_version: "1.0"
issue_date: 2026-05-14
test_level: 結合テスト
test_environment: Windows 10/11, Chrome, Edge
author: Kieu Thi Diem
reviewer: Nguyen Huy Dat
---


## 変更履歴

| No. | 発行日 | 版数 | 担当者 | 変更内容 | 確認者 | 承認者 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 2026-05-14 | 1.0 | Kieu Thi Diem | 新規作成 | Nguyen Huy Dat |  |


## システム概要

本システムは、JA向けのクラウド型読者管理システムであり、
購読者情報の管理、購読履歴の管理、口座振替データの管理などの機能を提供する。
主な機能として、購読者情報の登録・更新・検索、
購読内容の変更履歴管理、口座振替データの作成および管理、
ファイルのアップロード・ダウンロード機能、システムのお知らせ管理などを提供する。
また、ユーザーのログイン管理、ログイン履歴の記録、
ユーザー操作ログの記録などのセキュリティ・監査機能をサポートする。

## 資料目的

「支店マスタ登録画面（ACSMS-SCR-007）」において、システム上で新規作成されるテスト仕様書の詳細を記述した資料です。
本資料はISTQBおよびIEEE 829を参考とし、以下の品質基準を満たすものとする。

- 各テストケースは単一のシナリオに基づき作成する（単一責任）。
- 手順は再現可能な粒度で記述し、テストデータを明示する。
- 期待結果は測定可能とする（メッセージ内容、DBクエリ結果、HTTPステータスコード等）。
- 優先度（P0：リリース阻害／P1：高／P2：中）を設定する。


## 関連資料

| No | 資料コード | 資料名 |
| --- | --- | --- |
| 1 | ACSMS-SCR-007 | 支店マスタ登録画面設計書 |
| 2 | ACSMS-SCR-007-api | 支店マスタ登録 API設計書 |
| 3 | testcase-viewpoints | 全システム共通 テスト観点一覧 |


## テストカテゴリ一覧

| # | カテゴリ | テストケース数 |
| --- | --- | --- |
| 1 | アクセス権限制御（Access Control） | 5 |
| 2 | 画面表示・レスポンシブ（Layout & Responsive） | 6 |
| 3 | ヘッダー・パンくず（Header & Breadcrumb） | 4 |
| 4 | 入力バリデーション — 登録画面 | 22 |
| 5 | 業務ロジック — 登録（Function — Create） | 8 |
| 6 | 業務ロジック — 更新（Function — Edit） | 9 |
| 7 | 共通エラーハンドリング（Common Error Handling） | 8 |
|  | 合計 | 62 |

---

# カテゴリ 1: アクセス権限制御（Access Control）

## ACSMS-TC-007-001 — NICHINO_ADMIN による支店マスタ登録画面アクセス許可（JA代行入力）

- 観点ID: VP-A-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: NICHINO_ADMIN
  - ・ログイン済 + MFA認証済
  - ・権限 `shiten.view` / `shiten.create` / `shiten.update` を保持する（顧客CR 2026-08-24 で正式付与、seeder.md §3 マトリクス参照）
  - ・session.ja_id は null（NICHINO_ADMIN はどの JA にも属さない）

### 手順

ステップ1：
ダッシュボードを開き、サイドバーで「支店マスタ」項目を確認

ステップ2：
ブラウザのアドレスバーに `/shiten/create` を入力し、直接アクセス

ステップ3：
フォーム上部に表示される「JA名」（BaseJaDropdown）で対象JAを選択せず登録ボタンを押す

ステップ4：
「JA名」で対象JAを選択後、必須項目を入力し POST `/api/v1/shiten` を送信

### 期待結果

ステップ1：
サイドバーに「支店マスタ」項目が表示されること（権限 `shiten.view` を保持するため）

ステップ2：
`/shiten/create` 登録画面が表示されること。フォーム上部に必須の「JA名」（BaseJaDropdown）項目が表示されること（JA スコープの3ロールには表示されない — [staff-ja-id] 代行入力専用UI）

ステップ3：
クライアント側検証で「JA名」に必須エラー（`必須項目です。`）が表示され、送信されないこと

ステップ4：
HTTPステータスコード201が返却され、`m_shiten` に選択したJAの `ja_id` で新規行が追加されること。`GET /api/v1/shiten/dropdown` 系（管理支店 dropdown）が選択したJAにスコープされ再取得されること

補足：
・フロントエンド側メニュー、フロントエンド側ルーターガード、バックエンド側 APIガードの3層すべてで一貫してアクセスが許可されること
・BE は `dto.ja_id`（代行入力）を受理するが、JAスコープの3ロール（CHUOKAI / JA_HONTEN / JA_KANRI_SHITEN）が同フィールドを送っても無視し常に session.ja_id を使う（クロステナント注入ガード、shiten.service.ts `create()`）
・NICHINO_STAFF は本CRの対象外 — 依然 `shiten.*` 権限を保持せずアクセス禁止のまま

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

クリティカル — 失敗時は RBAC バイパスを意味し、セキュリティインシデントとして扱う。

## ACSMS-TC-007-002 — NICHINO_STAFF による支店マスタ登録画面アクセス禁止

- 観点ID: VP-A-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: NICHINO_STAFF
  - ・ログイン済 + MFA認証済
  - ・権限 `shiten.view` / `shiten.create` を保持しない

### 手順

ステップ1：
ダッシュボードを開き、サイドバーで「支店マスタ」項目を確認

ステップ2：
ブラウザのアドレスバーに `/shiten/create` を入力し、直接アクセス

ステップ3：
DevTools の Console から POST `/api/v1/shiten` を有効なリクエストボディで送信

### 期待結果

ステップ1：
サイドバーに「支店マスタ」項目が表示されないこと

ステップ2：
トースト `アクセス権がありません。` が表示されること、かつ `/dashboard` へ遷移すること

ステップ3：
HTTPステータスコード403が返却されること（`error_code: FORBIDDEN`、メッセージ `この画面へのアクセス権限がありません。`）

補足：
・seeder.md §3 マトリクスにより 支店マスタは CHUOKAI / JA_HONTEN / JA_KANRI_SHITEN のみ操作可。NICHINO_STAFF は対象外。
・DB 変更なし、エラーログ記録あり

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-003 — CHUOKAI による支店マスタ登録画面アクセス（正常系）

- 観点ID: VP-A-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: CHUOKAI（chuokai-001 所属）
  - ・ログイン済 + MFA認証済
  - ・権限 `shiten.view` / `shiten.create` / `shiten.update` を保持

### 手順

ステップ1：
ダッシュボードを開き、サイドバーで「支店マスタ」項目を確認

ステップ2：
「支店マスタ」項目を押下し、`/shiten` 一覧画面を開く

ステップ3：
「新規登録」ボタンを押下し、`/shiten/create` 画面を開く

ステップ4：
DevTools で POST `/api/v1/shiten` を有効なリクエストボディで送信

### 期待結果

ステップ1：
サイドバーに「支店マスタ」項目が表示されること

ステップ2：
支店マスタ明細検索画面が表示されること（自中央会配下のJA支店レコードが表示）

ステップ3：
支店マスタ登録画面が表示されること（フォーム入力可能状態）

ステップ4：
HTTPステータスコード201が返却されること（`message: 登録しました。`、`m_shiten` に1行追加されること）

補足：
・CHUOKAI は自中央会配下の支店マスタを参照／新規登録／更新可能
・データスコープは自中央会配下のJAのみに制限される（DataScope層で制御）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-004 — JA_HONTEN による支店マスタ登録画面アクセス（正常系）

- 観点ID: VP-A-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja-001 所属）
  - ・ログイン済 + MFA認証済
  - ・権限 `shiten.view` / `shiten.create` / `shiten.update` を保持

### 手順

ステップ1：
サイドバーで「支店マスタ」項目を確認

ステップ2：
「新規登録」ボタンから `/shiten/create` 画面を開く

ステップ3：
`/shiten/1/edit` （自JAレコード）に直接アクセス

ステップ4：
DevTools で PUT `/api/v1/shiten/1` を有効なリクエストボディで送信

### 期待結果

ステップ1：
サイドバーに「支店マスタ」項目が表示されること

ステップ2：
支店マスタ登録画面が表示されること（フォーム入力可能状態）

ステップ3：
支店マスタ編集画面が表示されること（既存値がフォームに反映）

ステップ4：
HTTPステータスコード200が返却されること（`message: 更新しました。`、`m_shiten` の対象レコードが更新されること）

補足：
・JA_HONTEN は自JA配下の支店マスタを参照／新規登録／更新可能
・データスコープは自JA配下のみに制限される

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-005 — JA_KANRI_SHITEN による支店マスタ登録画面アクセス（正常系）

- 観点ID: VP-A-04
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_KANRI_SHITEN（ja-001 / kanri_shiten-001 所属）
  - ・ログイン済 + MFA認証済
  - ・権限 `shiten.view` / `shiten.create` / `shiten.update` を保持

### 手順

ステップ1：
サイドバーで「支店マスタ」項目を確認

ステップ2：
URL `/shiten/create` に直接アクセス

ステップ3：
他JAの支店レコード ID（例 `/shiten/999/edit`）に直接アクセス

ステップ4：
DevTools で GET `/api/v1/shiten/999` を直接呼び出し

### 期待結果

ステップ1：
サイドバーに「支店マスタ」項目が表示されること

ステップ2：
支店マスタ登録画面が表示されること

ステップ3：
HTTPステータスコード403または404が返却され、フォームが表示されないこと、`/dashboard` または `/shiten` へ遷移すること

ステップ4：
HTTPステータスコード403が返却されること（`error_code: DATA_SCOPE_VIOLATION`、メッセージ `このデータへのアクセス権限がありません。`）

補足：
・JA_KANRI_SHITEN は自JA配下の支店マスタを参照／新規登録／更新可能
・URL 直接アクセス攻撃に対してもバックエンド側 DataScope ガードで遮断されること（4.3 §ja_id 不一致時 403）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 2: 画面表示・レスポンシブ（Layout & Responsive）

## ACSMS-TC-007-006 — 登録画面 全体レイアウトが設計仕様に一致

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・ブラウザ: Chrome (latest), 解像度 1920×1080

### 手順

ステップ1：
「支店マスタ」→「新規登録」ボタンを押下

ステップ2：
設計仕様書（screen-design.md / index.html）と画面を並べて比較

ステップ3：
各要素の位置を確認（タイトルバー、フォーム配置、ボタン配置）

### 期待結果

ステップ1：
`/shiten/create` 画面が表示されること

ステップ2：
背景色、フォント、フォントサイズ、padding/margin、ボタン色、入力欄スタイルがすべて一致すること

ステップ3：
設計仕様通り表示されること（タイトル「支店情報入力」、登録／前の画面に戻るボタンが下部左寄せ）

補足：
・設計仕様との視覚差異がないこと
・design tokens (`design-tokens.ts`) が適用されていること（ハードコーディング色なし）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-007 — 編集画面 全体レイアウトが設計仕様に一致

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・既存支店レコード `shiten_id = 1` が自JA配下に存在

### 手順

ステップ1：
支店一覧から既存レコードの支店コード列を押下

ステップ2：
設計仕様と比較

### 期待結果

ステップ1：
`/shiten/1/edit` 画面が表示されること、既存値がフォームに表示されること

ステップ2：
登録画面と同一レイアウト + 適切な無効化状態（支店コードが無効、必須マーカー `*` が支店コードラベル横で非表示）

補足：
・編集画面が登録画面と同等の視覚的一貫性を持つこと
・支店コードは編集モードで `:disabled="isEdit"` により無効化される

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-008 — レスポンシブ動作 — PC／タブレット／モバイル ブレークポイント

- 観点ID: VP-E-02
- 種類: Normal (正常)
- 前提条件:
  - ・登録画面を開いた状態

### 手順

ステップ1：
ウィンドウサイズを 1920×1080 (PC) に設定

ステップ2：
768×1024 (Tablet) に変更

ステップ3：
375×667 (Mobile) に変更

### 期待結果

ステップ1：
サイドバー固定表示、フォームが3カラムレイアウトで表示されること（管理支店 / 金融機関支店フラグ、支店コード / 支店名 / 支店名カナ）

ステップ2：
サイドバー折り畳み、フォームレイアウトが維持されること

ステップ3：
サイドバー オーバーレイ表示、フォーム要素が縦スタックで並ぶこと、横スクロールが発生しないこと

補足：
・全3ブレークポイントでレイアウト崩れが発生しないこと、横スクロールが発生しないこと
・フォーム要素はすべて操作可能であること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目)

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-009 — フォーム要素表示 — テキストボックス／ドロップダウン／チェックボックス／ボタン

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・登録画面を開いた状態

### 手順

ステップ1：
各テキストボックス（支店コード／支店名／支店名カナ／備考）のプレースホルダー、フォーマット、状態（有効／フォーカス／無効）を確認

ステップ2：
管理支店 ドロップダウンと、金融機関支店フラグ チェックボックスの表示を確認

ステップ3：
登録 / 前の画面に戻る ボタンのサイズ、位置、色を確認

### 期待結果

ステップ1：
設計通り — プレースホルダー「支店コードを入力してください」「支店名を入力してください」、フォーカス時の枠色変化、無効時はグレーアウト表示されること

ステップ2：
管理支店は「選択してください」のプレースホルダー付きドロップダウン、ACSMS-API-COMMON-004 経由で自JAの管理支店一覧が選択肢として表示されること、金融機関支店フラグは未チェック状態で表示されること

ステップ3：
設計通り。登録ボタンが主ボタン（青）、戻るボタンが副ボタン（白）で左寄せ表示されること

補足：
・全フォーム要素が設計仕様と完全に一致すること
・備考は4行のテキストエリアで表示されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-010 — タブ順序とキーボード操作（Tab / Enter）

- 観点ID: VP-E-03
- 種類: Normal (正常)
- 前提条件:
  - ・登録画面を開いた状態、フォーカスは body

### 手順

ステップ1：
Tab キーを押下していき、フォーカス遷移を確認

ステップ2：
テキストボックス内で Enter キー押下

ステップ3：
ダブルクリック・連打による登録ボタン操作

### 期待結果

ステップ1：
上から下、左から右の順 — 管理支店 → 金融機関支店フラグ → 支店コード → 支店名 → 支店名カナ → 備考 → 登録ボタン → 前の画面に戻るボタン

ステップ2：
フォーム送信がトリガーされないこと（`preventEnterImplicitSubmit` ユーティリティにより Enter による暗黙送信を抑止）

ステップ3：
1回目の押下で送信開始、送信中はボタンが無効状態で表示されること、2回目以降の押下が無視されること、DB に1レコードのみ登録されること

補足：
・タブ順序が設計仕様通り、すべてキーボード操作が可能であること
・vue.md §Block Enter implicit submit に従い、4項目以上のCRUDフォームは Enter による暗黙送信を抑止する

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

二重送信防止 — フォーム送信中のボタン無効化が UX 品質の要となる領域。

## ACSMS-TC-007-011 — 登録画面 初期表示時のデフォルト値確認

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・登録画面を開いた直後

### 手順

ステップ1：
管理支店 ドロップダウンを確認

ステップ2：
支店コード／支店名／支店名カナ／備考の各テキスト欄を確認

ステップ3：
金融機関支店フラグのチェック状態を確認

### 期待結果

ステップ1：
未選択（プレースホルダー「選択してください」が表示）

ステップ2：
全項目が空欄で表示されること

ステップ3：
未チェック状態で表示されること（デフォルト値 false）

補足：
・screen-design.md §1.1 のデフォルト値仕様（全項目空白状態）に完全準拠
・api.md §リクエストパラメータ「`kinyu_shiten_flg` デフォルト: false」に整合

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 3: ヘッダー・パンくず（Header & Breadcrumb）

## ACSMS-TC-007-012 — ヘッダータイトル表示

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・登録画面または編集画面を開いた状態

### 手順

ステップ1：
画面上部のヘッダー左側を確認（登録モード）

ステップ2：
編集モードで同様にヘッダーを確認

### 期待結果

ステップ1：
「支店マスタ登録画面」の文字が表示されること

ステップ2：
「支店マスタ編集画面」の文字が表示されること（router.ts の meta.breadcrumb 配列末尾要素準拠）

補足：
・タイトル文字列が仕様の literal 通りに表示されること
・登録／編集モードでタイトルが切り替わること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-013 — 通知アイコン + ユーザーメニュー表示

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN

### 手順

ステップ1：
ヘッダー右側を確認

ステップ2：
ユーザーアイコンにマウスオーバー

ステップ3：
ユーザーアイコン押下

### 期待結果

ステップ1：
通知アイコン → ユーザーアイコン → ログイン名の順で表示されること

ステップ2：
hover 時に背景色が変色すること

ステップ3：
ドロップダウンが展開され、ログアウト等のメニューが表示されること

補足：
・全 hover / click インタラクションが設計仕様通りに動作すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-014 — パンくずリスト表示（登録画面／編集画面）

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・登録画面または編集画面を開いた状態

### 手順

ステップ1：
登録画面のタイトル下部のパンくず領域を確認

ステップ2：
編集画面のパンくず領域を確認

ステップ3：
パンくず階層を設計仕様と比較

### 期待結果

ステップ1：
`ホーム / 支店マスタ明細検索 / 支店マスタ登録画面` の階層が表示されること

ステップ2：
`ホーム / 支店マスタ明細検索 / 支店マスタ編集画面` の階層が表示されること

ステップ3：
区切り文字、テキスト、リンクの有無が一致すること（中央の「支店マスタ明細検索」がクリック可能なリンク、現在画面はリンクなしの表示）

補足：
・パンくずが正しい階層 + 設計仕様通りに表示されること
・vue.md §Project breadcrumb convention に従い 3階層（ホーム → 一覧 → 登録／編集）で構成される

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-015 — パンくず各リンクの遷移動作

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・登録画面を開いた状態

### 手順

ステップ1：
パンくずの「ホーム」を押下

ステップ2：
登録画面に戻り、「支店マスタ明細検索」を押下

ステップ3：
登録画面に戻り、「支店マスタ登録画面」（現在画面）を押下

### 期待結果

ステップ1：
`/dashboard` へ遷移すること

ステップ2：
`/shiten` 一覧画面へ遷移すること

ステップ3：
現在画面のため、何も起こらないこと（再読み込みしないこと）

補足：
・各リンクが期待通りに動作すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 4: 入力バリデーション — 登録画面

## ACSMS-TC-007-016 — 管理支店 必須チェック

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・登録画面、管理支店以外の他項目は有効値入力済
  - ・テストデータ：支店コード `001`、支店名 `テスト支店`

### 手順

ステップ1：
管理支店 ドロップダウンを未選択のまま登録ボタン押下

ステップ2：
管理支店 ドロップダウンを一旦選択した後、× を押下してクリアし、登録ボタン押下

### 期待結果

ステップ1：
項目直下に `必須項目です。` が表示されること、送信が抑止されること

ステップ2：
項目直下に `必須項目です。` が表示されること（antd の `<a-select allow-clear>` クリア時の `undefined` 値が、Number チェックで必須として判定される）

補足：
・antd の `<a-select allow-clear>` クリア時の `undefined` 値が正しく必須エラーとして処理されること
・フロントエンド側 `validateClient` が `!form.kanri_shiten_id || form.kanri_shiten_id === 0` で必須判定

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-017 — 支店コード 必須チェック（空欄 + 空白文字）

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・登録画面、他項目は有効値入力済

### 手順

ステップ1：
支店コードを空欄のまま登録ボタン押下

ステップ2：
半角スペース3つを入力 → 登録ボタン押下

ステップ3：
全角スペース3つを入力 → 登録ボタン押下

### 期待結果

ステップ1：
項目直下に `必須項目です。` が表示されること、送信が抑止されること

ステップ2：
フロントエンド側で前後空白除去後、空欄として判定され `必須項目です。` が表示されること

ステップ3：
全角スペースは半角数字3桁の形式に該当しないため、`支店コードは半角数字3桁で入力してください。` が表示されること

補足：
・空欄と空白文字のみ入力がすべて必須違反として扱われること
・フロントエンド側 `validateClient` で `form.shiten_code?.trim()` による前後空白除去後の判定

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-018 — 支店コード フォーマットチェック（半角数字3桁）

- 観点ID: VP-B-03
- 種類: Abnormal (異常)
- 前提条件:
  - ・登録画面、他項目は有効値入力済

### 手順

ステップ1：
支店コードに `AB` （半角英字2桁） を入力 → 登録ボタン押下

ステップ2：
支店コードに `12` （2桁） を入力 → 登録ボタン押下

ステップ3：
支店コードに `１２３` （全角数字3桁） を入力 → 登録ボタン押下

ステップ4：
支店コードに `12A` （数字 + 英字混在） を入力 → 登録ボタン押下

### 期待結果

ステップ1：
項目直下に `支店コードは半角数字3桁で入力してください。` が表示されること

ステップ2：
同上メッセージが表示されること（3桁未満）

ステップ3：
同上メッセージが表示されること（全角数字は半角数字でないため拒否）

ステップ4：
同上メッセージが表示されること（数字以外の文字混在）

補足：
・支店コードは半角数字3桁固定（@Matches(/^\d{3}$/)）
・フロントエンド側 `validateClient` + バックエンド側 DTO の双方で同一正規表現を適用

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-019 — 支店コード 最大文字数（境界値）

- 観点ID: VP-B-02
- 種類: Boundary (境界)
- 前提条件:
  - ・登録画面

### 手順

ステップ1：
支店コードに `001` （3桁、正規形式）を入力 → 登録

ステップ2：
支店コードの入力欄に 4桁目以降の入力を試行

ステップ3：
支店コード欄に「12345」（5桁）をペースト

### 期待結果

ステップ1：
正常受付、登録成功（HTTPステータスコード201、`message: 登録しました。`）

ステップ2：
4桁目以降の入力が抑止されること（`<a-input maxlength="3">` 属性による）

ステップ3：
入力欄に先頭3桁 `123` のみ保持されること、4桁目以降は切り捨てられること

補足：
・支店コードは半角数字3桁固定（最大3文字、最小3文字）
・`<a-input maxlength="3">` でフロントエンド側からの入力長を制限

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-020 — 支店コード 重複時のエラー（DUPLICATE_CODE）

- 観点ID: VP-B-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja-001 所属）
  - ・事前データ: `m_shiten` に `ja_id=1, shiten_code='999', deleted_at IS NULL` 登録済

### 手順

ステップ1：
支店コードに `999` を入力し、他項目を有効値で埋め、登録ボタン押下

ステップ2：
DB 確認: `SELECT COUNT(*) FROM m_shiten WHERE ja_id=1 AND shiten_code='999' AND deleted_at IS NULL`

ステップ3：
エラーログ確認: `SELECT * FROM t_log WHERE target_table='m_shiten' AND result_status=2 ORDER BY log_datetime DESC LIMIT 1`

### 期待結果

ステップ1：
HTTPステータスコード400が返却されること（`error_code: DUPLICATE_CODE`、メッセージ `同一の支店コードが既に登録されています。`）、トーストでメッセージが表示されること

ステップ2：
1件のまま変化しないこと（重複登録なし）

ステップ3：
エラーログ1行記録、`log_type=3`、`result_status=2`

補足：
・同一JA内で支店コードの重複は禁止（api.md §4.3 重複チェック）
・他JAでは同一支店コードの存在を許容する（ja_id を WHERE 条件に含めるため）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-021 — 支店名 必須チェック（空欄 + 空白文字）

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・登録画面、他項目は有効値入力済

### 手順

ステップ1：
支店名を空欄のまま登録ボタン押下

ステップ2：
半角スペース5つを入力 → 登録ボタン押下

ステップ3：
全角スペース5つを入力 → 登録ボタン押下

### 期待結果

ステップ1：
項目直下に `必須項目です。` が表示されること、送信が抑止されること

ステップ2：
フロントエンド側で前後空白除去後、空欄として判定され `必須項目です。` が表示されること

ステップ3：
同上メッセージが表示されること（全角スペースも前後空白除去対象）

補足：
・空欄と空白文字のみ入力がすべて必須違反として扱われること
・フロントエンド側 `validateClient` で `form.shiten_name?.trim()` による前後空白除去後の判定

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-022 — 支店名 最大100文字制限（境界値）

- 観点ID: VP-B-02
- 種類: Boundary (境界)
- 前提条件:
  - ・登録画面

### 手順

ステップ1：
支店名に 100文字ぴったりの文字列を入力 → 登録ボタン押下

ステップ2：
支店名の入力欄に 101文字目以降の入力を試行

ステップ3：
支店名に 100文字を超える文字列をペースト

### 期待結果

ステップ1：
正常受付、登録成功（HTTPステータスコード201、`message: 登録しました。`）

ステップ2：
101文字目以降の入力が抑止されること（`<a-input maxlength="100">` 属性による）

ステップ3：
入力欄に先頭100文字のみ保持されること、101文字目以降は切り捨てられること

補足：
・支店名は最大100文字（api.md §リクエストパラメータ 最大長100）
・`<a-input maxlength="100">` でフロントエンド側からの入力長を制限

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-023 — 支店名 特殊文字・HTML／SQL インジェクション安全処理

- 観点ID: VP-A-06
- 種類: Abnormal (異常)
- 前提条件:
  - ・登録画面、他項目は有効値入力済

### 手順

ステップ1：
支店名に `<script>alert('XSS')</script>` を入力 → 登録ボタン押下

ステップ2：
支店名に `'; DROP TABLE m_shiten;--` を入力 → 登録ボタン押下

ステップ3：
支店名に `テスト'シングル"ダブル&アンド<タグ>` を入力 → 登録ボタン押下

ステップ4：
登録後、一覧画面と編集画面で表示確認

### 期待結果

ステップ1：
正常受付、登録成功。一覧画面で `<script>alert('XSS')</script>` がエスケープされてテキストとして表示されること、スクリプトが実行されないこと

ステップ2：
正常受付、登録成功。DB に文字列として保存されること、SQL は実行されないこと（パラメータ化クエリで遮断）

ステップ3：
正常受付、登録成功。特殊文字がエスケープされて表示されること

ステップ4：
画面表示で HTML タグがレンダリングされず、テキストとして安全に表示されること

補足：
・XSS／SQL インジェクション攻撃文字列が安全に処理されること
・Vue テンプレートのデフォルトエスケープ + TypeORM パラメータ化クエリで防御

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-024 — 支店名カナ 任意入力（空欄許容）

- 観点ID: VP-B-01
- 種類: Normal (正常)
- 前提条件:
  - ・登録画面、必須項目は有効値入力済

### 手順

ステップ1：
支店名カナを空欄のまま登録ボタン押下

ステップ2：
DB 確認: `SELECT shiten_name_kana FROM m_shiten WHERE shiten_id={新規ID}`

### 期待結果

ステップ1：
正常受付、登録成功（HTTPステータスコード201、`message: 登録しました。`）、エラーメッセージは表示されないこと

ステップ2：
`shiten_name_kana` カラムが空文字列または NULL で保存されていること（`@Transform(blankToUndef)` で blank → undefined 変換）

補足：
・支店名カナは任意入力（api.md §リクエストパラメータ 必須=−）
・バックエンド側 DTO の `@Transform(blankToUndef)` で空文字列を undefined に変換し、`@IsOptional` でスキップする

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-025 — 支店名カナ 最大100文字制限（境界値）

- 観点ID: VP-B-02
- 種類: Boundary (境界)
- 前提条件:
  - ・登録画面

### 手順

ステップ1：
支店名カナに 100文字ぴったりの文字列を入力 → 登録ボタン押下

ステップ2：
支店名カナの入力欄に 101文字目以降の入力を試行

ステップ3：
支店名カナに 100文字を超える文字列をペースト

### 期待結果

ステップ1：
正常受付、登録成功（HTTPステータスコード201、`message: 登録しました。`）

ステップ2：
101文字目以降の入力が抑止されること（`<a-input maxlength="100">` 属性による）

ステップ3：
入力欄に先頭100文字のみ保持されること、101文字目以降は切り捨てられること

補足：
・支店名カナは最大100文字（api.md §リクエストパラメータ 最大長100）
・`<a-input maxlength="100">` でフロントエンド側からの入力長を制限

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-026 — 金融機関支店フラグ 切替（チェック／未チェック）

- 観点ID: VP-E-01
- 種類: Normal (正常)
- 前提条件:
  - ・登録画面、他項目は有効値入力済

### 手順

ステップ1：
金融機関支店フラグを未チェックのまま登録

ステップ2：
金融機関支店フラグをチェック状態にして登録

ステップ3：
DB 確認: 両ケースとも `SELECT kinyu_shiten_flg FROM m_shiten WHERE shiten_id={新規ID}`

### 期待結果

ステップ1：
DB に `kinyu_shiten_flg = false` で保存されていること（デフォルト値）

ステップ2：
DB に `kinyu_shiten_flg = true` で保存されていること

ステップ3：
両ケースともブール値が正しく保存されていること

補足：
・金融機関支店フラグはチェックボックス（true / false）で表現
・未指定時のデフォルトは false（api.md §リクエストパラメータ）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-027 — 備考 任意入力 + 最大500文字制限（境界値）

- 観点ID: VP-B-02
- 種類: Boundary (境界)
- 前提条件:
  - ・登録画面、必須項目は有効値入力済

### 手順

ステップ1：
備考を空欄のまま登録ボタン押下

ステップ2：
備考に 500文字ぴったりの文字列を入力 → 登録

ステップ3：
備考の入力欄に 501文字目以降の入力を試行

ステップ4：
備考に改行を含む複数行テキストを入力 → 登録

### 期待結果

ステップ1：
正常受付、登録成功。DB の `biko` カラムが空文字列または NULL で保存されていること

ステップ2：
正常受付、登録成功（HTTPステータスコード201、`message: 登録しました。`）

ステップ3：
501文字目以降の入力が抑止されること（`<a-textarea maxlength="500">` 属性による）

ステップ4：
改行を含めて正常に保存されること

補足：
・備考は任意入力 + 最大500文字（バックエンド側 DTO `@MaxLength(500)`）
・空文字列許容（`※空文字許容` 注記、api.md §レスポンスデータ）
・テキストエリア（4行表示）で改行入力可能

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-028 — 管理支店 マスタ整合性チェック（不正 ID 送信）

- 観点ID: VP-B-07
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja-001 所属）
  - ・他JA配下の管理支店 ID（例 kanri_shiten_id=999）は自JAのスコープに存在しない

### 手順

ステップ1：
DevTools の Console から POST `/api/v1/shiten` に `kanri_shiten_id: 999` （存在しないIDまたは他JAのID）を含めて送信

ステップ2：
DB 確認: `SELECT * FROM m_shiten WHERE shiten_code=:新規` （登録されないこと）

### 期待結果

ステップ1：
HTTPステータスコード400または403が返却されること（`error_code: VALIDATION_ERROR` または `DATA_SCOPE_VIOLATION`）

ステップ2：
新規行が追加されないこと

補足：
・管理支店 ID は自JAの `m_kanri_shiten` テーブルに存在し、かつスコープ内であることを確認する
・DataScope層で他JAの kanri_shiten_id を拒否する

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-029 — 入力エラー時の自動フォーカス（先頭エラー項目）

- 観点ID: VP-B-01
- 種類: Normal (正常)
- 前提条件:
  - ・登録画面、全項目空欄

### 手順

ステップ1：
全項目空欄のまま登録ボタン押下

ステップ2：
画面のスクロール位置と自動フォーカスを確認

ステップ3：
1つ目のエラーを修正後、再度登録ボタン押下

### 期待結果

ステップ1：
複数のエラーメッセージが該当項目直下に表示されること

ステップ2：
DOM 順序の先頭エラー項目（管理支店）に自動でフォーカスが当たり、`scrollIntoView` で中央表示されること

ステップ3：
次のエラー項目（支店コード等）にフォーカスが移動すること

補足：
・先頭エラー項目への自動フォーカス + スクロールで利用者が探し回らずに済む
・`FIELD_ORDER` 配列（管理支店 → 金融機関 → 支店コード → 支店名 → 支店名カナ → 備考）の順序で先頭エラーを特定

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-030 — Enter キー押下による暗黙送信の抑止

- 観点ID: VP-E-03
- 種類: Abnormal (異常)
- 前提条件:
  - ・登録画面、各テキストボックスにフォーカス

### 手順

ステップ1：
支店コード入力欄で Enter キー押下

ステップ2：
支店名入力欄で Enter キー押下

ステップ3：
備考テキストエリアで Enter キー押下（改行目的）

### 期待結果

ステップ1：
フォーム送信がトリガーされないこと、POST `/api/v1/shiten` API 呼び出しが発生しないこと

ステップ2：
同上、暗黙送信が抑止されること

ステップ3：
テキストエリアでは改行されること（`<textarea>` 内の Enter は許可）

補足：
・vue.md §Block Enter implicit submit に従い、CRUD フォームは Enter による暗黙送信を抑止
・`preventEnterImplicitSubmit` ユーティリティで `<input>` の Enter を遮断、`<textarea>` の改行は保持

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-031 — 支店コード 前後空白の自動除去

- 観点ID: VP-B-09
- 種類: Normal (正常)
- 前提条件:
  - ・登録画面

### 手順

ステップ1：
支店コードに「 001 」（前後に半角スペース）を入力 → 登録

ステップ2：
登録後、DB の `shiten_code` カラムを確認

### 期待結果

ステップ1：
正常受付、登録成功（HTTPステータスコード201、`message: 登録しました。`）

ステップ2：
DB の `shiten_code` に `001` （前後空白なし）が保存されていること

補足：
・フロントエンド側 / バックエンド側 双方で前後空白除去（trim）
・利用者が誤って空白を含めて入力してもデータ品質を維持する

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-032 — VALIDATION_ERROR レスポンス形状確認（複数項目同時エラー）

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・DevTools の Console から API 直接呼び出し可能

### 手順

ステップ1：
DevTools の Console から POST `/api/v1/shiten` に `{}` （空のリクエストボディ）を送信

ステップ2：
レスポンスを確認

### 期待結果

ステップ1：
HTTPステータスコード400が返却されること

ステップ2：
レスポンス形式が JSON であること、`{ error_code: 'VALIDATION_ERROR', message: '入力値が不正です。詳細はerrorsフィールドを確認してください。', errors: [{ field, message }, ...] }` の構造を持つこと、`errors` 配列に少なくとも `shiten_code`／`shiten_name`／`kanri_shiten_id` の必須エラーが含まれること

補足：
・バックエンド側 `ValidationPipe` の `exceptionFactory` でフィールドエラーを集約する
・フロントエンド側 `useApiForm` が `errors[]` を `fieldErrors` にマッピングする

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-033 — 支店コード 重複（論理削除済レコードのコード再利用）

- 観点ID: VP-B-08
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ: `ja_id=1, shiten_code='888', deleted_at IS NOT NULL` （論理削除済）

### 手順

ステップ1：
支店コードに `888` を入力し、他項目を有効値で埋め、登録ボタン押下

ステップ2：
DB 確認: `SELECT shiten_id, deleted_at FROM m_shiten WHERE ja_id=1 AND shiten_code='888'`

### 期待結果

ステップ1：
正常受付、登録成功（HTTPステータスコード201、`message: 登録しました。`）

ステップ2：
2レコードが存在すること（旧レコードは `deleted_at IS NOT NULL`、新規レコードは `deleted_at IS NULL`）

補足：
・重複チェックは `deleted_at IS NULL` の条件付きで実行される（api.md §4.3）
・論理削除済レコードのコードは再利用可能

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-034 — 全項目最小入力（必須項目のみ）での登録

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・テストデータ：管理支店 ID=1、支店コード `010`、支店名 `最小入力テスト支店`、その他空欄

### 手順

ステップ1：
必須項目のみ入力 → 登録ボタン押下

ステップ2：
DB 確認: `SELECT * FROM m_shiten WHERE shiten_code='010'`

### 期待結果

ステップ1：
HTTPステータスコード201が返却されること（`message: 登録しました。`）、`/shiten` 一覧画面へ遷移すること

ステップ2：
1レコードが INSERT、`shiten_name_kana`／`biko` は空文字列または NULL、`kinyu_shiten_flg=false`（デフォルト）

補足：
・必須項目（管理支店、支店コード、支店名）のみで登録可能であること
・任意項目は空欄／デフォルト値で保存される

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-035 — 全項目フル入力での登録

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・テストデータ：管理支店 ID=1、支店コード `011`、支店名 `フル入力テスト支店`、支店名カナ `フルニュウリョクテストシテン`、金融機関支店フラグ true、備考 `フル入力テスト用備考`

### 手順

ステップ1：
全項目に有効値入力 → 登録ボタン押下

ステップ2：
DB 確認: `SELECT * FROM m_shiten WHERE shiten_code='011'`

### 期待結果

ステップ1：
HTTPステータスコード201が返却されること（`message: 登録しました。`）、`/shiten` 一覧画面へ遷移すること

ステップ2：
1レコードが INSERT、全カラムが入力値通りに保存されていること

補足：
・全項目入力時にも正常登録できること
・各カラムの値の整合性が保たれること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-036 — 管理支店 ドロップダウン スコープ確認

- 観点ID: VP-A-02
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja-001 所属）

### 手順

ステップ1：
登録画面で管理支店 ドロップダウンを開く

ステップ2：
表示される選択肢のリストを確認

ステップ3：
他JA (ja-002) でログインし直し、同じドロップダウンを確認

### 期待結果

ステップ1：
ACSMS-API-COMMON-004（GET `/api/v1/kanri-shiten/dropdown`）で取得した自JA配下の管理支店一覧が表示されること

ステップ2：
自JA (ja-001) 配下の管理支店のみが選択肢として表示されること、他JA配下の管理支店は含まれないこと

ステップ3：
他JA (ja-002) 配下の管理支店のみが表示されること（DataScope層によりJA間で分離される）

補足：
・管理支店プルダウンはセッションの ja_id でスコープされる（ACSMS-API-COMMON-004）
・他JAの管理支店 ID は選択不可

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 5: 業務ロジック — 登録（Function — Create）

## ACSMS-TC-007-037 — 登録 正常系（DB永続化 + 監査ログ）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN（ja-001 所属）
  - ・テストデータ：管理支店 ID=1、支店コード `100`、支店名 `業務ロジックテスト支店`、その他任意項目

### 手順

ステップ1：
有効値を全項目に入力 → 登録ボタン押下

ステップ2：
DB 確認: `SELECT * FROM m_shiten WHERE shiten_code='100' AND ja_id=1`

ステップ3：
監査ログ確認: `SELECT * FROM t_log WHERE target_table='m_shiten' AND operation='CREATE' ORDER BY log_datetime DESC LIMIT 1`

### 期待結果

ステップ1：
HTTPステータスコード201が返却されること（`message: 登録しました。`）、成功トースト `登録しました。` が表示されること、`/shiten` 一覧画面へ遷移すること

ステップ2：
1レコードが INSERT されていること、各カラムが入力値通り、`ja_id` がセッションの ja_id、`created_at` に現在時刻、`created_by` にログインユーザーの account_id が記録されていること

ステップ3：
監査ログ1行記録、`log_type=1`、`result_status=1`、`gamen_name='支店マスタ登録画面 (ACSMS-SCR-007)'`、`operation='CREATE'`、`target_id` に新規 `shiten_id`、`after_value` に登録データの JSON が格納されていること

補足：
・本処理 + 監査ログが単一トランザクションで実行されること（nestjs.md §Audit Log 準拠）
・整合性が保証されていること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-038 — 登録 失敗時の監査ログ記録（DUPLICATE_CODE 経路）

- 観点ID: VP-D-04
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ: `ja_id=1, shiten_code='200', deleted_at IS NULL` 登録済

### 手順

ステップ1：
支店コードに重複コード `200` を入力 → 登録

ステップ2：
監査ログ確認: `SELECT * FROM t_log WHERE target_table='m_shiten' AND result_status=2 ORDER BY log_datetime DESC LIMIT 1`

### 期待結果

ステップ1：
HTTPステータスコード400が返却されること（`error_code: DUPLICATE_CODE`、メッセージ `同一の支店コードが既に登録されています。`）、`m_shiten` には新規行が追加されないこと

ステップ2：
エラーログ1行記録、`log_type=3`、`result_status=2`、`error_message` に DUPLICATE_CODE エラー内容が格納されていること

補足：
・エラーログは本処理のトランザクション外で別途記録される（ロールバック対象外）
・nestjs.md §Audit Log 「エラーログ（log_type=3）はトランザクション外で別途記録する」準拠

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-039 — 登録 トランザクション境界（DB INSERT + 監査ログ）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・監査ログ INSERT が失敗するように DB セットアップ（例: `t_log` テーブルに NOT NULL 制約違反を仕込む）

### 手順

ステップ1：
有効値で登録ボタン押下

ステップ2：
DB 確認: `SELECT COUNT(*) FROM m_shiten WHERE shiten_code='300'`

ステップ3：
監査ログ確認

### 期待結果

ステップ1：
HTTPステータスコード500が返却されること（`error_code: INTERNAL_SERVER_ERROR`）

ステップ2：
0件（業務書き込みがロールバックされていること）

ステップ3：
監査ログ（log_type=1）は記録されないこと、エラーログ（log_type=3）はトランザクション外でロールバック後に記録されること

補足：
・監査ログ失敗 → 業務書き込みロールバックでデータ整合性を維持すること
・nestjs.md §Transactions 準拠

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

監査証跡と DB 状態の整合性を保証する重要テスト。

## ACSMS-TC-007-040 — 登録 戻るボタン押下時の挙動

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・登録画面、入力中（値あり）

### 手順

ステップ1：
登録画面で各項目に値を入力

ステップ2：
「前の画面に戻る」ボタンを押下

ステップ3：
DB 確認

### 期待結果

ステップ1：
入力値がフォーム状態に保持されること

ステップ2：
確認モーダルなしで一覧画面 `/shiten` へ遷移すること（screen-design.md §4 の確認ポップアップは実装でドロップ済み）

ステップ3：
登録されないこと（DBに新規レコードなし）

補足：
・キャンセル時は DB 変更なし、ログ記録なし
・実装では確認ダイアログは表示しない方針（ShitenFormView.vue `onBack` 関数）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

screen-design.md §4 に記載の確認ポップアップは現行実装ではドロップされている。実装方針確認済み。

## ACSMS-TC-007-041 — 登録 二重送信防止（同一データの連続押下）

- 観点ID: VP-C-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・登録画面、有効値入力済

### 手順

ステップ1：
登録ボタンを連打（5秒間に10回程度）

ステップ2：
DB 確認: `SELECT COUNT(*) FROM m_shiten WHERE shiten_code='400'`

### 期待結果

ステップ1：
1回目の押下で送信開始、送信中はボタンが無効状態（`:loading="submitting"`）で表示されること、2回目以降の押下は無視されること

ステップ2：
1レコードのみ登録されていること（重複登録なし）

補足：
・`useApiForm` の `submitting` 状態管理により二重送信を抑止
・送信完了後 / エラー後にボタン状態が元に戻ること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-042 — 登録 成功時の遷移先確認

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・登録画面、有効値入力済

### 手順

ステップ1：
登録ボタン押下

ステップ2：
遷移先URLを確認

ステップ3：
一覧画面に登録された支店レコードが表示されることを確認

### 期待結果

ステップ1：
HTTPステータスコード201が返却されること、成功トースト `登録しました。` が表示されること

ステップ2：
`/shiten` 一覧画面へ遷移すること（router.push name: 'ShitenList'）

ステップ3：
新規レコードが一覧の先頭に表示されること（デフォルトソート `updated_at DESC`）

補足：
・登録後は一覧画面の先頭に新規レコードが表示される
・vue.md §default list sort 準拠

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-043 — 登録 JST タイムスタンプ確認

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・JST 9:00 〜 18:00 の業務時間内に登録
  - ・サーバー側 timezone=Asia/Tokyo

### 手順

ステップ1：
登録ボタン押下

ステップ2：
DB 確認: `SELECT created_at, updated_at FROM m_shiten WHERE shiten_id={新規ID}`

ステップ3：
API レスポンスの `created_at` を確認

### 期待結果

ステップ1：
正常受付、登録成功

ステップ2：
`created_at` / `updated_at` が JST タイムゾーン（`+09:00` オフセット）で記録されていること

ステップ3：
レスポンスの `created_at` が ISO 8601 形式で `+09:00` オフセット付きで返却されること（例: `2026-05-14T10:00:00+09:00`）

補足：
・nestjs.md §Timestamp policy 準拠
・TIMESTAMPTZ + サーバー TZ=Asia/Tokyo で JST 一貫運用

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-044 — 登録 機密情報（before_value/after_value）の安全格納

- 観点ID: VP-D-04
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・登録画面、有効値入力済

### 手順

ステップ1：
登録ボタン押下

ステップ2：
監査ログ確認: `SELECT before_value, after_value FROM t_log WHERE target_id={新規ID} AND operation='CREATE'`

### 期待結果

ステップ1：
正常受付、登録成功

ステップ2：
`after_value` に登録データの JSON が格納されていること、`before_value` は空文字列（CREATE のため）であること、パスワード等の機密情報は含まれないこと

補足：
・nestjs.md §Audit Log 「パスワード等の機密情報は含めないこと」準拠
・before_value は INSERT のため空文字列を設定（api.md §4.5）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 6: 業務ロジック — 更新（Function — Edit）

## ACSMS-TC-007-045 — 編集画面 既存データ表示

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ: `shiten_id=10, shiten_code='500', ja_id=1` 存在

### 手順

ステップ1：
一覧画面で `shiten_code='500'` の支店コード列を押下

ステップ2：
各フィールドの初期値を確認

### 期待結果

ステップ1：
`/shiten/10/edit` 画面が表示されること

ステップ2：
DB の値と完全一致 — 管理支店 / 支店コード / 支店名 / 支店名カナ / 金融機関支店フラグ / 備考 すべて

補足：
・GET /api/v1/shiten/{id} で取得したデータがフォームに反映されること
・支店コードは無効化された状態で表示されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-046 — 編集画面 支店コードが変更不可（読み取り専用）

- 観点ID: VP-A-03
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ: `shiten_id=20, shiten_code='600', ja_id=1`
  - ・編集画面 `/shiten/20/edit` 表示済

### 手順

ステップ1：
画面上で「支店コード」入力欄が無効化されていることを目視確認

ステップ2：
DevTools で input 要素の `disabled` 属性を確認

ステップ3：
DevTools の Console から `fetch('/api/v1/shiten/20', { method: 'PUT', body: JSON.stringify({ shiten_code: '999', shiten_name: 'XXX', kanri_shiten_id: 1 }), headers: { 'Content-Type': 'application/json' }, credentials: 'include' })` を実行

ステップ4：
DB 確認: `SELECT shiten_code FROM m_shiten WHERE shiten_id=20`

### 期待結果

ステップ1：
支店コード入力欄が無効状態で表示、利用者は編集不可

ステップ2：
`disabled` 属性が付与されていること

ステップ3：
HTTPステータスコード200が返却されること（更新自体は成功）

ステップ4：
`shiten_code` カラムは `600` のまま（バックエンド側で `shiten_code` は UpdateShitenDto に含まれないため変更されない）

補足：
・api.md §更新ボディ注記「`shiten_code` は更新不可（画面側でdisabled）。リクエストに含めない」準拠
・フロントエンド側 `:disabled="isEdit"` は UX のみ。実際のセキュリティ境界はバックエンド側 DTO 設計

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-047 — 編集 正常系更新（DB + 監査ログ）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ: `shiten_id=10` 編集可能 state
  - ・編集 input: 支店名 `更新後の支店名`、備考 `更新後の備考`

### 手順

ステップ1：
編集画面で値を変更 → 更新ボタン押下

ステップ2：
DB 確認: `SELECT shiten_name, biko, updated_at FROM m_shiten WHERE shiten_id=10`

ステップ3：
監査ログ確認: `SELECT * FROM t_log WHERE target_id=10 AND operation='UPDATE' ORDER BY log_datetime DESC LIMIT 1`

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること（`message: 更新しました。`）、成功トースト `更新しました。` が表示されること、`/shiten` 一覧画面へ遷移すること

ステップ2：
`shiten_name` が `更新後の支店名`、`biko` が `更新後の備考`、`updated_at` が現在時刻に更新されていること

ステップ3：
1行記録、`before_value` JSON に旧値、`after_value` JSON に新値、`log_type=1`、`result_status=1`、`operation='UPDATE'`

補足：
・DB 更新成功、`updated_at` が自動更新
・監査ログで更新前後の完全比較が可能

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-048 — 編集 支店名変更時の t_dokusya 連動更新

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ: `shiten_id=10, shiten_name='旧支店名'`、`t_dokusya` に当該支店を参照する購読者レコードが複数存在

### 手順

ステップ1：
編集画面で支店名を `新支店名` に変更 → 更新ボタン押下

ステップ2：
DB 確認: `SELECT shiten_name FROM m_shiten WHERE shiten_id=10`

ステップ3：
DB 確認: `SELECT DISTINCT shiten_name FROM t_dokusya WHERE shiten_id=10`

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること（`message: 更新しました。`）

ステップ2：
`m_shiten.shiten_name` が `新支店名` に更新されていること

ステップ3：
`t_dokusya` の関連レコードの `shiten_name` も `新支店名` に同時に更新されていること

補足：
・screen-design.md §3.4 「支店名を変更した場合、関連する t_dokusya テーブルの支店名も更新する」準拠
・単一トランザクション内で `m_shiten` UPDATE + `t_dokusya` UPDATE + 監査ログ INSERT が実行されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

データ整合性に関する重要テスト — 支店名は購読者一覧の表示にも使われる。

## ACSMS-TC-007-049 — 編集 同時編集時の競合処理（楽観ロック）

- 観点ID: VP-C-02
- 種類: Abnormal (異常)
- 前提条件:
  - ・事前データ: `shiten_id=10` 存在
  - ・User A: ブラウザ A で `/shiten/10/edit` 開いて入力中
  - ・User B: 別ブラウザ B で同レコード編集、先に保存

### 手順

ステップ1：
User B が先に有効値で更新ボタン押下 → 成功

ステップ2：
User A が（古い `updated_at` で）更新ボタン押下

ステップ3：
DB 確認: `SELECT updated_at, shiten_name FROM m_shiten WHERE shiten_id=10`

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること、User B の変更が DB に反映されること

ステップ2：
HTTPステータスコード（楽観ロック競合時のエラー HTTP ステータス）が返却されること、User A の変更が拒否されること

ステップ3：
User B の値で保持されていること（User A の上書きが阻止されていること）

補足：
・楽観ロック実装の存在を確認 — 詳細メッセージは仕様未確定のため備考参照
・データ更新ロストを防止すること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

楽観ロック競合時の HTTP ステータスコードおよびエラーメッセージは api.md エラー一覧に未定義。仕様確認後に補完すること（TBD）。

## ACSMS-TC-007-050 — 編集 戻るボタン押下時の挙動

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・編集画面、入力中（既存値変更あり）

### 手順

ステップ1：
編集画面で値を変更

ステップ2：
「前の画面に戻る」ボタンを押下

ステップ3：
DB 確認: `SELECT * FROM m_shiten WHERE shiten_id=10`

### 期待結果

ステップ1：
入力値がフォーム状態に保持されること

ステップ2：
確認モーダルなしで一覧画面 `/shiten` へ遷移すること

ステップ3：
DB の値が変更されないこと（旧値のまま）

補足：
・編集キャンセル時は DB 変更なし、ログ記録なし
・実装では確認ダイアログは表示しない方針

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-051 — 編集 部分更新（変更項目のみ更新）

- 観点ID: VP-C-01
- 種類: Normal (正常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ: `shiten_id=10`、全項目に有効値が設定済
  - ・編集 input: 備考のみ変更、他項目は変更なし

### 手順

ステップ1：
編集画面で備考のみ変更 → 更新ボタン押下

ステップ2：
DB 確認: `SELECT * FROM m_shiten WHERE shiten_id=10`

ステップ3：
監査ログの `before_value` / `after_value` を比較

### 期待結果

ステップ1：
HTTPステータスコード200が返却されること（`message: 更新しました。`）

ステップ2：
`biko` のみが新値、他項目は旧値のまま、`updated_at` のみ更新されていること

ステップ3：
`before_value` JSON と `after_value` JSON で `biko` のみ差分があること

補足：
・部分更新時も全フィールドが UPDATE 文に含まれるが、値は同一なので結果として旧値維持
・他項目に影響がないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目)

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-052 — 編集 入力バリデーション（必須項目を空欄にして更新試行）

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・編集画面 `/shiten/10/edit` 表示済、既存値あり

### 手順

ステップ1：
支店名を空欄にして更新ボタン押下

ステップ2：
管理支店を未選択（× クリア）にして更新ボタン押下

### 期待結果

ステップ1：
項目直下に `必須項目です。` が表示されること、送信が抑止されること

ステップ2：
項目直下に `必須項目です。` が表示されること、送信が抑止されること

補足：
・更新時も登録時と同一のバリデーションルールが適用される
・送信抑止後、DB の値は変更されないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-053 — 編集画面 削除済レコードへの直接アクセス（NOT_FOUND）

- 観点ID: VP-D-02
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN
  - ・事前データ: `shiten_id=99, deleted_at IS NOT NULL` （論理削除済）

### 手順

ステップ1：
URL `/shiten/99/edit` に直接アクセス

ステップ2：
DevTools で GET `/api/v1/shiten/99` を確認

### 期待結果

ステップ1：
HTTPステータスコード404が返却されること、`/dashboard` または `/shiten` へ遷移すること

ステップ2：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`、メッセージ `指定された支店が見つかりません。`）

補足：
・論理削除済レコードはAPIで取得不可（api.md §4.3 `WHERE deleted_at IS NULL`）
・URL 直接アクセス攻撃に対してもバックエンド側で遮断されること

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

---

# カテゴリ 7: 共通エラーハンドリング（Common Error Handling）

## ACSMS-TC-007-054 — 共通エラー UNAUTHORIZED セッション切れ時の処理

- 観点ID: VP-A-05
- 種類: Abnormal (異常)
- 前提条件:
  - ・登録画面で入力中
  - ・セッションが切れた状態（24h 経過 or サーバー側で `DEL session:{sid}` 実行）

### 手順

ステップ1：
登録ボタン押下

ステップ2：
レスポンスを確認

ステップ3：
画面遷移を確認

### 期待結果

ステップ1：
POST `/api/v1/shiten` リクエストが送信されること

ステップ2：
HTTPステータスコード401が返却されること（`error_code: UNAUTHORIZED`、メッセージ `セッションが切れました。再度ログインしてください。`）

ステップ3：
セッションがクリアされ、`/login` 画面へ遷移すること（`?redirect=/shiten/create` クエリ付き）、トーストでメッセージが表示されること

補足：
・axios インターセプタでUNAUTHORIZED を捕捉し、auth store クリア + login 画面遷移
・vue.md §Error Handling Architecture Layer 1 準拠

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-055 — 共通エラー BAD_REQUEST 不正リクエストパラメータ

- 観点ID: VP-D-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・登録画面、DevTools 操作可能

### 手順

ステップ1：
DevTools の Console から POST `/api/v1/shiten` に不正な JSON（例: `'invalid-json'` 文字列）を直接送信

ステップ2：
レスポンスを確認

### 期待結果

ステップ1：
HTTPステータスコード400が返却されること

ステップ2：
レスポンス: `{ error_code: 'BAD_REQUEST', message: 'リクエストパラメータが不正です。' }`

補足：
・リクエストボディの JSON パースエラー時の挙動を確認
・vue.md §Error Handling Architecture Layer 1 BAD_REQUEST 処理

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-056 — 共通エラー VALIDATION_ERROR レスポンス形状検証

- 観点ID: VP-B-01
- 種類: Abnormal (異常)
- 前提条件:
  - ・DevTools の Console から API 直接呼び出し可能

### 手順

ステップ1：
DevTools の Console から POST `/api/v1/shiten` に複数の必須項目を欠いたリクエストを送信

ステップ2：
レスポンスを確認

### 期待結果

ステップ1：
HTTPステータスコード400が返却されること

ステップ2：
レスポンス: `{ error_code: 'VALIDATION_ERROR', message: '入力値が不正です。詳細はerrorsフィールドを確認してください。', errors: [...] }`、`errors` 配列に複数のフィールドエラーが含まれること

補足：
・バックエンド側 `ValidationPipe` の `exceptionFactory` でフィールドエラーを集約する
・フロントエンド側 `useApiForm` が `errors[]` を `fieldErrors` にマッピングする

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-057 — 共通エラー TOO_MANY_REQUESTS レート制限超過

- 観点ID: VP-A-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・登録画面、レート制限が有効（例: 1分あたり100回）

### 手順

ステップ1：
DevTools で POST `/api/v1/shiten` を短時間に連続100回以上送信

ステップ2：
レスポンスを確認

### 期待結果

ステップ1：
最初の N 回は正常応答（または重複コードエラー）

ステップ2：
レート制限超過後、HTTPステータスコード429が返却されること（`error_code: TOO_MANY_REQUESTS`、メッセージ `リクエスト回数が上限を超えました。しばらくしてから再度お試しください。`）、トーストでメッセージが表示されること

補足：
・nestjs.md §Rate Limiting 準拠
・連続攻撃に対する基本防御の確認

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-058 — 共通エラー INTERNAL_SERVER_ERROR サーバークラッシュ模擬

- 観点ID: VP-D-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・登録画面、入力中
  - ・サーバー側で予期しないエラーが発生する条件（DB接続切断、内部エラー等）

### 手順

ステップ1：
サーバー側で意図的にDB接続を切断（または予期しないエラーをスロー）

ステップ2：
登録ボタン押下

ステップ3：
画面表示を確認

### 期待結果

ステップ1：
DB接続が切断された状態

ステップ2：
HTTPステータスコード500が返却されること（`error_code: INTERNAL_SERVER_ERROR`、メッセージ `システムエラーが発生しました。しばらくしてから再度お試しください。`）

ステップ3：
トーストでメッセージが表示されること、画面は登録画面のまま維持されること

補足：
・サーバー側エラーログ（log_type=3）が記録されること
・nestjs.md §Error Handling 準拠

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-059 — 共通エラー NOT_FOUND 編集画面で存在しないIDアクセス

- 観点ID: VP-D-02
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN

### 手順

ステップ1：
URL `/shiten/9999999/edit` （存在しないID）に直接アクセス

ステップ2：
レスポンスを確認

ステップ3：
画面遷移を確認

### 期待結果

ステップ1：
GET `/api/v1/shiten/9999999` リクエストが送信されること

ステップ2：
HTTPステータスコード404が返却されること（`error_code: NOT_FOUND`、メッセージ `指定された支店が見つかりません。`）

ステップ3：
トーストでメッセージが表示されること、`/dashboard` へ遷移すること（ShitenFormView.vue の `catch` 経路）

補足：
・存在しないIDの編集試行時はメッセージ表示 + 一覧／ダッシュボードへ遷移
・vue.md §No standalone error pages 準拠（404 ページなし）

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-060 — 共通エラー ネットワーク切断時の処理

- 観点ID: VP-D-08
- 種類: Abnormal (異常)
- 前提条件:
  - ・登録画面、入力中

### 手順

ステップ1：
DevTools の Network タブでネットワーク切断状態にする（Offline モード）

ステップ2：
登録ボタン押下

ステップ3：
画面表示を確認

### 期待結果

ステップ1：
ネットワーク切断状態

ステップ2：
リクエスト失敗、トーストでメッセージ `ネットワークエラーが発生しました。接続を確認してください。` が表示されること

ステップ3：
画面は登録画面のまま維持されること、入力値も保持されること

補足：
・vue.md §Error Handling Architecture Layer 1 ネットワークエラー処理準拠
・利用者の作業を妨げないこと

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)

## ACSMS-TC-007-061 — 共通エラー DATA_SCOPE_VIOLATION 他JAレコードへのアクセス試行

- 観点ID: VP-A-02
- 種類: Abnormal (異常)
- 前提条件:
  - ・role: JA_HONTEN（ja-001 所属）
  - ・事前データ: `shiten_id=500, ja_id=2` （他JAの支店レコード）

### 手順

ステップ1：
URL `/shiten/500/edit` に直接アクセス

ステップ2：
DevTools で GET `/api/v1/shiten/500` を直接呼び出し

ステップ3：
DevTools で PUT `/api/v1/shiten/500` を呼び出し

### 期待結果

ステップ1：
画面が表示されないこと、`/dashboard` へ遷移すること

ステップ2：
HTTPステータスコード403または404が返却されること（`error_code: DATA_SCOPE_VIOLATION` または `NOT_FOUND`、メッセージ `このデータへのアクセス権限がありません。`）

ステップ3：
HTTPステータスコード403が返却されること（`error_code: DATA_SCOPE_VIOLATION`、メッセージ `このデータへのアクセス権限がありません。`）

補足：
・DataScope 層により他JAレコードへのアクセスが遮断されること
・api.md §4.3 「`ja_id` が一致しない場合：HTTP 403 (`DATA_SCOPE_VIOLATION`)」準拠

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

クリティカル — 失敗時は DataScope バイパスを意味し、セキュリティインシデントとして扱う。

## ACSMS-TC-007-062 — 店舗名 — 銀行charset文字種チェック（顧客要件 2026-06-25）

- 観点ID: VP-D-05
- 種類: Normal / Abnormal
- 前提条件:
  - ・role: 登録/更新権限あり
  - ・金融支店フラグ=ON（JASTEM連携項目が必須・入力対象）

### 手順

ステップ1：
店舗名に `ﾎﾝﾃﾝ BR-1`（半角カナ＋A-Z＋数字＋記号）を入力 → 登録

ステップ2：
店舗名に漢字 `本店` / ひらがな `ほんてん` / 全角カタカナ `ホンテン` / 半角英小文字 `br` / 長音符 `ﾎﾝﾃﾝｰ` / 小書き半角カナ `ｷｬｸ` を入力 → 登録

ステップ3：
店舗名に 16 文字を入力（maxlength超過）

### 期待結果

ステップ1：
正常受付（許容文字 `^[ｱ-ﾟ A-Z0-9.()\-]+$`）

ステップ2：
VALIDATION_ERROR。`errors[]` に `ACSMS-MSG-007-009`「店舗名は半角カタカナ・半角英大文字（A-Z）・半角数字・記号（. ( ) -）のみ入力できます。」

ステップ3：
FE は `maxlength=15` で入力をブロック。API直送の場合は MaxLength 違反で VALIDATION_ERROR

補足：
・委託者名・農協名（JAマスタ SCR-005）も同一ルール。支店名（shiten_name 本体）は別ルール（従来どおり）。

### テスト結果（1回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### テスト結果（2回目）

| 項目 | 値 |
| --- | --- |
| 結果 | - |
| 実績／アウトプット | - |
| 担当者 | - |
| 確認日付 | - |
| バグID | - |

### 備考

(なし)
