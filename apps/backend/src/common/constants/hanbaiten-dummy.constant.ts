/**
 * 電子版単独の購読者に割り当てるダミー販売店のコード。
 *
 * 電子版は紙を配達しないので実在する販売店に紐づかない。それでも JA ごとに
 * このコードの販売店を1件用意して受け皿にする運用とする（顧客要件 2026-08）。
 * 「販売店が空」と「電子版なので販売店を持たない」を画面・帳票から区別できる
 * ようにするのが目的。
 * （`t_dokusya.hanbaiten_id` 自体は NULL 許容 — migration 1783700000000 で
 *  NOT NULL を外している。DB 制約ではなく業務ルール上の受け皿。）
 *
 * 使用箇所:
 * - ACSMS-SCR-011 登録/編集の販売店ドロップダウン
 *   購読種別 = 2:電子版  → このコードの販売店**だけ**を候補にする
 *   購読種別 ≠ 2:電子版  → このコードを**除いた**販売店を候補にする
 * - 読者同期バッチ（dokusya-sync）
 *   電子版単独の行はこのコードの販売店を引いて `hanbaiten_id` に入れる。
 *   当該 JA に未整備なら null のまま取り込む（行は落とさない）。
 *
 * 併読（紙あり）には**付けない**。ダミーは「配達先の販売店が無い」を意味するため、
 * 紙を配る読者に付けると増減連絡票・名簿の配達担当が誤る。
 *
 * FE 側にも同じ値の定数がある（`apps/frontend/src/constants/hanbaiten-dummy.ts`）。
 * 絞り込み自体は BE が行うので FE は「どちら側か」を渡すだけだが、表示・テストで
 * 値を書き写さないための共有。
 */
export const HANBAITEN_DUMMY_CODE = '9999999999';
