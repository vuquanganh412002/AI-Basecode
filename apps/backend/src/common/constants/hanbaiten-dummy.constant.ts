/**
 * 電子版単独の購読者に割り当てるダミー販売店のコード。
 *
 * 電子版は紙を配達しないので実在する販売店に紐づかない。一方 `t_dokusya.hanbaiten_id`
 * は NOT NULL なので、JA ごとにこのコードの販売店を1件用意して受け皿にする
 * （電子版連携仕様書「電子版単独はダミー販売店」）。
 *
 * 選択のルール（顧客要件 2026-08・SCR-011 登録/編集）:
 *   購読種別 = 2:電子版  → このコードの販売店**だけ**を選ばせる
 *   購読種別 ≠ 2:電子版  → このコードを**除いた**販売店から選ばせる
 *
 * FE 側にも同じ値の定数がある（`apps/frontend/src/constants/hanbaiten-dummy.ts`）。
 * 絞り込み自体は BE が行うので FE は「どちら側か」を渡すだけだが、表示・テストで
 * 値を書き写さないための共有。
 */
export const HANBAITEN_DUMMY_CODE = '9999999999';
